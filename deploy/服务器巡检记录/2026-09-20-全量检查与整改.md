# 巡检记录 2026-09-20：全量检查与整改

> 依据 [服务器部署及巡检要求.md](../服务器部署及巡检要求.md) 第一部分第 5 节清单执行。

## 范围

| 服务器 | 用途 |
|--------|------|
| 129.211.65.243 | 生产（agent / agent1） |
| 124.222.3.254 | 测试 / 在线开发（agent / agent2 / agent3 等） |

## 检查结果

| 排查项 | 生产 243 | 测试 254 |
|--------|---------|---------|
| 1. nginx 记录域名 | ✅ main_ext 含 `$host`，日志行尾 `host=agent.aidingyi.cn` | ✅ main 含 `$host`，日志行尾 `host=agent2.aidingyi.cn` |
| 2. 宿主机日志轮转 | ✅ nginx daily / rotate 14 / compress（14 天 < 180 天） | ✅ nginx daily / rotate 52 / compress（52 天 < 180 天） |
| 2b. 应用日志保留期 | ❌ 按日切分文件无过期清理（`log/agent/` 32 个） | ❌ 无过期清理（`agent/log/` 56 个含 6 月文件，agent2 目录 307M） |
| 3. 容器日志轮转 | ✅ daemon.json 10m×3 兜底，5 容器均有 LogConfig | ✅ daemon.json 100m×3 兜底，7 容器均有 LogConfig |
| 3b. 例外项 | 无 | ⚠️ background2 / api3 为 `max-file:1`（截断式） |
| 4. 容器日志大小 | ✅ 最大 aid-postgres 47M（50m×5 封顶内） | ✅ 全部受控（最大 labor-cloud-api 4.3M） |
| 5. 时区 / 磁盘 | ✅ Asia/Shanghai；根分区 23% | ✅ Asia/Shanghai；根分区 63%（临界提醒，巡检关注趋势） |

## 本次整改（当日完成）

1. **nginx 记录域名**：两台 log_format 加 `host=$host`，`nginx -t` 通过后 reload；server 级 `access_log` 同步显式引用格式名（生产 agent1 conf 单独改）
2. **生产 daemon.json** 补默认 `log-opts`（10m×3），下次 docker daemon 重启后对新建容器生效（现有容器均已显式配置，无需专门重启）
3. **三个无限制容器重建**：aid-agent-api1（生产，10m×3）、aid-api-1（测试，100m×3）、c_qba（测试，100m×3），均复刻原端口 / 挂载 / 网络 / 重启策略
4. **应用日志 180 天清理**：两台安装 `/etc/cron.d/aid-log-cleanup`（root 每日 3 点档 find -mtime +180 -delete，范围 `/var/www/agent*/log/` 下 `*.log` / `*.jsonl`）；当前 >180 天文件数为 0
5. **max-file 例外修复**：测试机 aid-agent-background2（agent2/docker-compose.test.yml）、aid-agent-api3（agent3/docker-compose.dev.yml）改 `max-file: 3` 并重建，均恢复 running

## 遗留关注项

- 测试机根分区 63%，持续观察；超 80% 按第二部分 5.1 处理
- 生产 daemon.json 新默认值待下次 daemon 重启加载，届时顺带验证
- 改动文件均留 `.bak_时间戳` 备份（nginx.conf、agent1 conf、daemon.json、两份 compose）
