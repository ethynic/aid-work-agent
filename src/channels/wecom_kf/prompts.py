"""微信客服渠道提示词常量。

本文件定义 wecom_kf 渠道独有的能力约束说明，会在 agent 调用时作为
extra_system_prompt 注入到 LLM 的 system prompt 末尾。

设计动机：微信客服 send_msg 接口有「用户发一条消息后企业最多下发 5 条」
的硬性限制（48 小时窗口内），且单条 text 不超过 2048 字节。若 LLM 一次性
产出多语言/多文档/多表格，会击穿 5 条上限导致末尾内容丢失。

详见：docs/channel/wecom_kf/reply_quota_control_plan.md
"""

# ============ 客服账号到期/积分拦截固定话术 ============
# 由 channel_routes 消息入口拦截使用：到期（expire_at 当天结束）或积分用尽
# （credit_limit>0 且累计 >= 上限）时不调智能体、不计费，仅回固定话术。
MSG_EXPIRED = "本服务已到期，如需继续使用请联系工作人员开通。"
MSG_CREDIT_EXHAUSTED = "本服务积分已用完，如需继续使用请联系工作人员充值。"

WECOM_KF_CHANNEL_PROMPT = """---

## 当前渠道约束（微信客服）

你正在通过「微信客服」渠道与用户对话，该渠道有以下硬性限制，违反会导致消息发送失败：

1. **条数限制**：用户发一条消息后，你最多只能下发 **5 条**消息（包括文字、图片、文件、链接卡片等所有类型）。超过 5 条的部分会被微信拦截，用户收不到。
2. **单条文字限制**：单条文字消息不超过 2048 字节（约 600 汉字）。
3. **单轮产出预算**：为留安全余量，**单轮回复总产出 ≤ 4 个发送单元**（文字段 + 表格图 + 文件卡片各算 1 个单元）。

### 必须遵守的输出规则

**规则 1：多份文档/多语言分批输出**
当用户一次性要求多份内容（多语言翻译、多版本方案、多份报告）时：
- **只产出第 1 份**，其余暂不产出
- 输出后明确询问：「已为您提供 {第1份语言/版本}，是否继续生成 {剩余清单}？回复『继续』即可。」
- 禁止一次性输出 2 份及以上完整文档/翻译

**规则 2：单轮产出上限**
单轮回复最多包含：
- 1 张 md 表格（会自动转成图片）
- 1 份可下载文件（Word/Excel/PDF）
- 必要的简短文字说明（合计 ≤ 3 段，每段 ≤ 600 汉字）
- 若有剩余内容，按规则 1 询问是否继续

**规则 3：长内容主动拆分**
当单份内容（如完整行程方案）文字量超过 600 汉字时：
- 先发简短摘要 + 表格图
- 再问「是否需要查看完整版？回复『继续』获取」
- 不要在一条文字里塞满所有内容

"""
