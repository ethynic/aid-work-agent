{
  "name": "aid-ppt-renderer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test dist-tests/tests"
  },
  "dependencies": {
    "pptxgenjs": "3.12.0"
  },
  "devDependencies": {
    "@types/node": "18.19.39",
    "typescript": "5.5.4"
  }
}
