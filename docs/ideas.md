# 项目开发目录

> 本文件是进行中和待开发内容的索引，链接设计文档和开发计划文档，跟踪开发状态。
> 已完成的功能归档在 [ideas_finished.md](ideas_finished.md)。

---

## 状态说明

| 状态 | 含义 |
|------|------|
| ⛔ 已停止 | 方案或前提已作废，不继续开发 |
| 🔧 部分完成 | 已开始开发，部分阶段完成 |
| 📋 待开发 | 设计完成或进行中，尚未开始编码 |
| 💡 灵感 | 早期想法，尚未正式设计 |

---

## 基础设施

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 1 | 可观测性与质量保障 | 🔧 部分完成 | 分布式追踪 + LLM 质量评估 + 实时监控 + 结构化告警。Phase 1 全部完成（含渠道追踪方案 C：TraceCollector 下沉到 `Agent.process_message`，从 record_service 自动读 source_type，渠道零改造）。1.6（单测/e2e）和 1.7（JSONL 双写迁移）已取消：obs 系统每日真实流量运行已事实验证；JSONL 与 obs 永久并行。Phase 2-4 未开始。2026-07-07 | [设计](infrastructure/observability-design.md) / [延伸设计](infrastructure/observability-channel-sessions-design.md) | [计划](infrastructure/observability-dev-plan.md) |
| 65 | 母体 Agent 收敛（agent.py Kernel 化） | 📋 待开发 | **不设专项重构、不阻塞其他工作**，继续采用“冻结增长 + 有真实需求时伴生拆分”。保留与领域无关的行数守卫、依赖方向和纯 helper 候选；ActionDispatcher、ContinuationManager 等只有在具体功能需要时另行评估，每次拆分必须行为回归全绿。 | [原则](system/agent-kernel-convergence-principles.md) | — |
| 70 | 数据库增量升级脚本 YAML 化 | 🔧 部分完成 | 将 deploy/db_update.sql 全量哈希重跑机制改为 deploy/db_update.yaml + datetime 增量执行（last_datetime），免手动清理、结构强制拦截遗漏时间与顺序颠倒、启动只执行新增块。一个块 = 一个逻辑批次（逻辑相关放一起，不相关分开放）。格式选型 YAML block scalar（SQL 零转义，复用项目已有 PyYAML 生态）。执行粒度 = datetime 块，全部成功才更新 last_datetime，advisory lock + SAVEPOINT + 锁超时重试保留。现有 SQL 由运维手动执行完，无需转换与迁移。2026-09-01 开发进行中：database.py 改造 + db_update.yaml 初始模板 + 单测重写 + rules 更新已就绪，待测试智能体与 CR 智能体独立验证。 | [设计](system/database-db-update-incremental-design.md) | — |
| 71 | 租户附件存储规范整改（第二阶段）| 🔧 部分完成 | 2026-08-14 a50bf60 整改主上传链路后，服务器仍存在多处违规写入。排查确认 7 处现行代码违规：data_analyzer（analysis_charts/analysis_data，生产 9-4 仍活跃）、write_tool（storage/output + 嵌套）、cp_tool（storage/storage 嵌套）、wecom_kf_account（uploads tmp）、渠道媒体（无条件 makedirs + 旧兜底）、lead_manager（storage/exports），另将 LongTermMemory 纳入 storage/tenants/{tid}/memory/（含旧带前缀目录自动迁移）。启用新场景 report/memory/export。存量文件迁移：归属类 cp 合并 + documents.file_path SQL 同步，无法归属类（analysis/output/uploads 旧媒体）mv 到宿主机归档保留 30 天。测试环境已于 2026-09-05 迁移完成；生产环境已于 2026-09-07 迁移完成（归档 /var/www/qb3_upload/agent_storage_legacy_20260907/，106M，30 天后约 2026-10-07 删除；documents 表旧路径命中 0 行无需 SQL 同步；顶层仅剩 memory/tenants/tmp，memory 兜底 1 文件已 cp 归位）。 | — | [迁移方案](plans/plan-storage-legacy-migration.md) |
| 73 | 本地开发调试环境说明（venv / WSL 容器 / Mac 容器） | ✅ 已完成 | 明确三种本地开发调试方式与网络要求：①本地 venv；②WSL2 内容器（要求 WSL mirrored 镜像网络 + 容器 host 网络覆盖文件，不走 NAT，后端落在 localhost:8000 与 venv 一致）；③macOS Docker Desktop 容器（端口发布到 localhost，或 4.34+ 开启 host networking）。新增 `docker-compose.local.hostnet.yml` host 网络覆盖文件。2026-09-08 | [说明](infrastructure/local-dev-environments.md) | — |


## 系统功能

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 5 | 知识库能力增强 | 🔧 部分完成 | Phase 0 知识库分类管理已完成。Phase 1-2 已重排（2026-07-06）：**Phase 1（合规阻塞，2 周）= 文档级权限 + 检索日志**；**Phase 2（销售精度 + 体验，3-4 周）= LLM Rerank + 查询改写 + 质量评估（与 Rerank 配套）+ Pipeline 协调器**。重排理由：原 Phase 1 按实现依赖排序无商业化优先级，现按销售推动力重排——权限是中大型企业上线卡点，质量评估需与 Rerank 配套（否则评估的是基线无意义）。**分类树形化（任意级多级分类，2026-08-25 🔧 部分完成）**：知识库页面左侧文档分类由 1 级改为**任意级树形**，子分类下可直接放文档。核心设计：`knowledge_categories` 加 `parent_id` 自引用（NULL=顶级）；`documents` 加 `sub_category`（文档直接所属分类代号，顶级下为 NULL），**`documents.source_type` 恒为顶级分类代号**——使 LLM 提示词注入（读 `subagent_knowledge_sources`）、跨租户知识库共享（粒度=source_type）、检索过滤（`tenant_range.py` 按 `(tenant_id, source_type)` 精确匹配）全部**零改动**，共享顶级分类=共享其下全部文档。`list_categories` document_count 统计关键改动：顶级分类=该 source_type 全部文档（含子分类，相关子查询按 `source_type` 计数），子分类=直接归属（`sub_category`）文档（相关子查询按 `sub_category` 计数）。API：`create_category` 支持 `parent_id`（校验父存在同租户）、`upload_document`/`upload/batch` 支持 `sub_category`（校验其祖先根分类 source_type 等于入参）、`GET /documents` 支持 `sub_category` 过滤、`delete_category` 禁止删除含子分类的分类。前端：`KnowledgeBase.vue` 左侧改递归树组件 `CategoryTreeItem.vue`（折叠/展开 + 缩进 + 文档数）、添加分类弹框选父分类、上传弹框两级分类选择、点击子分类按 sub_category 筛选；`TenantMgmt.vue` 跨租户共享弹框**只展示顶级分类**（前端过滤 `parent_id == null`）保持勾选现状。数据库变更已同步 `deploy/init-postgres.sql` + `deploy/db_update.sql`（`knowledge_categories.parent_id` + `documents.sub_category` + 两个索引，幂等）。集成测试 `tests/integration/test_knowledge_categories_tree.py` 9 用例通过。**文档移动（2026-08-27 ✅ 已完成开发）**：知识库支持批量移动文档到目标分类（顶部「移动」按钮 + 分类树弹框选目标）。后端 `POST /api/knowledge/documents/move`：`move_documents` 事务内批量 UPDATE `documents.source_type` + `sub_category`（`id = ANY(...)` + `tenant_id` 隔离，`NOT (source_type = %s AND COALESCE(sub_category,'') = COALESCE(%s,''))` 保证 moved 只计真正改分类的行），`upload_document` 的子分类归属校验提取为 `_validate_sub_category` 供两者复用；chunks/向量无需改动（检索 join documents 过滤），`list_categories` 统计自动联动；校验分支（空入参 / 目标子分类不存在 / 传顶级自身 / 不属于入参顶级）均 400。原「跨顶级改分类影响 LLM/共享」顾虑已解除：移动只改文档归属，不改变分类本身，共享按 `(from_tenant_id, source_type)` 授权语义不受影响。前端 `moveDocuments` + `KnowledgeBase.vue` 移动弹框复用 `CategoryTreeItem`（新增 `showActions`/`defaultExpanded` prop 隐藏操作按钮并全部展开）。集成测试 11 用例全绿 + 前端 build 0 错误。**子级文档包含统一（2026-08-27 ✅ 已完成开发）**：修复「顶级分类显示/统计其下所有子级文档，而子分类不显示/不统计其下子级文档」的不一致，统一为**每一级分类都显示并统计其下所有子级文档**（方案 A）。实现：`service.py` 新增 `_get_category_subtree_source_types`（一次查该租户全部分类，内存建树 DFS 收集自身+后代 source_type），`list_categories` 子分类计数与 `list_documents`/`count_documents` 的 `sub_category` 过滤统一展开为 `IN (自身+后代)`；前端零改动。已评估用户建议的 `all_descendants` 物化字段方案：分类量小（单租户几十条）实时计算 <1ms，加字段需表结构变更+写路径重算+一致性维护，无性能收益，不采用。详见[计划](plans/plan-knowledge-category-subtree-filter.md)。**搜索跟随当前选中分类（2026-08-27 ✅ 已完成开发）**：知识库顶部搜索由「本租户全局」改为「跟随左侧选中的分类（含子级）」——前端 `performSearch` 传 `selectedSourceType`/`selectedSubCategory`，`SearchRequest` 新增 `source_type`/`sub_category`，后端 `service.search_documents` 选中子分类时复用 `_get_category_subtree_source_types` 展开子树，`HybridRetriever`/`vector_db.search` 新增 `sub_categories` 参数（SQL `AND d.sub_category = ANY(...)`），向量检索与 FTS 双路过滤一致，未选中分类仍为全局搜索；UI 微调：搜索框加宽（`w-80`→`w-96`）+ placeholder 改为「在当前选中分类（含子级）中搜索文档」。集成测试 14 用例全绿 + 前端 build 0 错误。 | [设计](system/knowledge-base/knowledge-base-enhancement-design.md) | [计划](system/knowledge-base/knowledge-base-dev-plan.md) / [子级文档包含统一](plans/plan-knowledge-category-subtree-filter.md) |
| 75 | 知识库 Excel 行级分块（表头注入 + 格式前置判定） | 🔧 部分完成（2026-09-08 开发完成，待部署实测） | 上传 Excel 时按「一行数据 = 一个 chunk」分块，表头字段名注入每个数据行 chunk（键值对形式），使单个商品/记录可独立命中检索。**现状问题**：①`TextChunker` 按句子边界跨段落合并到 ~512 字符，「一行一片」实际未生效，多个商品行被合并进同一 chunk；②表头行与数据行分离，数据行 chunk 是无字段语境的裸值拼接，检索近似噪声；③所有 sheet 一律按二维表处理，无格式前置判定。**方案**：`ParseResult` 新增 `precomputed_chunks` 结构化分块旁路，ExcelParser 逐 sheet 读取矩阵后先 `detect_layout` 启发式判定（行数/列数一致性/首行像表头打分，零成本无 LLM）；table 布局走行级分块（一行一块 + `[工作表: x] 字段: 值 | ...` 格式 + 超长行切割 + chunk metadata 带 sheet/row_number），freeform 布局回退现有 TextChunker 路径（行为不变）；每个 chunk 注入文档标题前缀（现有路径仅首 chunk 有）。可选 LLM 兜底判定做成开关（默认关，启用需 `record_background_llm_usage` 计费）。不触检索链路、无 schema 变更。2026-09-08 设计定稿。**2026-09-08 开发完成**：`ParseResult.precomputed_chunks` 旁路 + ExcelParser 重构（矩阵读取/`_detect_layout` 六规则启发式/行级分块/`layout_report`）+ service 旁路分支，单测 23 用例 + 知识库回归 103 全绿（`test_knowledge_endpoints.py` 7 个失败为存量问题：测试文件自身用旧列名 `total_chunk`，与本次无关）；独立验证通过（无 P0/P1，P2 已修：wb 异常路径 try/finally、空 sheet 不产垃圾 chunk、分段宽度预留文件名前缀 headroom）；同构实测 6 行 SKU 表 → 6 个独立 chunk 表头全注入（旧路径仅 3 块）。判定规则实现时细化：行宽用行跨度（span）度量防稀疏行误判、<3 行判 freeform（2 行无法区分表头+数据与散文）、单列内容判 freeform。 | [设计](system/knowledge-base/excel-row-chunking-design.md) | — |
| 42 | 租户积分充值与计费 | 🔧 部分完成 | 预付费积分（credit）充值 + 对话消耗积分 + 余额报警 + 账单查询。Phase 1 数据基础+扣费链路（`tenants.credit_balance` / `chat_records.credit_cost` / 新建 `tenant_recharges` 表 / `billing.usage_factor` 配置）✅；Phase 2 平台管理员手动充值（`/api/saas/billing/recharges` + `TenantRecharge.vue`，不可改仅可删除回扣）✅；Phase 3 租户余额/用量明细/充值记录（改造 `TenantTokenUsage.vue` 为「积分用量」、`PlatformTokenUsage.vue` 新增「消耗积分」列、新建 `TenantRechargeRecords.vue`）✅；Phase 4 余额报警三入口（`useCreditCheck.ts` composable + 前端 sendMessage 阻断 + 登录提醒 + 后端 `/api/chat`、`/api/chat/stream`、`channels/session.process_and_persist` 硬阻断）✅；Phase 5 在线支付对接（预留 `payment_orders` 复用）待开发。计费算法：`credit_cost = ceil((prompt_tokens × input_price_per_m + completion_tokens × output_price_per_m) × usage_factor)`，`usage_factor` 默认 100。扣费挂载在 `src/services/session_record.py` 写 `chat_records` 时同步原子 UPDATE。**LLM 计费接入改造（2026-08-12）**：将原未计费的 Embedding/ASR/视频提示词 LLM/background_runner 半夜任务（长期记忆摘要/工作成果复盘/上下文压缩扫描）/对话内后台 LLM（情感/分类/案件匹配/内容生成/数据分析）/知识库文档向量化与摘要 全部接入 `chat_records` 计费体系。`chat_records` 加 `embedding_tokens`/`asr_calls`/`usage_breakdown` JSONB；`token_cost_prices` 加 `embedding_price_per_m`/`asr_price_per_call` + 种子（text-embedding-v3=0.7 元/百万 token，aliyun-nls-asr=0.06 元/次）；新增 `calculate_embedding_credit_cost`/`calculate_asr_credit_cost`；`SessionRecordService` 加 `add_embedding_usage`/`add_asr_usage`/`record_background_llm_usage`（双路径：对话内累加主记录，background_runner 降级独立落库 `source_type=background_llm`）；`TextEmbeddingV3Client` 透出 `last_usage_tokens`/`reset_usage`/`embed_sync`，4 个绕过 client 的检索调用点（hotel/attraction_search_tool + hotel/attraction_retriever）改用统一入口；视频提示词 4 处调用点独立计费 `source_type=video_prompt`（避免放弃创建视频时计费丢失）；`knowledge/service.py` 文档上传独立计费 `source_type=knowledge_embedding`。Phase 1-3 代码完成，单测 50 用例通过；Phase 4 文档同步中。**积分用量明细 6 分项对账（2026-08-15）**：`TenantTokenUsage.vue` 每日用量明细弹框由 3 列（输入/命中缓存/输出 Token）改为 6 列（未命中缓存输入/命中缓存输入/输出/视频模型/ASR/向量模型），仅平台管理员可见，展示 `{数量} × {单价} × {系数} /1M = {积分}` 对账公式；后端 `get_daily_usage_detail` 平台管理员时返回 `breakdown_items` 6 分项结构（从 `usage_breakdown` JSONB 解析）。**老数据降级**（2026-08-14 前 usage_breakdown 无单价/系数/分项积分）：6 列降级只显示数量（token/秒/次），`non_cached_input` 用 `prompt_tokens - cached_input_tokens` 兜底计算，video 分项缺失显示 "-"。**向量模型列恒空修复（2026-08-15）**：实测 8-14 旅游咨询会话 16:46 确实调用了 `attraction_search`/`knowledge_base_search` 并返回酒店/景点向量检索结果，但所有 `chat_records.embedding_tokens=0`——根因是 DashScope TextEmbedding 响应 `resp.usage` 为 **dict**（`{"total_tokens": 9}`），而 `TextEmbeddingV3Client` 用 `getattr(usage, "tokens", None)` 读取（dict 恒返 None）→ `last_usage_tokens` 恒为 0 → 各 `if last_usage_tokens > 0` 计费分支永不触发。修复：`embedding_client.py` 新增 `_extract_usage_tokens()`（兼容 dict/对象双形态，优先 `total_tokens`），`_embed_single_batch`/`embed_sync` 改用它累加 `last_usage_tokens`。命中 billing_audit.md §5 缺口「对话内 RAG embedding 1 处待修复」。旧数据不可追溯补计，修复仅对后续调用生效；单测 25 用例通过 + 容器内实调验证（embed_sync 后 `last_usage_tokens`=9/11，`add_embedding_usage` 正确累加 breakdown）。**续费提醒（2026-08-15）**：租户按过去 n 天日均积分消耗推算余额不足 7 天用量时提醒续费。统计窗口 n：开通 >30 天取 30，否则取开通到今天天数（`created_at` 缺失默认 7）；日均消耗 = `chat_records` + `client_usage_logs` 两表 `SUM(credit_cost)` / n。`renewal_pending` = 余额 ≤0 或（日均>0 且 `int(余额/日均) < 7`）。落点：①管理后台仪表盘新增「续费提醒」卡片（`renewal_pending_count`）→ 租户管理列表 `?renewal=1` 过滤 + 新增「日均使用积分」「预估可用天数」2 计算列（<7 红、7~30 黄、>30 黑、暂无数据灰字）；②租户前台登录后 `useCreditCheck.ts` 由固定阈值 100 改为 `renewal_pending` 判断（toast.success 续费提示 + 当天去重），平台管理员跳过检查。核心实现 `src/saas/services/renewal.py`（批量聚合 SQL，避免逐租户 N 次查询）；`TenantTokenUsage.vue` 日均消耗口径统一为动态 n。2026-08-15。**qwen3.7-flash 分段计价（2026-08-15）**：百炼 qwen3.7-flash 按单次请求输入 token 数分段计价（0<T≤32K=输入0.2/输出0.8、32K<T≤256K=0.6/2.4、256K<T≤1M=1.2/4.8，缓存命中按输入价 20%）。chat_records 把多轮 LLM 调用累加后的单一单价无意义，采用**逐轮计费累加成本 + 合并反向单价**（`{成本价合计}/{token数合计}`）入 `usage_breakdown`，前端积分用量明细统计零修改。表结构 `token_cost_prices` 加 `tiered_pricing` JSONB（含三档 max_input/input/cached_input/output 单价）；billing.py 原 `calculate_credit_cost_with_breakdown` 加 tiered 单次查档分支（单次调用点自动正确）+ 新增 `calculate_llm_credit_cost_with_breakdown`（多轮合并）；session_record.py `add_llm_usage` 记录每轮 usage 快照、`save()` 改调新函数。状态：📋 待开发（2026-08-15 设计定稿）。**qwen3.7-flash 平替 deepseek-v4-flash（2026-08-16 开发完成）**：deepseek-v4-flash 官方涨价至 3.0/9.0/0.1；选定 qwen3.7-flash（0.2/0.8 分段）+ `enable_thinking:false`（关闭思考 1-2s）+ 显式缓存（cache_control ephemeral，命中按 10%）作平替，实测典型场景降幅约 91%。✅ 开发完成：config.yaml/settings.py 新增 `llm.enable_thinking`/`llm.context_cache`；qwen.py chat/stream_chat 写入 enable_thinking + base.py `_format_messages` 首条 system 转数组 + `cache_control`（仅 qwen 系文本模型生效，排除 qwen-vl 视觉模型防 400，视频提示词路径安全）；tiered_pricing cached 单价修正 20%→10%（0.04→0.02 等，init-postgres.sql + db_update.sql 2026-08-16）；QWEN_MODEL_CODE 切 qwen3.7-flash。单测 12 用例通过 + 回归 133 通过。**显式缓存创建计费补齐（2026-08-16）**：缓存创建 token（cache_control 首条消息触发）按输入价 **125%** 独立核算（百炼官方口径），不再混入 non_cached 按 100% 计费。链路：qwen.py `_parse_response` 读 `prompt_tokens_details.cache_creation_input_tokens` 透出 usage.cache_creation_tokens → billing.py tiered（`calculate_llm_credit_cost_with_breakdown` 逐轮 `creation×input_price×1.25`）+ 非 tiered（`calculate_credit_cost_with_breakdown` 新增 `cache_creation_input_tokens` 参数，`max(prompt-cached-creation,0)` 剔除）→ breakdown 新增 `cache_creation_input_per_m`/`cache_creation_input` 分项。所有计费入口透传：session_record 主 save/background/admin 三路径、client_binding_db（×5 系数）、memory_summarizer、work_outcome_review、knowledge 摘要、reports 日报生成。管理后台用量明细对账由 6 分项扩为 **7 分项**（新增「缓存创建输入」列，后端 `_parse_breakdown_items` + 前端 `TenantTokenUsage.vue`/`billing.ts`）。单测新增 cache_creation 4 用例（tiered 单次/合并/非 tiered/无缓存单价兜底）+ qwen `_parse_response` 3 用例；累计 86 单测 + 9 明细 API 集成 + 84 报告/客户端回归全绿。[调研](research/deepseek-v4-flash-replacement-research.md) / [平替计划](plans/plan-qwen3-7-flash-replacement.md)。**上下文缓存优化（显式末尾标记 + 工具结果截断，2026-08-17 🔧 部分完成）**：实测生产记录 5551（qwen3.7-flash，244K prompt）仅缓存首条 system（5103 token），其余 ~239K 按 100% 原价计费、成本高于 deepseek。实验证实：隐式缓存对生产不可用（legacy/MaaS 两条端点 + `enable_thinking=false` 均无命中，仅默认思考模式偶发 ~30%）；显式**末尾标记**（`cache_control` 放消息数组最后一条）创建的缓存块覆盖全数组，13/27 content 块均完整命中（10%），仅新增尾部按 125% 创建——输入成本约降一个数量级。Phase 1 ✅ 已完成开发并提交（e48d744）：`base.py` `_format_messages` 缓存标记由「首条 system」改为「最后一条消息」，单测 18 用例全绿 + 相邻回归 131 通过 + 三智能体流程通过。Phase 2 工具结果截断（计划定稿）：超 12K 字符的工具结果截到 8K（保头 6K + 保尾 2K + 省略标记），主/子智能体两个接入点通用截断，降低输入基数（缓存未命中/首次调用按 100% 计费时收益最大）。详见[上下文缓存优化计划](plans/plan-qwen3-7-flash-context-cache-optimization.md) / [工具结果截断计划](plans/plan-tool-result-truncation.md)。 | [设计+计划](system/saas/tenant-credit-billing-design.md) / [LLM 计费接入设计](system/saas/llm-billing-integration-design.md) / [开发计划](plans/plan-llm-billing-integration.md) / [qwen3.7-flash 分段计价计划](plans/plan-qwen3-7-flash-tiered-pricing.md) / [qwen3.7-flash 平替计划](plans/plan-qwen3-7-flash-replacement.md) / [上下文缓存优化计划](plans/plan-qwen3-7-flash-context-cache-optimization.md) / [工具结果截断计划](plans/plan-tool-result-truncation.md) | - |
| 48 | 连接中心 | 🔧 部分完成 | 租户前台新增「连接中心」一级菜单（`/t/:tenant_id/connections`），单页面 4 Tab：API 配置 / 环境变量 / 内置连接器 / 自定义连接器。**对标 Coze/Dify/n8n/Anthropic Claude 的连接器抽象**。搬迁现有「管理后台 - 租户管理 - 编辑租户 - 数字员工授权 - API 配置 + 环境变量」到前台（原功能保留给平台管理员），并扩展内置连接器（12-15 个：Tavily/SMTP/飞书/钉钉/企微/企查查/高德/天气/百度翻译/网页抓取/OCR/图片生成）和自定义连接器（MCP/OpenAPI 双轨待调研）。**关键决策**：①现有 `subagent_env_vars` 表不加密升级，仅新建 `tenant_connector_configs` 表用 Fernet 加密敏感字段；②加密能力抽取自 `wecom_personal_rpa/secret_crypto.py` 到 `src/core/secret_crypto.py` 公共模块（环境变量优先级 `APP_SECRET_KEY > RPA_SECRET_KEY` 向后兼容）；③菜单形态为一级菜单 + 4 Tab 单页面（不做 flyout）；④自定义连接器技术路线 Phase 4 启动前先调研后定。**Phase 1 已完成（2026-07-30）**：新建 ConnectionCenter/ApiConfigTab/EnvVarsTab/connections.ts，修改 MenuSidebar 和 agentRoutes；4 Tab 容器立起来，Tab 1/2 复用现有 API 搬迁、Tab 3/4 显示「即将上线」占位，原 TenantMgmt.vue 弹窗保留给平台管理员；前端 build 0 错误，后端零改动。Phase 2 框架+5 个高优先级连接器+加密+新表（3-5 天，完整三智能体）+ Phase 3 补齐 7-10 个连接器（2-3 天）+ Phase 4 自定义连接器（5-7 天，含调研）。 | [设计](system/connection-center-design.md) | [计划](plans/plan-connection-center.md) |
| 51 | 第一方 CLI / MCP Provider 架构规范 | 📋 待开发 | 所有第一方 CLI 必须成为独立标准 MCP Provider，同时支持 aid-work-agent Web Local Tool Runtime、未来 Agent Desktop、Codex、WorkBuddy 等 Host；统一 operation/CLI/MCP 分层、manifest、SERVER/LOCAL_REQUIRED/EITHER、结构化结果、effect、版本、签名分发和跨 Host conformance tests。BOSS CLI 为首个 reference provider。 | [规范](system/first-party-cli-mcp-provider-standard.md) | 首次落地并入 [BOSS MVP 计划](plans/recruiting/plan-recruiting-cli-agent-integration.md) |
| 64 | Agent 用户可见中间消息（verbose） | 🔧 部分完成 | **2026-08-31 开发完成：Phase 0–4 全部代码与自动化测试已交付，各 Phase 经 开发→独立测试→独立CR 三智能体流程通过（CR 累计修复 8 项 P1/P2，含 dispatcher drain 空等延迟 final、browser_human_required 终态缺口）；全量单测对比 pristine HEAD 基线零新增失败（66 个既有环境性失败持平）、前端 build/typecheck 通过。剩余动作：按 [灰度回滚手册](plans/verbose-feedback-rollout-runbook.md)（含 18 项真机验收矩阵）执行真机渠道验收与灰度开启，通过后移入 ideas_finished.md。** 2026-08-31 已完成三轮独立设计审计，全部阻断项已纳入设计；随后按产品决策收敛为全确定性文案。MVP 新增独立 `verbose` 事件，只承载面向最终用户的一句人话，与隐藏的技术 progress 和最终 response 硬隔离。业务文案只来自 Skill metadata、Tool `get_user_feedback(args)` 或 delegate 固定模板（2026-09-01 产品决策：system watchdog 兜底已删除，verbose 仅由策略产生，无策略的慢 turn 不提示）；LLM 不生成、不选择、不改写 verbose，也不增加相关 prompt。事件由编排层旁路产生，每个 owner 生命周期最多 1 条；不进入内存 Agent messages，不进入同轮后续或下一轮 LLM 上下文，Skill feedback metadata 也不渲染进 prompt/tool schema。Web 按 `type=verbose` 原地更新 assistant 占位；第三方渠道按同一事件进入 owner 级容量 1 dispatcher，cancel/merge 重跑不重置。**可靠性约束**：禁止调用 `session_queue.mark_responding`；status 发送原子预留 final 限流额度；RPA verbose/final 使用两个真实幂等键；微信客服共享 5 次 reply budget 并保障 final 正文优先；全局 `force_disabled` 可紧急回滚。配置默认开启（2026-09-01 产品决策随 watchdog 删除改为默认启用，force_disabled 可紧急回滚）。MVP 分 5 Phase，预计 7～9.5 人天；多阶段播报、子智能体实时透传和工具内部 `report_verbose` 后置。 | [设计](system/agent-intermediate-feedback-design.md) | [开发计划](plans/plan-agent-intermediate-feedback.md) / [灰度回滚手册](plans/verbose-feedback-rollout-runbook.md) |
| 68 | zhipu 默认模型切 GLM-5.3-Flash + 价目表多模态标识 | 🔧 部分完成 | ①zhipu 缺省模型 glm-4 → GLM-5.3-Flash（GLM-5 系列首个原生多模态，输入 0.8 / 输出 2.8 元/M tokens，缓存命中价官方 0.23 元/M 暂按输入价填待确认），改 `configs/config.yaml` 缺省值 + `.env.example` 注释 + `ZhipuProvider` 构造缺省；存量环境 `.env` 显式设了 `ZHIPU_MODEL_CODE` 的不受影响。②`token_cost_prices` 加 `is_multimodal BOOLEAN NOT NULL DEFAULT FALSE` 多模态标识（init-postgres.sql + db_update.sql 双写），kimi-k3 / GLM-5.3-Flash / qwen-vl-max / qwen-vl-plus / qwen3-vl-flash 标 TRUE（qwen-vl 三个存量视觉模型由 CodeReview 补标）；`TokenCostPriceDB.get_by_model_name` 透出该字段，新增 `list_multimodal_models()` 供图片路由查询。③图片路由（多模态模型图片直发 content 数组、纯文本模型维持 OCR）本次**未实现**——聊天上传图片目前只在 `agent.py` 拼文件路径文本上下文，切入多模态需动消息管线/会话历史持久化（memory/chat_messages 均为纯文本），留待下一步小步改造。2026-08-28 | [设计](design/weixin/weixin-cli-billing.md) | — |
| 77 | 外部系统入口（SSO 打开第三方系统） | 🔧 部分完成（Phase 1 开发完成，待真机联调） | **触发**：售前智能体客户数据已推送 10605（60/61/63 号），产品要求在我方页面提供打开第三方系统的入口打通操作链路——连接中心新增「外部系统」入口 + 外部接待客户页面右上角打开按钮。**核心决策：机制与实例分离**——系统层做通用机制（入口配置模型、SSO 通用契约、跳转接口、前端渲染），代码不硬编码 10605；实例层细节由租户文档 `pre-sales-api.md` 的 `api-meta` 块声明（复用 61 号既有事实来源，新增 `sso_enabled/sso_system_name/sso_mode/sso_url/sso_ticket_param` 可选键），无声明的租户不显示入口，未来其他第三方系统复用同一契约。**SSO 通用跳转模式**：`direct_url`（缺省，第三方无 SSO 接口仅提供入口网址，前端直接 window.open，零凭据参与）/ `ticket_redirect`（后端持 client_token 调第三方换一次性短 TTL ticket → 前端打开，token 不出后端，有 SSO 接口时首选）/ `token_param`（URL 直接拼 token，兜底）/ `form_submit`（账密代填降级，预留不实现）；direct_url 模式列表 API 直接返回 entry_url 无需调 sso-url 接口；api-meta 可选 `sso_fallback_url` 键——ticket_redirect 换票失败（如用户账号在第三方不存在）不阻断，后端返回 success=false + fallback_url（未声明回退 sso_url），前端仍 window.open 打开登录页并提示手动登录，操作链路不因 SSO 故障中断；token_param 失败发生在第三方页面内不回退。**改动点**：①后端 api-meta 解析扩展（建议抽公共模块供推送与入口共用）+ 新 `src/api/external_systems.py`（GET 列表 + POST sso-url，ticket_redirect 复用 delegate_login 取 client_token 缓存机制）；②前端连接中心 4 Tab → 5 Tab 新「外部系统」卡片列表 + `ExternalCustomerService.vue` 右上角条件渲染「打开 XX 系统」按钮（window.open）；③租户文档模板补 sso 块示范。**安全**：client_token 不下发前端、ticket 一次性 TTL≤60s、sso-url 生成写审计、X-Tenant-Id 租户隔离。**决策点（2026-09-08 已确认）**：Q1 10605 为 ticket_redirect（首选 sso_grant 复用委托会话双 token、兜底 sso_login 手机号，票据一次性 5 分钟有效，第三方直接返回完整跳转 url，接口细节已补入 ext/10605-售前咨询接口文档.md §2.3 与 api-meta sso_* 键）、Q2 普通员工与管理员均可见、Q3 新开窗口、Q4 连接中心一级菜单下新增独立二级菜单（非 Tab）。**分期**：Phase 1 解析+API+前端入口（direct_url/ticket_redirect/token_param，2-3 天）→ Phase 2 真机联调 + form_submit + 第二个第三方系统接入验证（1-2 天）。**Phase 1 开发进度（2026-09-08，三智能体流程）**：后端新增 src/services/tenant_api_doc.py（api-meta sso_* 公共解析）+ src/api/external_systems.py（GET 列表 + POST sso-url，ticket_redirect/token_param 双模式：sso_grant 换票 + 失效强刷重试一次 + sso_login 兜底签发，失败不阻断返回 fallback_url）+ main.py 注册；前端新增 api/externalSystems.ts（占位窗口防拦截 + 失败重定向 fallback_url）+ connections/ExternalSystems.vue 页面 + 连接中心「外部系统」二级菜单 + ExternalCustomerService.vue 右上角按钮。单测 33 例（tenant_api_doc 9 + external_systems_api 24）+ 相邻回归 37 通过、容器 import OK、前端 build 通过。**待办**：部署 agent2 后真机验证（列表可见性、SSO 无感跳转 10605、Code=-1 回退手动登录、ticket 一次性/5 分钟过期路径）。 | [方案](system/external-system-entry-design.md) | — |

## 数字员工 / 子智能体

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|**🔧 P3 定稿（2026-09-10 总工终门禁）**：三工作包（A1 后端补齐/A2 前端工作台/B 聊天工具）全部经三智能体验收；A1 修复轮补 catalog weixin 受信 Provider（4 只读+v2 写，无 v1 写）、同幂等键粘滞失败修复（终态残留 :r2 重建）、detail 增 draft_blocks 草稿回显；A2 修复轮修 3 P1（创建流块清空/卸载后轮询复活/试发确认预览与已发布内容错位——dirty 时仅位置占位）；B 验证 0P0/P1，企微反证词与空串租户两项 P2 已修。终门禁：前端 build 503 模块 + 304 用例全绿、Python 全量 2277 passed 全绿；视觉/窄屏/键盘浏览器实测与真机验收（P0）未做，page_metadata 保持 developing 待发布评审。**P3 复审返工（2026-09-10）**：2 P1 修复并经独立验证（变异护栏真实）——①发布后无草稿回显：detail 增 active_blocks/active_trigger/active_group_binding_id，编辑器 draft 优先→active 兜底（触发不回退默认、回显态不脏、试发可用）；②群核验完整性门槛：候选集合不完整（显式截断标志或条数达请求上限 50）一律保持 pending 并提示缩小搜索词，仅完整集合进入唯一性判定，阻断路径零 DB 写。终门禁：Python 全量 2289 passed + 前端 build 503 模块/306 用例全绿。登记：真机 weixin-CLI 参数契约缺口（服务端 keyword/limit=50 vs CLI query/limit≤20，真机 fail-closed 安全但端到端不可用——设备侧契约对齐随 P0）。**P3 复审二轮（2026-09-10）**：试发序号 off-by-one 修复——前端提交与聊天工具参数说明统一为后端 1 基编号（block_position ge=1），补「请求参数 + 实发内容」双侧断言（前端断言 POST body block_position=所选条目；集成断言 position=2 时 delivery payload_ref=:2 且 hash=第 2 块冻结内容）；变异复验（加回 -1 即转红）；域内 216 passed + 前端 307/build 全绿。
| 74 | 桌面 CLI 无人值守自动任务底座＋微信营销首场景 | 🔧 部分完成 | 2026-09-08 修订：通用调度/事件、运行账本、许可、journal/outbox、effect/phase、桌面锁和配额归中立模块；微信保持固定内容、时间/业务事件、文字/网址/图片/内容包范围及真机门禁。BOSS 为第二契约消费方，场景另行立项。P1 重估后底座+微信约26–41人日，待验证，不含BOSS。**🔧 P1 底座开发完成（2026-09-08，四工作包三智能体流程）**：①`src/desktop_automation/`（16 模块：subject registry/触发接纳/quota 固定序预留/§5.4 聚合/executor 骨架/审计/outbox）+11 张系统表（DDL 三处同步并登记 database_system_table.md）；②`src/local_tools` 扩展（LocalInvocationService、write-authorize/operation-result/payload 三 runtime 端点、许可表与 invocation v2 扩列）；③Runtime 多 Provider（manifest 注册表化+weixin 条目、capability 协商、claim 按 provider 路由、命名管道共享桌面锁、DESKTOP_RESOURCE_BUSY 不触发旧 BUSY 自愈）；④写路径 E2E（permit 单调时钟/journal fsync/result outbox 重投/迟到幂等，旧 boss 链路 /result 字节级不变）；⑤payload resolver + weixin/boss v2 双消费方契约样例（Python+Runtime 两侧独立锚点）。P1 门禁：Python 终跑 313 全绿（焦点+集成+执行器+db_update 校验，多轮稳定）、Runtime 96/96+typecheck 0、boss CLI 414/414、weixin-cli 97/97 回归全绿。三轮验证共抓出并修复 1 P0（发布误停他 task schedules）+10 P1（§5.4 截止语义/迟到回执/额度竞态/fence/fail-open/嵌套连接/shutdown 不可重试等）；外部复审追加 6 项阻塞（额度不看 used、许可在桌面锁等待外申请、迟到成功回执被拒后删除、过期即释放额度、verified 无证据判成功、桌面锁含 device_id）已按 R19-R26 契约修复并经独立复审确认（变异验证证实关键断言有鉴别力）；复审二轮补完证据链（R27 desktop_automation_evidence 证据登记表 + 适配器 validate_evidence + DB 唯一事务级防复用 + 绑定交叉核对，R28 终态后迟到证据持久登记与完整回执审计），独立验证含变异复验通过。遗留 P2 已登记：permits/runs 过期清扫与 tick 接线、dedupe per-attempt、迟到分支 PERMIT_REQUIRED 不对称等。范围声明：providers 微信/BOSS 保持 v1 未翻转，「真实断网/崩溃」仅在真实进程+fake Provider 层验证，微信真实 v2 执行基础与真机断网/崩溃未验证（P0 真机门禁未做，不宣称真机通过）。**🔧 P2 时间闭环开发完成（2026-09-09，三工作包三智能体流程）**：①src/weixin_marketing/ 场景模块（7 张 bs_weixin_marketing_* 业务表双轨落地、WeixinFixedContentAdapter[compile/authorize/aggregate/serve_payload/validate_evidence 结构校验+可插拔真实验证接口]、once/interval/calendar 触发编译、内容冻结、生命周期服务[发布单事务/CAS/epoch/pause 撤销未开始]、手动 run、resolve/retry 单事务链）；②/api/weixin-marketing 15 端点（项目 envelope/稳定码/Idempotency-Key 三态+资源维度+TTL 接管/跨租户 404 统一）；③调度闭环（时间扫描/执行驱动/许可清扫/租约回收四类 tick；weixin_marketing.enabled 默认 false 零注册零执行零 DB；tenant_allowlist 时间/调度/手动三处门控）+ executor 首派 dedupe per-attempt + 底座 permits 判重修正（仅活跃 issued 阻断，人工重试链放行）。R48 验收全有假时钟用例：once 只一次、interval 不积压、崩溃恢复 unknown 收敛、may_have_started 不重派、enabled=false 零动作、与旧 dry_run 架构隔离（AST+文本+运行时三重）；HTTP 层 API→publish→run→tick→假设备→查询全链路经独立探针验证。终门禁 443 passed 全绿（多轮稳定、DB 零残留）；P2 验证共抓出并修复：底座 permits 判重阻断重试链、retry 竞态、幂等缺资源维度、allowlist 语义不完整等 6 P1。P2 复审返工（2026-09-09 外部复审 5 项功能阻塞）：①取消/许可互斥（cancel_run 单事务撤销在途 invocation[queued→终态/claimed+running→cancel_requested]、write_authorize 锁序改 subject→run→invocation→delivery→quota 复验执行资格，双线程交叉探测无死锁）；②确定性领取（底座 claim_run 增 expected_run_id 定向领取，dispatch 领到后才加载该 run 配置与设备，删除快照归还 hack，tie 用例 10 连跑全绿——修复了复审实测的 unknown 错落）；③幂等同事务（业务写入与幂等记录同 commit、手动 run request_id 恒取幂等键、TTL 接管强制 digest 匹配，崩溃窗口重复创建不可达）；④重试证据门槛三轮收紧（机器安全必须 none+prepared+**显式 safe_to_retry=true**（False/NULL 不放行）；未知效果须双证据——resolve 记录 confirmed_not_sent 人工决定 **且** 绑定原 invocation 的受信停止确认（设备通道迟到回执 operation_result_late：none+prepared+safe_to_retry=true；当前 Runtime 无此更正流程，未知效果重试在真机停止确认能力交付前 fail-closed 关闭），缺任一 409 RETRY_EVIDENCE_REQUIRED；证据时点须晚于 attempt 终结）；⑤扫描过滤下推（scenario/allowlist 进 SQL LIMIT 前，100 排除+1 允许饿死测试实证）。返工验证：独立测试 457→收尾 459 passed 全绿、死锁探测两轮 PASS、变异 2/2 被抓、评审抓出并修复 cancel×pause AB-BA 锁序（含并发用例）与孤儿 invocation 死键防绑。终态 run 重试链放行判据（delivery 曾终态经 retry 回置+最新 attempt 绑定）经评审论证不可伪造。遗留登记：expire_overdue_runs 接线前须改锁序、重试后 run 终态不重聚合（账本诚实语义）、多场景清扫/候选池划分与 run_finished 条目消费者（P4 议题）、D1-D4 底座缺陷（runs 聚合可见性/outbox kind 过滤等，部分已随 R50/R53 修复）。**🔧 P3-A2 前端工作台开发完成（2026-09-09，待独立测试/评审验收；独立验证 3 P1+指定 P2 已修复，见下方增补）**：frontend/web/api/weixinMarketing.ts（23 端点契约快照：TriggerConfig/ContentBlock 判别联合、RunState/DeliveryState 强类型、WeixinApiError 统一错误含 409 冲突信号、前端注入 Idempotency-Key）；components/weixinMarketing/ 七组件（列表 5s 可见时轮询+降级、编辑器 baseVersion/dirty/409 保留用户输入并显示差异+useModalCloseGuard、触发/内容块判别联合表单+键盘排序+上限校验、内容预览、群绑定搜索→绑定→核验（2s 异步轮询）、运行详情 2s 至终态停止+退避+AbortController、resolve（confirmed_not_sent 需 note）/retry（证据门槛文案化）/cancel）；路由 /t/:tenant_id/weixin-marketing/* + page_metadata（developing，验收后由总工程师定 published）+ BusinessPageIcon 规则。前端 build 0 错误、全量 298 测试两轮全绿（含新增 AutomationEditor dirty/409 与 RunDetail 轮询终止用例）。已报告契约缺口：GET /automations/{id} 不返回草稿内容块明细，编辑器以会话内缓存+显式覆盖确认交付。P3-A1（后端 8 端点）与 P3-B（聊天工具）不在本次范围。**验证返工（2026-09-09，总工程师指令）**：V-P1-1 创建流缓存顺序（保留表单块）、V-P1-2 卸载后轮询复活（AbortError 不归一+代际检查）、V-P1-3 试发按已发布内容门控（不一致只显位置占位）、V-P2-1 paused 发布禁用、V-P2-2 window.confirm→BaseModal（3 处）、V-P2-4 退避封顶/paused 用例；顺带对接 A1 并行下发的 detail.draft_blocks 契约（回显+无草稿横幅）。登记不改：V-P2-3 绑定列表翻页、V-P2-6 getAutomation signal 竞态、幂等键网络层重试不复用。**🔧 P3-B 聊天工具与子智能体开发完成（2026-09-09，三智能体流程，待独立验证）**：①src/tools/weixin/weixin_automation_tools.py 三工具（weixin_automation_prepare 创建/更新草稿[无发送副作用，返回草稿摘要+校验+未来5次预览+未发布状态]、publish[automation_id+expected_version CAS，authorization_source=chat 留痕，返回首次触发时间，不试发]、manage[pause/resume/archive/run/cancel/resolve/retry/test_send/list/get_runs/get_run_detail；run/test_send/retry 结果带高危人工确认提示]），InputModel extra=forbid 只接业务字段、身份取 ToolExecutionContext fail-closed、服务层错误稳定码透传（与 api 码表对齐）；经 Catalog 自动发现装配（黄金清单已登记）。②通用 create_scheduled_task 保守路由约束（微信营销域+调度词+发送动作三词共现且无其他渠道词才拦，dry_run 前拒绝并结构化引导专用工具）；③subagents/weixin-marketing/SUBAGENT.md（tools.allowlist 三专用工具+transfer_to_human，body 声明固定内容规则/授权边界/unknown 处理）；④测试 37 项新增（mock 服务单测 29+路由约束 4+冒烟/装配 6 类）+真实 DB 零副作用断言 4 项（prepare/validate 零 run 零 invocation、属主隔离、发布后零 invocation）；完成门禁 1909 passed 全绿 ×2。遗留：SUBAGENT.md tools.allowed 为文件态默认，管理后台 subagent_definitions 可运行时覆盖；skills.allowed 空（无专属技能）。 | [底座设计](design/desktop-automation/desktop-cli-automation-design.md) / [场景设计](design/weixin/weixin-marketing-automation-design.md) / [调研](research/weixin-cli/automation-readiness-2026-09-08.md) | [底座计划](plans/desktop-automation/plan-desktop-cli-automation.md) / [微信实施与BOSS衔接](plans/weixin/plan-weixin-marketing-automation.md) |
| 76 | BOSS 直聘聊天自动化 | 🔧 部分完成 | 跟踪已打招呼候选人，未读 observer＋候选人绑定＋版本话术/受限决策＋预授权＋人工接管及员工群通知。独立 P0′ 验证身份、未读/长会话、增量发送证据；P2′ 起依赖第74项 P1，不改变既有 BOSS MCP/恢复语义。**2026-09-10 审核修正**：当前分支发送仍为 strings naive 校验，未发现 visibleMessagesOf / resolve-probe；撤回本条“435/435、P0′ 已开发”声明，历史测试来源未核验。写后验证设计已修订，代码未开始、真机门禁未通过；身份唯一性与其他 P0′ 项仍待验证。新增 [写后验证设计](design/recruiting/boss-send-verification-design.md) / [开发计划](plans/recruiting/boss-send-verification-dev-plan.md)。 | [场景设计 §11](design/weixin/weixin-marketing-automation-design.md#11-第二场景boss-直聘聊天自动化待独立立项) / [底座设计](design/desktop-automation/desktop-cli-automation-design.md) | [BOSS 里程碑](plans/weixin/plan-weixin-marketing-automation.md#12-boss-聊天自动化实施衔接-待独立立项) / [底座计划](plans/desktop-automation/plan-desktop-cli-automation.md) |
| 72 | 营销 App 智能外呼代理 | 🔧 部分完成 | 2026-09-05 完成调研、详细设计与实施规划。客户系统疑似“云图”；不依赖厂商 API/技术支持，采用自选安卓专机＋GLM-5.3-Flash 截图定位＋安卓控制＋有线双向音频桥，沿原 App 虚拟号链路完成 AI 对话与反馈回填；云图录音/关联按黑盒处理，Agent 自留音轨仅作非权威参考、不能上传云图。先验证双向音频、线索身份、保存回读和故障防重拨；新增 P0-A：替代 App 视觉操作＋普通号码独立音频实验，无云图也可开工；首批 E0/E1。2026-09-06 E0 doctor/observe 代码、37 项离线测试与独立审查完成，Windows＋小米后续实测见下文，[起步记录](research/marketing-call-agent-experiments/e0-bootstrap-20260906.md)；2026-09-07 Windows＋小米真机截图、GLM 图文返回、单步进入线索页及系统拨出获现场验证，用户确认拨打成功；严格 E0 解析因 Markdown 代码块仍失败，E1 正式实现及音频未完成，见[真机记录](research/marketing-call-agent-experiments/device-validation-20260907.md)。[调研](research/marketing-call-agent-feasibility.md) | [设计](design/marketing-call-agent-design.md) | [验证与计划](plans/marketing-call-agent-plan.md) / [GLM 实验执行手册](plans/marketing-call-agent-experiment-runbook.md) |
| 62 | 工程审计智能体（客户方案阶段） | ⏸️ 已搁置 | 面向工程管理咨询公司的「四库一平台三智能体」工程审计智能体商务方案（客户需求《工程审计智能体开发建设方案V1.0》）。基于现有平台差距分析：RAG（pgvector 混合检索）、9 类文档解析器、OCR/Qwen-VL、子智能体与 Skill 框架、多租户 SaaS 底座可复用约 60-70%；CAD/DWG 解析、算量数据对接、动态指标库为全新开发。技术边界声明：CAD 造价级算量、广联达/鲁班原生接口（无公开 API）、围串标定性、影像真伪鉴定、信息价自动同步（数据版权）等当前技术无法有效实现，方案给出降级替代路径。建议分三期：一期 12 周（施工+结算核心闭环）、二期约 10 周、三期约 8 周，合计 584 人天 / 约 116.8 万元（综合人天单价 2,000 元含税）。未签约，2026-08-17 输出方案与报价。**⏸️ 暂缓（2026-08-19）**：项目暂时搁置，需求原文（audit-req.md）、技术解决方案、报价单及生成脚本已归档至 git 忽略目录 `docs/backups/engineering-audit-agent/`（仅本地保留，不入库）。 | 见 `docs/backups/engineering-audit-agent/`（git 忽略，仅本地） | — |
| 17 | CRM 智能体 | 📋 待开发 | 客户关系管理，客户数据整合与智能跟进建议 | — | — |
| 45 | 旅游报价数据补全（消费 Excel 模板工具） | 📋 待开发 | 解决旅游报价"内容项不全无法支撑任意版式"问题。模板填充已下沉为通用无状态能力（见 #44，样例每次必传、调用方持有），本条只管 travel-quote 领域职责。Phase1 数据模型补全（item 加 row_total/audience_split/group，QuoteData 加分类与分组小计、多人群人均）；Phase2 消费集成（QuoteData→通用 data 适配器、generate.py 带 sample_file_path 调 #44 fill_with_sample、退役自有 excel_export.py）；Phase3 travel-consultant 聊天接入（客户发样例→agent 记住样例路径→后续报价带上，全程无入库）；Phase4 领域补全（大交通机票/高铁、签证/小费/装备/税费/赠送/加床、餐饮住宿儿童差异化）。2026-07-20 | [设计](system/design-travel-quote-template-engine.md) | [计划](plans/plan-travel-quote-template-engine.md) |
| 46 | 旅游顾问行程 HTML 长图导出（图片独立行 + x-to-image 图片内联） | 📋 待开发 | 客户要求行程图片嵌在表格内，现有 Word 路径图片在表格外独立章节、且 Word 对图片布局控制弱、多客户样式难定制。改为：详细行程默认以 **HTML 长图(PNG)** 交付，CSS 精确控图；**图片独立成行**(紧跟景点信息行下方跨列)+分档布局(1张大图/2-3张等宽横排/≥4张每行最多3张换行)；**图片 base64 内联由 x-to-image 内部完成**(file_id/远程URL→base64)，智能体只写 `<img src="file_id:xxx">`；Word 降为可选备份(客户要可编辑/打印时)。本期一套标准模板，多样式延后。**关键验证(2026-07-20)**：Pandoc pipe_tables 支持单元格内嵌图(Word 备份可行)；headless Chromium set_content 禁加载本地文件→HTML 图必须 base64 内联；x-to-image 已注册(agent.py:431)、旅游顾问 inherit 已可用；agent 主循环 hasattr 钩子自动注入 tenant_id(agent.py 不改)。改动：`_image_inliner` 加 `inline_images_as_data_uri` / x_to_image models+html_renderer+tool 接入 / SUBAGENT.md 改 HTML 模板。原型已验证(1/2/3/4 张四种布局)。2026-07-20 | [设计](subagent/travel-consultant/itinerary-html-export-design.md) | [计划](plans/plan-itinerary-html-export.md) |
| 49 | AI 批量视频内容生产系统 | 🔧 部分完成 | **v0.4 回归本质（2026-07-29）**：这不是"批量生产系统"，是一个**抽卡式 AI 视频工具**——选场景→上传素材填文案→生成2-4个视频抽卡→满意留下继续优化编辑→下载。运营拿到成片自己上传视频号/抖音。**MVP唯一闭环**：①选1-2个通用场景(产品展示/氛围)；②向导内上传产品图/模特图(自动抠图,落file_id)+填文案；③一次生成2-4条差异化视频卡片式展示供挑选；④满意的"留用"可重新生成式编辑(换运镜/换背景/改文案重跑,不做帧级剪辑)，最终下载。**v0.4 相对v0.3收敛**：删除审核台、删除发布链路对接(到下载为止)、删除批量任务看板(就是一次抽卡)、场景从7类收到1-2个、编辑改为重新生成式(不集成前端剪辑器)。**极简数据模型**：gen_sessions(一次抽卡会话)+gen_cards(抽出来的视频卡片,含kept留用标记/parent_card精修溯源)，素材复用file_id不建表，无gen_batch_tasks/social_*对接。**内部仍走工艺链**(抠图→主体一致→prompt→运镜→通义万相I2V→拼接→超分→字幕→合规标识)但对用户隐藏为"一次抽卡"。**Spike实测结论(v0.5,2026-07-30)**：模型`wan2.7-r2v-2026-06-12`(r2v)，用`reference_image`+`first_frame`防变形成功(假睫毛/模特图均验证产品不变形)；**万相支持base64直传图**(无需公网URL/OSS)；**长图须人脸裁剪**否则鬼影(新增preprocess预处理环节)；必须用httpx。**合并架构(v0.5)**：视频生成合并到社媒营销智能体作为"内容创作"模块，后端独立模块(src/video_gen/)+社媒工作台内加Tab入口(不进chat,不新增路由,社媒零侵入)，MVP成片先独立下载暂不接审核发布链。**MVP 4-6周**：通义万相API。**Phase1**：更多场景/自建Wan2.2/批量/产品库/接社媒内容管理流。**Phase2**：审核发布链路/帧级剪辑/数据回流。PRD第0章为权威定义，MVP技术设计/开发计划已就绪(spike验证通过)。**🔧 开发进度(2026-07-30)**：Phase0-5代码完成并通过三智能体流程(开发→测试45单测全绿→CodeReview可安全提交)。后端独立模块src/video_gen/(db建2表/scenes2场景/preprocess人脸裁剪/media注册+烧录AI标识/wanx_provider/service全业务) + src/api/video_gen.py(7端点) + 启动链路接入(settings/database/main/scheduler轮询job) + 前端(videoGen.ts + VideoGeneration.vue子组件 + SocialMediaWorkbench.vue加"内容创作"Tab)。依赖opencv-python-headless。剩余：Phase6端到端联调(真实美妆素材验证变形率/bling效果)待用户部署环境验证。**📋 产品定位重新审视(2026-08-06)**：竞品对比下价格倒挂(Minimax-H3成本0.5元/秒 vs 小云雀C端售价0.33元/秒)，工具效率+单秒价格赛道无优势。回归企业级定位，差异化核心=**提示词沉淀+企业素材资产中心+协作价值度量**，让C端不解决的"组织能力沉淀"成为壁垒。演进路径:Phase1企业素材库+提示词沉淀->Phase2双模型提示词引擎(内化"借第三方AI生成提示词"步骤)->Phase3协作与价值度量(对接工作日报)->Phase4合规品牌->Phase5社媒发布回流。**💰 计费接入(2026-08-06)**：视频创作每次大模型调用(文本模型+视频模型)都写chat_records计费(source_type=video_gen)，与主业务计费链路对齐；新增env变量`VIDEO_GEN_USAGE_FACTOR`默认30(3倍加价)，区别主业务`BILLING_USAGE_FACTOR=100`(10倍)；按秒计费(Minimax 0.5元/秒×3=1.5元/秒售价)，仍贵于小云雀但含企业级能力，TCO视角而非单秒价格竞争。**📋 Phase 1 开发计划就绪(2026-08-07)**：三大重点①企业组织沉淀(素材库/视频库/提示词库三表)+②会话化创作(精修/敏捷双模+工具栏+文件卡片，复用chat_sessions表)+③计费接入(VIDEO_GEN_USAGE_FACTOR+chat_records，source_type=video_gen)；8个Phase总周期5-7周；现有「视频创作」页面保留为MVP原型，新功能走src/video_agent/新模块，不改src/video_gen/原代码。**🔧 会话化创作链路补通(2026-08-11)**：①前端视频参数弹框新增"提示词模型"下拉（qwen-vl-plus/qwen-vl-max/qwen3-vl-flash/qwen3.7-plus，视觉/文本标注）；②补通整条参数链路（之前前端弹框参数实际未传到后端）：ChatRequest 加 video_params → Agent.process_message 存 _current_video_params → 工具执行注入 _video_params kwargs → 新 BaseTool `submit_video_task` 读取参数调 VideoChatService；③PromptEngine 支持自定义 prompt_model_code（按需构造局部 LLMGateway，带白名单校验），覆盖 SUBAGENT.md 默认 qwen-vl-plus。前端 build + 后端 import 全绿，三智能体流程（开发→测试45单测→CR 2P1已修）通过。**max_tokens 配置驱动（2026-08-11）**：gateway 层按模型取默认 max_tokens——`configs/config.yaml` 新增 `llm.model_max_tokens` 表（qwen-vl-plus/qwen-vl-max=8192、qwen3.7-plus=131072、qwen3-vl-flash=32768，数据源百炼官方文档），删除 qwen.py 硬编码 clamp 字典改从配置读；qwen-vl-plus 调用默认直接取 8192，不再每次触发「max_tokens 超过上限，自动 clamp」warning（未配置模型回退 16384）。 | [调研](research/ai-video-production-research.md) / [PRD](system/content-production/ai-video-production-prd.md) / [MVP技术设计](system/content-production/mvp-design.md) / [MVP开发计划](plans/plan-video-gen-mvp.md) / [产品定位重新审视](system/content-production/video-agent-enterprise-positioning-design.md) / [Phase 1 开发计划](plans/plan-video-agent-phase1.md) | — |
| 20 | 社媒营销智能体 | 🔧 部分完成 | 企业社媒营销全链路单一子智能体，三大模块共享账号/凭证/调度/审核/数据归一化/文件存储核心与统一社媒平台连接器：①内容管理（公众号/视频号自有阵地发布）；②广告管理（腾讯广告Marketing API朋友圈广告：投放诊断+策略建议+人在环受控执行+留资转化回传，不自建竞价）；③巡检商机（知乎/小红书等第三方平台，agent经browser工具以真实账号正常登录后做需求雷达/内容播种/留言接洽，商机汇总分配销售——公司第一优先级）。**已实现**：13张数据表全建、连接器协议骨架(PlatformCapability含内容/广告ADS_*/webWEB_*三类枚举/SocialPlatformConnector/ConnectorRegistry/CapabilityResolver resolve/supports/require)、SocialMediaService(账号/计划/素材/内容/审核/发布任务CRUD+幂等,已接入CapabilityResolver)、连接器契约测试(参数化覆盖全部连接器+capability_resolver单测)、21个API、1个聚合工作台前端(~20%)。**stub/未实现**：公众号/视频号连接器无真实HTTP(wechat_official声明矛盾已于S0修复,现仅声明ACCOUNT_CREDENTIALS)、AI内容生成、发布调度执行(Dispatcher/Executor/APScheduler未集成,QUEUED后不可达)、运营数据分析(仅计数)、子智能体(.disabled)、广告管理与巡检商机两模块完全未做。当前对生产无影响（页面planned、子智能体disabled）。**S0共享底座已完成(2026-07-21)**。2026-07-21 三大模块设计合并收敛为单一智能体（删除原分散的内容/广告/巡检独立设计文件，去RPA化，巡检改用agent原生web操作表述）。开发计划分S共享底座/B巡检/A广告/C内容补齐四系列+I整合，约125-155人日，推荐顺序：底座→巡检(第一优先级)→广告→内容补齐。 | [调研](research/social-media-operations-agent-platform-research.md) / [设计](system/digital-employee/social-media-marketing-agent-design.md) / [S1发布调度设计](system/digital-employee/publish-dispatcher-design.md) / [后台运行时设计](infrastructure/background-runner-design.md) | [开发计划](system/digital-employee/social-media-marketing-agent-dev-plan.md) |

## 工具

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 50 | 协会信息收集客户端（交付产品） | 🔧 部分完成 | **下周一（2026-08-10）交付给客户**。将现有「协会信息收集」能力（4步流水线：搜官网→Playwright采官网→网络兜底→微信RPA取证）从服务端内部工具改造为可交付客户电脑独立运行的产品。**三端架构**：①Electron 客户端（GUI入口，输入/进度/日志/结果/积分展示）；②本地命令行工具（PyInstaller exe，承载完整业务逻辑，Electron spawn 调用）；③服务端（LLM/OCR/WebSearch 代理网关 + 积分计费×5 + 激活鉴权 + 日志接收）。**关键决策**：客户端共用租户通用积分池（`tenants.credit_balance`），消耗按5倍系数实扣；一次性激活码绑定租户（激活后换 access_token 长期鉴权）；业务逻辑全部在本地命令行 exe（含微信RPA/Playwright/LLM代理），客户端只是壳；LLM 不直连模型商，走服务端代理集中计费。**新增3表**：`client_activation_codes`（激活码）/`client_bindings`（绑定凭证）/`client_usage_logs`（消耗日志）。**新增7端点**（`/api/client/v1/` activate+credits+llm/chat+ocr/parse+websearch+logs + `/api/saas/client-activations` 管理端）。**最高风险**：PyInstaller 打包 Playwright（Phase 0 优先验证）。**🔧 开发进度（2026-08-07）**：第1步 search_profile 已优化——从 DeepSeek 不联网直出（官网全是幻觉）改为「文心一言联网采集原文 → DeepSeek 解析」，官网准确率显著提升；新增 wenxin_collect.py（文心采集子脚本，attach 常开调试浏览器 9222）+ wenxin_browser.py（常驻浏览器自启），search_profile 改造接入并保留 DeepSeek 兜底，main.py 启动确保浏览器，progress_reporter 修正第1步 step 推断（原"搜索协会基础信息"关键词与文案不匹配的 latent bug）；38 单测通过（wenxin_collect IO + search_profile 文心/降级三分支 + wechat 回归全绿）。2026-08-07。**激活码管理 UI（2026-08-10）**：portal 租户管理编辑弹窗新增「激活码」Tab（`TenantActivationCodes.vue` + `api/clientActivation.ts`），平台管理员可查看/生成/禁用激活码、吊销客户端绑定（复用已有 `/api/saas/client-activations` 端点，前端纯新增、后端零改动）。**可观测性补全（2026-08-10）**：客户端自动遥测（`start`/`log`/`error`/`complete`→`/api/client/v1/logs`，不上报 progress 控量）+ 本地完整日志 `%LOCALAPPDATA%\AidWorkAgent\association-client\logs\app.log`（`_emit` tee）+ GUI 顶栏「📦 诊断包」一键导出（脱敏 token + PowerShell Compress-Archive，零依赖）；后台新增 `/portal/client-logs` 页（`/api/saas/client-usage-logs/list|recent-errors`，platform_admin）跨租户查 `client_usage_logs` + `(status,created_at)` 索引。真机 6 协会 run 验证通过（遥测落 agent2、app.log 完整、导出包脱敏正常）。设计 §10。**采集联系人角色调整（2026-08-12）**：采集目标从「会长+秘书长」改为「秘书长+会员服务相关部门负责人+办公室/综合办负责人」，会长彻底不采——`PROFILE_FIELDS` 14→16（删 president_*，增 member_director_*/office_director_*）、文心问句/官网领导页兜底/编排器 Step3-4 循环（CONTACT_ROLES）/Excel 人员列（只留三个新角色，删其他职务占位列）同步切换；新角色走完整链路查手机，仅秘书长保留文心手机号快速路径；`wechat_search_leader_name` 删死参数 known_president。149+5 单测全绿。**文心 tab 隔离（2026-08-12）**：每个协会开新 tab 拿全新会话（旧会话上下文变长降准确率），先开新 tab 就绪再关旧 wenxin tab，浏览器常驻不关；同 profile 复用不触发验证码。**文心秘书长手机号 LLM 判别（2026-08-17）**：文心回答常含多个手机号，原纯正则取第一个会张冠李戴；改为正则圈候选 → LLM 判别哪个明确属于秘书长本人（姓名号码同段/职务绑定）→ 返回值必须在候选集合内（防幻觉），判不出/失败落回微信兜底。**计费倍率维持 5 倍（2026-08-17 曾改 10 倍后撤销）**：暂按 5 倍计费，未来调价改 `settings.client.credit_multiplier`（可 env/yaml 覆盖）即可。**官网领导页第一优先（2026-08-24）**：秘书长姓名以官网为权威（纠文心错值）——①词表扩容（学会镜像/领导泛化词120权重/机构泛化词/裸词兜底，人口学会等学会站点曾被协会前缀词表整体拦截）；②采集预算 max_pages 4→8、导航尝试 12→20；③新模块 official_site_leadership_search：词表没拿到秘书长时导航栏交 LLM 逐层挑选点击（4层/8点击/60s封顶），页面含强领导标记即收录再提取；④领导提取提示词加代秘书长/兼职/换届/兼任规则。324 单测全绿；30 协会真机验收探针 scripts/probe-leadership-search.py 待跑。**停止按钮修复与增强（2026-08-28）**：原 kill 只杀 PyInstaller bootloader，孙进程（python/powershell/llm-judge）全存活导致停止无效且成果丢失。修复：①Electron 每次 collect 生成停止文件经 ASSOCIATION_STOP_FILE 传入，kill() 先写文件（CLI 轮询到后保存部分结果优雅退出），8 秒宽限期后未退出则 taskkill /PID /T /F 杀整棵进程树；②CLI 新增 runtime/stop_flag.py（UserStoppedError 继承 BaseException 穿透业务 except 兜底），enrich_many 每协会前检查 + powershell_runner/providers 子进程等待改轮询（未配置环境变量时零行为变化）；③每协会处理完立即追加写 <output>.partial.jsonl 增量落盘，停止时仍生成最终 Excel（未完成行标注 stopped）；④新增 stopped NDJSON 事件（completed/failed/remaining/output），前端停止中置灰按钮、stopped 事件展示部分结果并把未处理名单回填输入框，进度图标实时 ✓/✗/■；退出码 2 归为非异常。Python 新增 16 单测 + Electron node:test 3 项全绿。 | [设计](tools/association-client-design.md) / [部署手册](tools/association-client-deployment.md) | [开发计划](tools/association-client-dev-plan.md) |
| 52 | weixin-cli 第一方微信操作 CLI / MCP Provider | 🔧 部分完成 | 第二个第一方 CLI，面向未来其他项目提供独立 Windows 微信操作能力。参考协会客户端和旧微信搜一搜脚本中已真机验证的经验，但不反向更新、迁移或依赖已经交付的协会客户端。复用 BOSS CLI 的 TypeScript operation/CLI/MCP/manifest/conformance 架构，首期产品化 `probe`、搜一搜搜索与详情证据采集；后续公众号文章 URL/正文、好友与群检索、单条消息发送、关注公众号能力先走分级 probe，再逐项进入正式 manifest。写动作使用短期精确 target ref、单目标硬上限、写后校验，`unknown` 永不自动重试。**🔧 开发进度（2026-08-11）**：M0 现状冻结与实验规范完成——离线/真机基线记录、协会与旧 RPA 字节级比对（4 个 ps1 同源）、ps1 协议/错误码/artifact/cleanup 样本冻结、probe 报告模板与 P0~P3 风险等级+测试目标白名单格式，产物见 [M0 文档](research/weixin-cli/m0-baseline-freeze.md)；协会禁改路径零改动。M1 TypeScript Provider 骨架已完成（2026-08-11，三智能体流程）：新建 clients/weixin-cli/（Node22+TS+ESM，MCP SDK 1.30.0 与 BOSS 锁定一致），统一 OperationResult/错误映射/progress/AbortSignal/operation 注册表，aid-weixin mcp --stdio、doctor（严格只读）、version --json，provider-manifest + schema 单一来源 + sha256 digest，进程内单飞 + Windows 命名管道跨进程互斥（进程死亡自动回收），weixin_probe 最小只读 tool，接入共享 conformance（npm test 8 项 + 独立 CLI 6/6 全绿），53/53 测试通过、typecheck 0 错误；CR 修复命名管道 error 崩溃风险、互斥异常结构化、toolDefs↔registry 启动期守卫。命名决策：可执行名 aid-weixin，操作对象仅作 --domain 参数值。下一步 M2 独立 PowerShell driver + v0.1 三个搜一搜 operation。**首个 probe（2026-08-11）**：`p1-chat-search-group`（P1 导航，好友/群检索 + 点击进入）真机通过——微信 4.1.12.26 / Win11 / 2560×1440。关键结论：① 中文逐字输入必须用**剪贴板逐字粘贴**（`KEYEVENTF_UNICODE` 被 Qt 搜索框静默丢弃）；② 微信 4.x 主窗口 Qt 自渲染，UIA 子树仅外壳 Pane（深度 TreeWalker 遍历 0 个 Value/Invoke），**搜索结果项不以 UIA 节点暴露**；③ **目标定位方案已确定并验证**——**Kimi `kimi-k3` 视觉模型（首选，4/4 成功）**：固定 prompt 返回 `{"x,y,label,found}` JSON，label 准确（俞万友/陶亚楠/芦龙豪宅俱乐部均对，无 OCR 字形误识），PNG 坐标 +window_rect 换算屏幕坐标，overlay 终态机器可验证，满足设计 §7.3 门禁第 1 条；**PaddleOCR 未完全否定**（`/layout-parsing` 的 `prunedResult.parsing_res_list[].block_bbox` 坐标链路通，但字形把"俞"识成"前"，作降级）；**百度 OCR 仓库无实现**（仅 `provider:baidu` 空配置，无调用代码）；会话模型目测坐标已否定（不可复现）。下一步 P3 稳定性矩阵（DPI/主题/版本/重名）。报告见 [p1-chat-search-group.md](research/weixin-cli/p1-chat-search-group.md)。**第二个 probe `p2-message-send`（P2 真实写，2026-08-11）真机通过**——定位当前会话输入框 + 发送单条 `你好`，vision 二次核对消息已发出（右侧绿色气泡 + 输入框清空）。两个关键结论：① **发送动作走 Enter 键，不靠视觉定位"发送按钮"**——Kimi 把发送按钮坐标误识为语音通话按钮（麦克风），微信 4.x 发送按钮视觉特征不稳定（常是输入后才出现的动态按钮），Enter 是零识别成本的稳定路径；② **输入框坐标可缓存复用**——窗口几何（位置/尺寸）不变则输入框中心 PNG 偏移恒定，切换会话/收发消息不改变布局，故首次定位后缓存（键=窗口几何），仅坐标失效（点击未聚焦/窗口移动/尺寸变化）才重新定位；发送终态判据改为"最后一条消息特征 + 输入框清空"（p1 的 overlay 判据不适用写动作）。发送链路：视觉定位输入框（可缓存）→ 剪贴板粘贴 → Enter 发送 → vision 核对。报告见 [p2-message-send.md](research/weixin-cli/p2-message-send.md)。**第三个 probe `p3-main-window-uia-observe`（P1 只读观察，2026-08-11）真机通过**——验证正常主窗口（非搜索态）「会话列表」「对话窗口」两个核心区域在 UIA 的可见性。结论：**两个区域 UIA 均零暴露**，把 p1 §5 搜索态结论推广到正常主窗口态。主窗口 TreeWalker 深度枚举仅 3 节点（2 个可见：窗口外壳 `Qt51514QWindowIcon` + 渲染画布 `MMUIRenderSubWindowHW`），零 InvokePattern / 零 ValuePattern / 零 ScrollPattern——连会话列表/消息区滚动都无法 UIA 驱动。含义：「读取会话列表」「读取对话内容」「滚动消息」「点击会话项」全部必须走外部视觉识别 + 物理坐标点击，UIA 路径在主界面核心交互上完全关闭。报告见 [p3-main-window-uia-observe.md](research/weixin-cli/p3-main-window-uia-observe.md)。**🔧 开发进度（2026-08-27）**：M2 聊天域四能力完成并真机通过——PostMessage 驱动层（点击/输入/回车/滚轮，免疫 RDP 会话光标纠偏）+ PrintWindow 遮挡截图 + target_ref（HMAC 签名、5 分钟 TTL）+ 四个 operation（weixin_chat_search 只读检索含最常使用分组与头像 kind 判定 / weixin_message_send 写动作含标题+残留草稿+写后三重校验 / weixin_history_read 滚动抓取+本地 RapidOCR 归属区分 / weixin_unread_list 未读角标扫描不开会话）；CLI 动词面 search/send/read + --json 机器输出；npm test 90/90 全绿。真机闭环用例通过：搜哈尼群→读记录→生成温馨回复→发送。**调研（2026-08-28）**：视觉定位模型对比评估——GLM-5.3-Flash 坐标与 kimi-k3 几乎重合且全部命中（差 5-24px），成本约 1/40、速度快 3-4 倍，结论可替代，报告见 [vision-model-comparison.md](research/weixin-cli/vision-model-comparison.md)。 | [设计](design/weixin/weixin-cli-design.md)、[计费关联设计](design/weixin/weixin-cli-billing.md) | [计划](plans/weixin/plan-weixin-cli.md) |
| 69 | wecom-cli 第一方企业微信操作 CLI / MCP Provider | 🔧 部分完成 | **M1+M2+M3 已完成（2026-08-31，三轮三智能体流程 + 全部真机端到端复验通过）**：`clients/wecom-cli`（aid-wecom，TS 骨架镜像 weixin-cli + PS1 驱动 + RapidOCR）。**7 个 operation**：`wecom_probe`（环境/登录态/当前页）、`wecom_add_customer`（添加客户全链路：通讯录→新的客户→添加→输号→回车检索→读微信名→添加→发邀请→「已发送申请」终态轮询）、`wecom_chat_search`（搜索联系人/群聊，返回 HMAC target_ref）、`wecom_message_send`（target_ref 消歧→标题校验→草稿检查→发送→三选二终态校验）、`wecom_unread_list`（未读会话快照）、`wecom_history_read`（滚轮翻页+页间去重读消息，仅 CLI）、`wecom_watch_poll`（新消息跟踪单轮：unread diff→点会话行→读增量→水位去重→NDJSON）+ CLI `watch` 循环。CLI + MCP stdio 双入口（6 MCP tool）。113 单测全绿 + typecheck 0 错误。**真机验证沉淀的关键事实**（详见设计文档 §5 补遗）：企微 5.0.9 丢弃全部注入输入（SendInput 键鼠/keybd_event）交互必须纯 PostMessage；Shift 修饰字符走 WM_CHAR（否则大写变小写）；搜索框残留必须点 × 清理+回读验证；overlay 渲染异步需轮询稳定；未读角标在头像右上角需红色 blob 检测+放大 OCR；进会话清角标，watch 水位读取后 last_unread 必须归零防漏报；时间分割线整行锚定防误吞消息。待开发：与服务端 wecom_personal_rpa channel 的集成（取代 C# 客户端出站执行职责）、watch 长循环部署形态。 | [探测与设计](research/wecom-cli/probe-and-design-20260829.md) | — |
| 20 | 浏览器混合执行、可视化与人工接管 | 🔧 部分完成 | Phase 0～1 已完成；Phase 2 已实现、待真实 Redis/PostgreSQL 门禁。2026-07-20 Phase 3 代码已收口：完成原 `tool_call_id` exactly-once continuation、原 Agent 后台 LLM 续跑、持久 resume worker、自动完成后台监测、人工超时 reaper、iframe 挑战识别、断线事件补取及前端轮询回收；三智能体流程和主控终检通过（browser 177、Phase 3 定向 32、Agent 相邻 12、前端组件 3，production build 与主入口 import 通过）。真实 Redis opt-in、PostgreSQL、双 Gunicorn worker、真实验证码页同 context E2E 和进程崩溃补偿仍待部署验收，故保持部分完成。架构已收口为 Agent Desktop 内置 browser runtime；Phase 4 浏览器侧只负责 `browser/1.0`、DesktopRuntimeExecutor、路由安全和联合验收。 | [设计](tools/browser/browser_visualization_design.md) | [开发计划](tools/browser/browser_execution_dev_plan.md) |
| 29 | PDF 工具质量验证增强 | 🔧 部分完成 | P0+企业文档增强+Playwright HTML 转 PDF 已完成：新增 inspect/render_pages/validate、结构化检查、生成后自动校验、页码语义统一和依赖探测；修复 split 保存顺序、无效页码静默成功、文件名安全、HTML 表格顺序、测试漂移和旧设计文档不一致问题；新增 clean_metadata/add_watermark/protect/compress/extract_images/rotate；复杂 HTML/CSS 默认优先 Playwright print-to-pdf，失败回退 fpdf2；Docker 增加 LibreOffice Writer 及构建期校验，DOCX 转 PDF 统一使用 LibreOffice 隔离配置目录并保留失败诊断。PDF 全量单测通过，127 passed；真实 Playwright 生成和结构校验通过。2026-07-01。**2026-07-15 新增图片支持**：md_to_pdf + html_to_pdf 复用 `_image_inliner`（扩展 HTML `<img>` 解析）解析 `file_id:`/远程 URL 为本地路径；Playwright 路径本地 src 转 base64 data URI、fpdf2 兜底路径用 `pdf.image` 双引擎渲染；`PdfProcessTool` 接入 tenant/user 注入（镜像 Word）。同步完成 **#34 Phase 3b 落盘闭环**（`_merge_results` 超长 content/markdown 走 `spill_large_content` 落盘，返回 file_path 供 read/grep 回读）+ **全规范核对**（错误返回接 `sanitize_error`，不再泄漏 `str(e)`）。21 新增 + 135 PDF 回归单测全绿。| [设计](tools/pdf/pdf_tool_gap_analysis_design.md)、[图片支持设计](tools/pdf/pdf-image-support-design.md) | [开发计划](tools/pdf/pdf_tool_quality_validation_dev_plan.md)、[图片支持计划](plans/plan-pdf-image-support.md) |
| 30 | PDF reportlab 固定版式生成器 | 📋 待开发 | 暂不开发，未来如出现强固定版式需求再评估。适用场景：结构化业务数据直接生成正式报价单、报告、对账单、审批单等，要求页眉页脚、页码、签章区、复杂跨页表格和版式位置稳定。当前复杂 HTML/CSS 已由 Playwright print-to-pdf 覆盖，不优先投入 reportlab。2026-06-30 | [设计](tools/pdf/pdf_tool_gap_analysis_design.md) | — |
| 32 | PDF 视觉回归样本集 | 💡 灵感 | 低优先级未来项。用于沉淀小型样例 PDF、渲染 PNG 或预期检查结果，后续在改动 PDF 生成器、渲染器、验证器时做回归校验，防止中文乱码、空白页、黑页、页数错误、表格溢出等质量退化。当前已有结构化校验和 Playwright HTML 转 PDF，暂不投入完整样本集建设。2026-06-30 | [设计](tools/pdf/pdf_tool_gap_analysis_design.md) | — |
| 34 | 工具总体优化梳理 | 🔧 部分完成 | 跨工具层面的优化梳理登记文档（区别于单工具设计）。进度：#34a `upload_to_remote` 工具直接删除 ✅（已提交）；#34b 全部 27 个业务工具 token 审查 + 规范 ✅；Phase 0/1（删工具+建规范+helper）✅；Phase 2 试点 http_api/pdf_process/paddleocr token 优化 ✅（token 对比达标，服务器验证通过）；#34c **大内容落盘检索闭环 + grep 工具** 🔧（pdf 侧已回填）——Phase 2 截断后完整内容丢弃形成信息黑洞，新增「截断→落盘临时文件→read/grep 回读」闭环（对齐 Claude Code harness），含 `_spill.py` 落盘管理器 + 新增 grep 工具（调 ripgrep）+ Dockerfile 装 rg；**2026-07-15：Phase 3b 的 pdf_process 回填已完成**——`_merge_results` 的 read/ocr/pdf_to_md 超长字段接入 `spill_large_content` 落盘，返回 file_path 供 agent read/grep 回读（http_api 落盘在先），见 [PDF 图片支持设计](tools/pdf/pdf-image-support-design.md)；#34d **文件工具对齐 Claude Code 水准** 📋——read/edit 的 offset 从 0-based 改 1-based（breaking，需适配 skill）+ edit 新增 replace_all 批量替换。开发计划已调整：Phase 3 升级为「文件工具对齐+grep+落盘闭环」优先实施。2026-07-06 | [总体设计](tools/tool-overall-optimization-design.md) / [落盘闭环+grep](tools/large-content-retrieval-design.md) / [文件工具对齐](tools/file-tools-claude-code-parity-design.md) | [开发计划](tools/tool-overall-optimization-dev-plan.md) / [落盘闭环计划](tools/large-content-retrieval-dev-plan.md) |
| 44 | Excel 智能模板填充工具（样例 + 数据 → 按版式生成） | 🔧 部分完成 | 通用**无状态**能力：任何"样例表格 + 结构化数据 → 按样例版式生成 Excel"的场景（旅游报价 / CRM 对账单 / 财务报表 / 贸易报价单）。**无状态单 action**：调用方每次必传样例（样例存储是调用方职责），工具数据感知分析样例结构 + 填充，**不建注册中心/DB/template_id**（原计划的 bs_excel_templates 取消）。扩展现有 `src/tools/excel/excel_template.py`（fill_template/结构感知填充/行样式保留）和 `ExcelProcessTool` 多 action 管线，不重写。**行数不匹配处理（核心正确性）**：数据行 vs 样例行 多/少/相等——多则按行样式续填、少则删除剩余样例行，**输出不得残留任何样例数据**（行级+单元格级强校验）。**样式完整保留**：以克隆样例为基底只改值、不重绘样式，保留颜色/字体/合并单元格/行高/列宽/数字格式/条件格式等；行增删后显式重锚定合并范围（补 openpyxl 不自动平移的缺陷）+ 补插入行行高复制。通用 data 契约（meta/rows/group_subtotals/totals，columns 可选）领域无关；`fill_with_sample` 库可被 skill 脚本直接 import。租户注入照搬 word_process_tool 双轨模式。Phase A 数据感知分析+行数不匹配渲染+不变式校验 / Phase B 分组合计人均+样例克隆渲染 / Phase C action 接线+租户+测试。消费者：旅游报价（#43）。2026-07-20。**Phase A+B+C 已完成（2026-07-20，经独立 CodeReview）**：`src/tools/excel/excel_template_ai.py` 实现 fill_with_sample 无状态填充 + 数据感知结构分析 + 行数不匹配渲染 + 多分组（自下而上增删+行号重映射）+ 分组小计/合计/人均 + 样式/合并/行高保留 + 残留不变式校验；`ExcelProcessTool` 接通 data 入参 + 确定性路由 + 双轨租户注入（agent 主循环 hasattr 钩子自动注入）；excel 全量 36 单测全绿（template_ai 18 + process_tool_template 6 + routing 12）。B.4 占位符兜底延后。 | [设计](tools/excel/excel-template-ai-design.md) | [计划](plans/plan-excel-template-ai.md) |
| 63 | 多源脏 Excel → 标准模板 LLM 抽取填充 | 🔧 部分完成 | 场景：用户一次上传多家供应商人员增减报表 + 一个标准模板，统一汇总填入模板（样本 `ExcelAI测试.zip`，5 来源 + 1 模板）。**定位：来源格式完全不可控，大模型解析能力就是"不规范→规范"映射的核心机制**——不建规则映射引擎，代码只做三件事：把单元格真实语义渲染给 LLM、确定性校验 LLM 输出并回喂修复、执行填充交付。需补 5 模块：M1 渲染层（值按数字格式渲染——Excel 序列日期/datetime/百分比 LLM 看不见数字格式）；M2 目标 schema 固化（一次抽取长期复用，模板指纹校验，不变永不重抽，摊销≈0，首版直接内置 18 字段 schema）；M3 LLM 抽取 action（脏结构理解/别名/多列合并/语义派生/值规范化全由 LLM，分块+JSON 容错）；M4 校验+修复回路（error 行回喂 2 轮，不静默丢行）；M5 合并去重+编排 skill（模板识别、xlsx+校验报告交付）。填充纯代码零 LLM（schema 固化后列绑定确定）。**点数测算（deepseek-v4-flash，tiktoken 实测）**：每 100 条 ≈ 25 积分（缓存 80%）~ 39 积分（无缓存），封顶 50；积分两本账——扣费端 1 积分=0.01 元成本、充值端 1 积分=0.1 元售价（内含 10 倍毛利）；报价建议面值 50~100 积分/百条 或 文件+条数双因子。**开发决议 D1~D20 已定**（宿主=领域 skill、库层进 src/tools/excel、模板识别确定性规则、渲染/分块/校验分级/交付契约、黄金样例验收，见调研文档 §7）。**业务三项已拍板（2026-08-19，设计无遗留歧义）**：① error 行修复 2 轮仍败→不写入进人工清单；② 同人多条不去重不合并、全部保留+标出处+报告按人分组提示；③ 敏感数据脱敏出域（占位符往返，LLM 全程不见真实证件号）+回写还原，具体方法后定、接口预留 mask/unmask 钩子。**邮件接入（2026-08-19 补充场景）**：场景延伸为 读邮件→判定相关附件→下载→进 ETL 管线；现有 email_read 只能列邮件（**无附件元信息、无附件下载、无日期范围搜索**，正文预览仅 200 字），需补 D21~D24——email_read 返回附件清单+SINCE 服务端日期过滤+预览可调、附件下载落盘到 skill_ws（25MB 上限）、附件相关性两级判定（扩展名+文件名关键词确定性预筛，未命中走 LLM 批量判定）、UID 水位幂等防重复处理。2026-08-19。**开发计划已出（2026-08-19，与 #64 合并排期 Phase 0~3）**：Phase 0 邮件整改基础（1~1.5 天）→ Phase 1 渲染层+黄金夹具标注（1 天）→ Phase 2 抽取+校验修复+**skill 子进程 LLM 计量接线（开工核对新发现的必须项：存量 skill 直连模型 API 零计量漏账，公用云计费依赖，本次建可复用模式）**（2 天）→ Phase 3 skill 编排+邮件附件 action+e2e（2 天）；全程三智能体流程。**Phase 1 已完成开发（2026-08-21，在 feat/excel-etl-phase1 分支待提交）**：`excel_reader.render_llm_view()` 落地 D5 契约（日期/百分比/序列日期按数字格式渲染——46239→`2026-08-05`、0.05+`0%`→`5%`；合并单元格非锚点空串；全空行跳过空列保留；300 字截断加 `…(截断)`；markdown 表原样输出、不消歧表头不去重列名）；新增 `excel_mask.py`（Q3 占位符往返 `[ID_n]/[TEL_n]`，数字串贪心回溯分词解决无分隔相连切分，同真值同占位符，已脱敏形式放行）；原样本 zip 不在本地，按 §2 脏点文档合成 `tests/fixtures/excel_etl/`——5 个 xlsx（build_fixtures.py 确定性可重建，重跑逐字节一致）+ expected_render.json（8 sheet 黄金渲染快照，与渲染器输出双向验证逐字符全等）+ golden_records.json（14 条标准记录，12 个明文身份证经 ISO 7064 MOD 11-2 校验位独立验证）；新测试 39 用例（渲染 23 + 脱敏 16）全绿，回归净增 39 通过零新增失败。三智能体流程通过：测试智能体独立复核（校验位手算、对抗探针、夹具再生）+ CR 零 P0/P1，CR 5 项 P2 转 Phase 2 设计输入（渲染行数上限防御、mask session 并发作用域、百分比分节格式加固、markdown 竖线转义、from_excel 兜底死代码维护）。**Phase 2 已完成开发（2026-08-21，在 feat/excel-etl-phase1 分支，三智能体流程通过待提交）**：① 新增 `src/tools/excel/excel_extract.py`（纯库不注册工具）——18 字段 schema 资产 `assets/schema.json`（template_header 别名数组 + sha256 模板指纹，`schema_matches` 指纹匹配 / `extract_schema_from_template` 不匹配时 LLM 一次重抽供 Phase 3 固化）；`extract_records`（D7 分块 50 行/块、超 24k 字符减半行数重切、表头区随块重复；D8 输出 `{"records":[...]}` 包装复用 `_extract_json` 容错、`_source` 由调用方钉死；D9 单块解析失败/空结果重试 1 次附失败原因、仍败任务级失败；`on_usage(usage,stage)` 逐 LLM 调用含重试计量回调）；`validate_records`（D10 error/warning 分级，身份证 ISO 7064 MOD 11-2 校验纯函数、脱敏形式放行+warning、社保≠公积金 warning 等）；`repair_records`（D11 仅 error 行回喂原行文本+已抽记录+错误清单 ≤2 轮，仍败进 manual_review 附原始行，warning 行不触碰）；`annotate_same_person`（D12 不合并不去重，完整身份证优先键/姓名+参保地+增减类型降级键，命中同人多条备注追加【同人多条：另见 X】顿号连接）；`run_extraction` 多 sheet 编排（抽取→校验→修复→同人标注，stats: extracted/repaired/manual_review_count/llm_calls）；`excel_template_ai._default_llm` 加 `return_usage=True` 可选参数取回 usage（默认 False 完全向后兼容，excel_extract 复用其 provider 分支不复制逻辑）。② 计量接线：`session_record.record_skill_llm_usage`（skill 子进程直调显式参数版，AID_* 环境变量回退，ChatRecordDB.create(source_type="skill_llm") 独立落账扣积分，异常只 warning 不抛）；`skill_executor._execute_command` 子进程 env 注入 AID_TENANT_ID/AID_SESSION_ID/AID_USER_ID（来自 current_tool_execution_context，缺失不设）；agent.py 主循环+execute_as_subagent 两处 skill_execute 拦截包 `tool_execution_scope`（与 delegate 拦截一致）。③ 夹具补 `生成标准模板格式.xlsx`（build_fixtures 从 schema 生成单 Sheet1 仅 18 列表头，fixture 级 .gitignore 排除）。测试新增 35 用例全绿（excel_extract 24——含 5 夹具 render→mask→run_extraction 与 golden_records 14 条字段级全等端到端、分块/容错/校验 D10 逐规则/修复回路 D11/同人 D12/指纹匹配/计量回调次数=LLM 调用数；record_skill_llm_usage 7；skill_executor env 4）；Phase 1 39 用例零回归，test_tool_result_truncation + excel_template_ai（含 llm 版）全通过，unit/tools 全量 1571 通过、24 失败全为存量环境依赖（Pandoc/Playwright 缺失，stash 验证与本次改动无关）。三智能体流程：测试智能体独立复核全绿（校验位手算、修复回路封顶、计量落库断言、数字口径修正 8→7）；CR 修复 1 P0（占位符身份证 `[ID_n]` 被 D10 判 error 致脱敏管线全量落人工清单——黄金 e2e 因 mock 返真值漏网，已加占位符放行+回归锚点）+ 2 P1（模块 docstring 管线顺序改为「mask→全程脱敏域→unmask」防 Phase 3 照错序泄漏真值；计量 usage 附 model 名+qwen/deepseek 缓存 token 归一，修错价）；P2 8 项转 Phase 3 消化（姓名子串误配、修复行错误清单丢失、warnings 不回传等）。**Phase 3 已完成开发（2026-08-21，在 feat/excel-etl-phase1 分支；三智能体流程全通过（CR 修 5 P1：IMAP SINCE 日期 locale 失效改固定英文月表、download_dir 限 storage/ 白名单防 LLM 传参任意写、fill 合并单元格模板先拆合并防 MergedCell 崩溃、输入路径递归兜底限 cwd 防跨租户 glob、真值复检行计入 manual_review_count 防漏报；P2 8 项登记下迭代——zip 解压上限、D24 下载失败水位误记优先））**：① 新增 `src/tools/excel/excel_fill.py`（纯库零 LLM）——`fill_template`（D15 克隆模板+表头去空白规范化匹配 template_header 别名列绑定、D13 排序增员前减员后组内按来源文件名+行号、数据行样式 copy.copy 复制表头行、终检行数/表头完整/无残留行）、`generate_report`（D16 独立 .md：摘要各来源抽取/写入/跳过、逐行 warning/error+原始行、同人多条分组、计量小计）、`output_names`（D17 `{模板名}_汇总_YYYYMMDD-HHMM.xlsx`/`{模板名}_校验报告_*.md`）。② `email_process` 补 `download_attachments` action（D22：uid/filenames 过滤/download_dir，25MB 上限超限跳过进 skipped，RFC2231/encoded-word 中文文件名解码复用 decode_header_value，BODY.PEEK 不标已读，同名附件数字后缀去重，文件名净化防路径穿越）；D21 补齐——read 返回 `attachments: [{filename,content_type,size}]` 元信息、`since` 参数走 IMAP 服务器端 SINCE（ASCII 服务端过滤，非法格式固定安全文案）。③ 新增 skill `src/skills/excel-to-template-1.0.0/`（SKILL.md 触发词/双入口/步骤化指南 + scripts/pipeline.py 子进程 CLI）——文件入口（--files/--zip，zip 解包防路径穿越只收 .xlsx/.xls）+ 邮件入口（--email-since：read(since)→D23 两级判定①扩展名∈{xlsx,xls,zip}+关键词增减/社保/公积金/人员/变动/通知/派单直下②未命中一次 LLM 批量判定→download_attachments 落盘；D24 UID 水位 `.email_uid_watermark` 重复执行跳过）；主流程 init DB 池（失败降级记日志）→render_llm_view→MaskSession.mask→run_extraction（on_usage→record_skill_llm_usage(source="excel_etl",stage=...)）→unmask→validate_records 真值复检（仍 error 转人工清单）→fill_template+generate_report→stdout JSON 供 cp 交付；模板识别 D3 指纹匹配（恰一命中即模板，零/多命中报错请 --template 指认）；指纹不匹配 LLM 会话内重抽 schema+报告提示（D14 只读降级）；schema 资产单一事实来源读 PROJECT_ROOT/src/tools/excel/assets/schema.json 不复制；`--llm-fake` 测试开关（fake 模式跳过 DB 与真实 LLM）；config.yaml master_agent.allowed 加 excel-to-template。④ 消化 Phase 2 CR 遗留 P2 5 项：_find_source_row 姓名精确匹配优先于子串、修复第 2 轮解析失败保留原始校验错误合并进 errors、run_extraction 回传 record_warnings（与 records 等长对齐）、非 dict 项丢弃计数 stats.dropped_items、extract_schema_from_template 校验 fields 元素形状（name/type/template_header 缺失按下标报错）。⑤ 夹具 build_fixtures.py 追加 build_zip()（5 来源+模板打一个 ExcelAI测试样本.zip，*.zip 全局 gitignore 按需重建）。测试新增 55 用例全绿（excel_fill 13：黄金 14 条写入读回字段级全等含 D13 排序/同人备注落表/终检失败路径/未匹配列 warning/样式复制冒烟/D17 命名/报告分节；email_attachments 18：download 全链路 mock imap（RFC2231 中文名/25MB 边界/filenames 过滤/BODY.PEEK/同名去重/错误脱敏）+read attachments 元信息+since 服务端条件+纯函数；excel_pipeline 19：zip 入口 golden e2e 字段级全等+模板识别四态+邮件两级判定与 UID 水位二次跳过+zip 路径穿越拒绝+DB 降级+schema 重抽+D4 .xls 拒绝；excel_extract 追加 P2 回归 5）；Phase 1+2 既有用例零回归（test_email_tool 34 原样通过），unit/tools 全量 1630 通过/24 失败全为存量环境依赖（Pandoc/Playwright 缺失，与 Phase 2 基线一致零新增）。**测试智能体独立复核通过（2026-08-21）**：115 用例全绿；stash 对照 24 失败与基线完全一致零新增；启动安全（import src.main/settings allowed/skill registry 发现/SkillRegistry 9 skill 含 excel-to-template）；独立 e2e 子进程亲跑 zip 夹具（14 条字段级全等、D13 增员 6 前/减员 8 后组内文件名稳定序、D17 命名格式、报告摘要合计 14/14/0 与 stats 一致、golden 无人工清单/无同人组）；mock IMAP 亲验 RFC2231 中文名落盘净化、25MB 边界（恰 25MB 放行/+1 跳过）、D24 同 UID 二次不重复下载、BODY.PEEK、zip 穿越（../ 与绝对路径拒绝且无文件逃逸）、P2 五项逐项注入对抗验证；复核追加 1 回归用例（邮件入口 D24 水位重跑零新附件时报错不再误导为"没有输入文件"，修 pipeline.main 文案+email 摘要），unit/tools 终值 1631 通过/24 存量失败。 | [调研+决议](tools/excel/excel-etl-gap-analysis.md) / [点数测算](tools/excel/excel-etl-points-estimation.md) | [开发计划](plans/plan-excel-etl-and-email.md) |
| 64 | 邮件工具整体审查与整改 | 🔧 部分完成 | 邮件是项目最早实现的工具（先于开发规范），随 #63 邮件附件场景触发全量审查。**资产**：3 工具（send/read/list_folders，687 行单文件）+ 加密凭据层 + 绑定 API（保存前发测试邮件）+ 前端设置页 + 22 个测试（mock 为主）。**P0 bug/风险 5 项**：① 同步 imaplib/smtplib 裸跑在 async execute 里阻塞事件循环（对照 word 工具 asyncio.to_thread 惯例）；② 软删除后重绑 upsert DELETE 分支早退丢配置（解绑再绑定永远"未绑定邮箱"）；③ IMAP/SMTP 无 timeout 可永久挂起；④ close/logout 不在 finally 连接泄漏；⑤ 本地过滤时整封拉取（含附件字节）×3 倍邮件量，应 HEADER 先行两段式。**P1 规范违反 4 项**：错误返回裸 str(e) 未接 sanitize_error、IMAP 原始响应调试日志 + "普通搜索对比"死代码、HTML 邮件正文为空（只解析 text/plain）、body_preview 硬编码 200 字。**删除/合并（2026-08-19 用户确认三合一）**：调试残留段删除；**三工具合并为 `email_process`（action: send|read|download_attachments 确定性分发，不建内部 LLM 路由器——区别于 excel_process，规避每次 ~0.75 积分路由费；系统提示词 schema 3 份→1 份省每轮固定 token）**，EmailListFoldersTool 取消独立形态（folders 改 read 返回字段+报错提示），附件下载作为 action 并入而非第 4 个工具。**新增**：#63 D21~D24 附件四件套。**行动**：第一步修 bug+合规+三合一+拆分 email_lib.py/工具薄壳（1~2 天，含 upsert 重绑回归测试）；第二步附件能力随 #63 P0。2026-08-19。**开发计划已并入 #63 合并排期**（Phase 0 承载邮件整改，见 [开发计划](plans/plan-excel-etl-and-email.md)）。**Phase 0 已完成开发（2026-08-19，待测试/CR）**：拆分 `email_lib.py` 同步逻辑层 + 工具薄壳；三合一 `EmailProcessTool`（action: send\|read，确定性分发，folders 并入 read 返回）；P0 全修（asyncio.to_thread 包裹、连接 timeout=30、IMAP finally 关闭、HEADER 先行两段式过滤）；P1 合规（sanitize_error 白名单、删调试残留/死代码、HTML 正文回退去标签、body_preview_len 参数化默认 500）；修 upsert 软删除后重绑丢配置 bug；引用方全迁移（agent.py / email_settings.py / planner / executor / SKILL.md / 角色权限）。测试：unit/tools/test_email_tool.py 重写（30 用例）+ 新增 unit/db/test_email_credential.py（9 用例，mock get_db_connection）+ 集成重绑回归，全绿。**三智能体流程已通过（2026-08-19）**：测试智能体独立复核数字一致 + 修复 INTERNALDATE 响应结构 bug（强化弱断言）；CR 修 2 个 P1（执行链不走 Pydantic——limit/body_preview_len 加夹紧归一、空收件人归一化后落误导文案改明确报错）+ 2 个 P2；主控终检 import 链 + 72 测试全绿、全量 unit 4235/57（57 全存量零回归）。**Phase 3 附件四件套已交付（2026-08-21，随 #63 Phase 3；三智能体流程全通过（CR 修 5 P1：IMAP SINCE 日期 locale 失效改固定英文月表、download_dir 限 storage/ 白名单防 LLM 传参任意写、fill 合并单元格模板先拆合并防 MergedCell 崩溃、输入路径递归兜底限 cwd 防跨租户 glob、真值复检行计入 manual_review_count 防漏报；P2 8 项登记下迭代——zip 解压上限、D24 下载失败水位误记优先））**：download_attachments action（D22）+ read attachments 元信息与 since 服务端过滤（D21）已上线（email_lib.list_attachments/download_attachments + 工具层分发/显示名/描述更新），新测试 18 用例全绿、既有 34 用例零回归（测试智能体 mock IMAP 独立亲验 RFC2231 中文名/25MB 边界/净化/BODY.PEEK 全通过）。待用户指令提交。 | [审查报告](tools/email/email-tool-audit.md) | [开发计划](plans/plan-excel-etl-and-email.md) |


## 渠道集成

> 本区覆盖企业微信（客服 wecom_kf / 应用 / 个人账号 RPA）、钉钉、飞书渠道接入。
> 企业微信个人账号 RPA（#29 系列）采用「服务端拉取会话存档 + C# 客户端 PowerShell 自动化」双链路，**第一阶段真机验收已于 2026-07-16 通过**（agent2 消息入库 / 6.2 漏抓率 / outbox 端到端 / 附件与多动作发送 / 生产 Secret 配置）；持续集成与各子功能进度见 #29 及 #29a~#29d 子条目。

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 29 | 企业微信个人账号 RPA 接入 | 🔧 部分完成 | **服务端**（已完成）：schemas/db/SQL 表结构锁定共享契约；auth(HMAC)/router/message/action_client/adapter/connection/secret_crypto 七模块按 protocol.md §B 签名实现，68 单元测试通过；callback/config/files/ws 路由接入 main.py；6 条集成测试全部通过；管理前端 P1.1 已交付；指标+告警 P1.4 部分交付（83 测试通过）；平台后台绑定管理 Tab + 「+ 新增绑定」按钮已交付。**服务端拉取会话存档模式**（2026-07-03 完成 Phase 1-11，**2026-07-08 真机验证修复 2 个关键 bug**）：放弃客户端轮询为主方案，改为服务端直接拉取企微会话存档（回调 + 拉取双模式）。复用 `tenant_channel_configs` 表 + `ChannelConfig.vue` 前端，新增 `listen_mode` 字段（第一期强制 'server'，client 模式前端禁用、代码保留）。archive 模块含：credential_codec（5 字段 Fernet 加密 + 强制 server 防绕过）/ callback_crypto（企微签名 + AES 解密，复用 wecom.crypto.WeComCrypto + hmac.compare_digest）/ chat_crypto（**RSA-PKCS1v15** + AES-256-CBC，commit 5791d36 修正：早期"OAEP-SHA1 与 C# 对齐"是错的，企微官方文档要求 PKCS1，真机密文验证 3 条全 PKCS1 通过、OAEP 全失败）/ http_client（access_token Redis 缓存 + 45009 异常 + 3 次重试）/ fetcher（拉取密文 + 解密 + 构造 envelope + 复用 _process_inbound_message，Redis 分布式锁防多 worker 并发）/ poller（60s 兜底轮询，main.py lifespan 启停）/ callback_handler（GET echostr + POST 事件双验签兼容，按 body 格式 XML/JSON 天然分流）/ verifier（5 步链路自测 + 5 类错误诊断）/ audit（6 类事件接入 fetcher/callback_handler）。**SDK 调用子进程隔离**（commit c5ee158）：gunicorn worker 加载 cryptography/psycopg 后加载 .so 破坏 C 堆导致 worker 静默退出，ctypes 实现整体移到 `_sdk_inner.py`（子进程内 import），主进程走 multiprocessing.Pool(spawn, size=1)。`_process_inbound_message` 加 source 参数区分来源。**测试**：214+ 单元/集成测试通过，archive 模块覆盖率 91%。**真机验证进度**（详见 memory project_wecom-personal-rpa-server-archive-verification）：verify 接口 ✅；fetcher 拉密文 ✅；PKCS1v15 解密 ✅；消息入库 + 6.2 漏抓率已于 2026-07-16 真机验收通过 ✅。**客户端方向已重定（2026-06-26）**：放弃 Qwen3-VL 视觉定位 + FlaUI/UIA3 + Windows OCR 三条路径（均已真机验证失败）；放弃把 PS 重写到 C#。新方向：① C# 客户端通过 Process+JSON 调用 PowerShell 脚本作为自动化后端（复用 `debug-navigate.ps1` 真机验证能力）；② 服务端拉取会话存档（已上线，见上）；③ Fallback 监听方案当前所有候选（Windows 通知/视觉/OCR）都不够稳，**先占位不开发**；④ 未登录二维码 30 秒一次截取 + base64 直推服务端 Redis（30s TTL，不入审计/DB）；⑤ 绑定级监控白名单（DB `monitor_user_names`/`monitor_user_ids` + 协议下发 + 客户端缓存 + 服务端二次校验）。**已删过时文档**：vision-design / vision-breakthrough / vision-plan / enter-fix-design / enter-fix-plan / ps-automation-design / ps-automation-plan（均已永久废弃，不再保留误导后人）。**客户端入站消息功能已完全删除**（commit `0c74769`，2026-07-08 用户决策落地）：客户端无公网域名收不到企微回调，拉取+解密全部归服务端负责。已删 `Client.App/MessageArchive/` + `Client.App/Inbound/` 整目录（9 个 `.cs`）、对应测试及 DI 注册；C# 端 OAEP-SHA1 bug 随删除自动消失。**遗留（第一阶段已清零）**：原"立即做 agent2 部署 + 6.2 漏抓率测试"已于 2026-07-16 真机验收通过；"客户端入站功能全删"已由 commit `0c74769` 完成。#29a~#29d 子条目均已真机验收通过并归档至 ideas_finished.md；剩余推进项为客户端增强方向 ④⑤（未登录二维码推送、绑定级监控白名单），属后续规划。 | [协议](system/wecom-personal-rpa-protocol.md) / [架构设计](system/wecom-personal-rpa-design.md) / [客户端设计](system/wecom-personal-rpa-client-design.md) / [绑定管理 Tab 设计](system/wecom-personal-rpa-portal-binding-design.md) / [服务端监听存档设计](system/wecom-personal-rpa-server-archive-listener-design.md) / [SDK 部署](system/wecom-personal-rpa-sdk-deploy.md) | [服务端+部署计划](plans/plan-wecom-personal-rpa.md) / [客户端计划](plans/plan-wecom-personal-rpa-client.md) / [绑定管理计划](plans/plan-wecom-personal-rpa-portal-binding.md) / [服务端监听存档计划](plans/plan-wecom-personal-rpa-server-archive-listener.md) |
| 56 | 企微客服账号引流归因（客服账号管理 + 二维码 + C端客户引流统计） | 🔧 部分完成 | **Phase 1 后端 + Phase 2 前端已开发完成**（2026-08-18）：企微 API 5 方法（account_add/del/update/list + add_contact_way）、`customer_referrals` 表（init/db_update 已登记）、账号 CRUD API（创建头像链[上传>租户logo>默认占位] + scene 生成 + add_contact_way + 二维码 base64 data URL；编辑支持换绑 tenant_user_id[open_kfid/scene/contact_url 不可变]；删除先企微后本地幂等）、channel_routes enter_session 引流归因 + 欢迎语 + 到期/积分消息入口拦截（固定话术不计费）、external_customers `/referral-stats` + referrer 返回/筛选。前端：ChannelConfig.vue 账号列表 + 创建/编辑弹窗（头像/到期/积分/标题/绑定员工）+ 二维码回显/下载 + 换绑确认框 + 删除确认；ExternalCustomerService.vue 双 Tab（客户对话记录[引流人显示 + 下钻筛选] + 引流统计[近7/30天/自定义日期段 + 总引流/总消息卡片 + 员工表格下钻]）。测试：单测 20 + 相邻回归 76 通过、前端 build 0 错误、路由挂载 401 验证通过。**2026-08-20 修复积分上限 3 bug**：会话按 `channel_chat_id`（open_kfid）拆分——session_id 生成/缓存 key/DB 查找条件均纳入（空参时行为不变），同一微信用户从不同客服账号进入各自独立会话，修复①已使用积分恒 0/10、③超限不拦截；【积分用量】每日明细加通用「渠道会话」列（`channel_label` 反查客服账号名，其它渠道回退显示会话 id），修复②明细不显示消费来源账号。测试：session 单测 +18、daily-detail 集成 +3、相邻回归通过、容器 import OK、模拟数据复验（A 账号积分归集 5.0 / B 0.0）。**2026-08-20 外部接待客户页改「客户×客服账号」组合展示**：`UserDB.list_external_users` 改组合粒度（wecom_kf 按 `channel_chat_id`[open_kfid] 拆分、其它渠道折叠为「客户×渠道」单行，避免 RPA conversation_id 不稳定行爆炸）；`ChannelSessionManager.list_sessions` 加 `channel_chat_id` 过滤（None 不过滤 / '' 匹配 NULL-or-empty legacy 会话 / 非空精确匹配）；`/users` 返回 `kf_name` 反查（ChannelConfigDB 一次建表）；`/users/{id}/sessions` 透传 channel_type + channel_chat_id。前端左侧列表改组合条目（三元组 key + 选中匹配 + wecom_kf 客服账号徽章），点击按组合过滤加载会话（CR 修复：折叠渠道不传 channel_chat_id，避免 NULL-or-empty 漏掉非空 chat_id 的 RPA 会话）。测试：新单测 7 + 集成 5 + session 回归 18 = 30 全绿、三智能体流程通过（测试 50 回归 + CR 修 1 P1）、前端 build 0 错误。**2026-08-21 外部接待客户页优化（普通用户可见 + 数据隔离 + 交互优化）**：①客户列表引流信息加 tooltip（`referral_time` 首次访问时间 + 引流人，'i' 图标悬停显示「首次访问 XX 的客服账号」）；②客服账号徽标加「客服：」前缀；③搜索框缩窄 + 客服账号下拉框筛选（新增 `GET /kf-accounts`、`/users` 支持 `channel_chat_id`）；④权限扩展：普通用户（引流员工）可访问，仅见自己负责客服账号（`kf_account.tenant_user_id`==自己）的客户/对话 + 自己引流客户；A 引流后客户与 B 聊天，A 不可见 B 对话（会话/消息按可见账号 fails-closed，非自己账号不反显 kf_name）。测试：集成 +6（普通用户隔离）共 11 全绿、三智能体流程通过（CR 修 1 P2 隔离缺口）、前端 build 0 错误、main import OK。**待办**：Phase 3 真机联调（需企微后台开通微信客服 + 可调用接口应用）：创建账号企微可见、扫码归因落库、到期/积分拦截话术、编辑同步企微、删除失败回显。 | [方案](channel/wecom_kf/kf-account-referral-plan.md)（含开发计划） | — |
| 57 | 微信客服回复长图化 + 废除渠道约束提示词 | 🔧 部分完成 | **开发+单测完成（2026-08-19），待部署真机验证**。① 废除 `WECOM_KF_CHANNEL_PROMPT`（Layer 1 产出节俭提示词）：生产 trace 实测它会干扰工具失败后的恢复路径——fill_template 报错后模型弃模板走 markdown 一次性生成，Web 端无此提示词则重试模板；废除后配额风险由发送侧承接。② adapter.send_message：纯文本超过单条 2048 字节、需要切分时直接整段渲染为 1 张长图发送（「需切分即长图」，不按切分条数判定；含表格/图片走长图为既有能力，回归确认）；长图失败降级分段逻辑不变。③ 长图渲染/上传/发送失败日志 warning→error（带 open_kfid/字节数），避免 Playwright 静默降级无感知。测试：test_wecom_kf_adapter.py 29 过（新增 3 个长文本分支用例），channels 全目录回归通过（3 个存量失败与本次无关：QR 生成与 deploy SQL 断言，stash 验证）。**已设计暂缓**：附件超配额打 zip 以 file 消息发送（见配额方案文档）。**待办**：部署测试租户验证三类场景（表格回复长图、超长纯文本长图、模板填充与 Web 行为一致）。 | [配额方案（含 2026-08 变更）](channel/wecom_kf/reply_quota_control_plan.md) | — |
| 58 | 微信客服处理超时等待提示 | 🔧 部分完成 | **开发+单测完成（2026-08-20），待部署真机验证**。智能体处理微信侧用户消息超过 N 秒（默认 15s，如 LLM 多轮工具调用）未回复时，先发送提示语「我正在处理您的问题，可能需要几分钟，请稍等下。」，避免用户不知道智能体在工作。配置放**渠道级**：`tenant_channel_configs.config` 顶层 `waiting_indicator`（enabled/delay_seconds/message），整条 wecom_kf 渠道所有客服账号统一生效；前端 `/t/:tenant_id/channels` 编辑弹窗可配置（默认启用）。后端：`_get_waiting_indicator_cfg`（容错读取 + 字段缺省回退默认常量）+ `_process_with_waiting_indicator`（**非取消式 watchdog**：create_task + shield + wait_for，超时只发提示、不取消任务——session_queue 处理器持有 Redis 锁，取消会泄漏锁/cancel 标志）；`WeComKfAdapter.waiting_indicator` 经 ChannelFactory `**config` 注入，配置 PUT 后 `invalidate_adapter` 即时生效。仅对真正走智能体的消息启用（merged 场景 process 秒回不触发）。测试：新单测 13 过 + wecom_kf adapter 回归 29 过、前端 build 0 错误、容器 import OK。**待办**：部署测试租户验证超时提示发送与 merged 不触发。 | [计划](plans/plan-wecom-kf-waiting-indicator.md) | — |
| 59 | 微信客服售前咨询客户留资（手机号 / 顾问微信二维码） | 🔧 部分完成（Phase 1-3 已完成，Phase 4 独立排期） | **适用边界**：仅售前咨询场景需改造；售后客服支持场景无需改造，现有功能即满足。**核心洞察**：wecom_kf 会话是一次性触点，留资本质是把"一次性客服会话"转化为"可长期触达的客户资产"；三种人工触达手段（转人工/留手机号/加微信）统一设计，共用判定入口。**v2.0 架构（2026-08-21）**：客户留资从「渠道能力」调整为「**技能 + 智能体能力**」，四层解耦——①**渠道层**（wecom_kf）回归**纯对话通道**，不实现留资业务逻辑，仅注入会话上下文；②**智能体层**：新建 `subagents/pre-sales/` **售前咨询智能体**承载售前留资，`after-sales` **回归纯售后**（原售前+售后双重职能拆出）；③**技能层**：新建 **lead-capture 技能**（策略知识包：触发判定/留资话术/需求字段收集），pre-sales 跑通后可供旅游咨询等其他智能体在 `skills.allowed` 复用；④**工具层**：新增 `record_lead_capture` 工具（catalog=True，get_kf_context 渠道隔离，读会话状态→写线索→更新状态→返回顾问二维码）。**方案要点**：①判定标准由租户管理员改智能体提示词决定，默认宽松（购买意向 或 需求字段覆盖度），客户主动发起留资为最高优先级信号；②留资方式按场景推荐（客服在线→加微信，不在线→留手机号），两条话术都给客户自选；③顾问二维码单一字段（不区分微信/企微）`kf_account.employee_qr_file_id` 存 ImageRegistry file_id，**TTL 风险用 knowledge_base 永久语义+显式清理规避**；④**线索表单独新建 `bs_lead_capture_leads`**（中性表名、渠道无关，不与既有 `bs_customer_followup_leads` 混用，通过 customer_user_id 可关联）；⑤状态机轻量两层协同（技能层策略话术推进 + 工具层仅记录 `channel_sessions.metadata.lead_capture` 结果状态防重复）；⑥即时通知+**留资统计报表**（复用引流统计模式：日期段 7d/30d/custom + 统计卡片 + 客服账号分组表，前端外部接待客户页新「留资线索」Tab），不做定时汇总；⑦手机号加密存储，老客户识别避免重复留资。**MVP 分期**：Phase 1 工具层（线索表+record_lead_capture+会话状态注入+防重复，渠道零业务改动）→ Phase 2 技能+智能体（lead-capture 技能 + pre-sales 智能体 + 顾问二维码配置 + 留资话术）→ Phase 3 运营侧（线索管理+留资统计报表）→ Phase 4 after-sales 回归纯售后 + 租户渠道账号切换（独立排期）。7 项决策点已确认（判定标准/主动发起/转接能力即转人工/即时通知+统计报表/单一二维码/架构分层/独立线索表）。各 Phase 按项目规范（建表登记 init/db_update、手机号加密、普通用户隔离、X-Tenant-Id）。**开发进度（2026-08-21）**：Phase 1 ✅（`bs_lead_capture_leads` 表 + `LeadCaptureDB` + `record_lead_capture` 工具 + 会话状态注入 + 防重复 + 渠道隔离）；Phase 2 ✅（`subagents/pre-sales/` 售前咨询智能体 + 顾问二维码配置后端/前端 + 留资即时邮件通知 + 工具 catalog=True）；Phase 3 ✅（后端留资 API：/lead-stats、/leads、/leads/{id}、PATCH 阶段 + 前端「留资线索」Tab：统计卡片 + 客服账号分组 + 线索列表 + 详情弹框阶段流转 + 普通用户隔离）。Phase 4（after-sales 回归纯售后 + 租户渠道账号切换）待独立排期。 | [设计](subagent/pre-sales/lead-capture-design.md) | [开发计划](subagent/pre-sales/lead-capture-dev-plan.md) |
| 60 | 售前咨询外部推送重构（unionid 查重 + 每轮跟进记录） | 📋 已完成方案 | **触发**：2026-09-03 实测（tr_9483b1bf02af4f51）同一微信用户 2 次留资导致向 10605 推送 2 条客户 + 2 条跟进记录。**产品需求**：①同一 unionid 在第三方只保留 1 条客户，信息更新走修改接口（10605 已加 t_kehuxinxi.unionid 字段）；②每轮问答（用户一句 → 智能体回复 1~5 句）推送 1 条跟进记录；③留资与推送解耦（留资保留但不作为推送触发条件）。**补充要求**：客户名称用微信昵称兜底（替代"待补充"）；微信头像/性别透出给 Agent 供后续应用选用。**架构分工**：租户差异逻辑全部下沉到租户上传的 pre-sales-api.md（unionid 查重建/改分流、external_userid 兜底、每轮跟进记录、昵称兜底）；SUBAGENT.md 只保留通用表述"每完成一轮问答按 pre-sales-api.md 推送"。**系统侧改动**：①kf context 注入 channel_user_info（nickname/avatar/gender/unionid，零额外 API 调用）；②新增通用工具 get_channel_user_info 透出微信用户信息；③（可选 P2）client_token 按 tenant+assignee 缓存。**关键核实结论**：users 表 wx_unionid 为空是因企微未绑定微信开放平台（代码链路完整），wx_openid 在 wecom_kf external_userid 体系天然不存在；external_userid（回调 FromUserName，wm 开头）对同一企微恒定可作查重键。**决策（2026-09-03）**：Q1 external_userid 复用 t_kehuxinxi.unionid 字段落库（10605 无需加新字段）；Q2 client_token 实施 Redis 缓存（10605 token 有效期 1 天，TTL 23h，Code=-99 强刷，技能新增 delegate_login.py）；Q3 跟进记录由 LLM 生成摘要（Agent 循环内无额外调用）；Q4 每轮全推不设内容门槛。 | [方案](subagent/pre-sales/external-push-redesign-plan.md) | — |
| 61 | 售前推送代码级兜底（recap 任务 external_push） | 🔧 部分完成（代码实现完成，待服务器 agent2 实测） | **触发**：9732230b 提示词驱动的「每轮问答推送」实测不生效（2026-09-04 trace tr_3e7123a2f23a41de / tr_d07191f6a8674932 交叉验证：qwen3.8-flash 与 DeepSeek-v4-flash 均不发起推送调用，system prompt 含指令、工具齐备、无异常，换模型无效）。**方案**：推送从「LLM 自觉执行的多步工具链」改为「代码在回复送达后异步执行的固定流程」，归入 **62 号 recap 机制**作为第一个任务实例（适配器 external_push_10605）——①幂等由 recap runner 统一（Redis SET NX recap_task:{tenant}:{task}:{round_message_id} TTL 24h）；②LLM 单轮生成对话摘要（无工具调用，record_background_llm_usage 计费，失败降级截断原文）；③复用 delegate_login 缓存取 client_token；④代码实现 10605 客户查重/建改分流（external_userid→unionid）+ 每轮跟进记录。业务规则全部继承 60 号方案（不重复决策）。**新增模块**：src/services/recap/tasks/external_push_10605.py（登录复用技能脚本函数）；触发点 session.py process_and_persist 末尾（send_ok=True，全渠道收口）；config 开关 external_push.pre_sales.enabled。**配套**：SUBAGENT.md 增加 recap 配置块 + 推送章节降级为「后台自动，无需操作」；10605 文档 §12 标注已代码化。**附加收益**：客户零延迟感知推送、推送故障与主回复隔离、可独立重试。**遗留**：Q1 任务崩溃丢失接受（下轮自然补推，强可靠另加队列 P2）；Q3 电商客服 11022 实时查询另行设计意图预取。**2026-09-04 通用化改造（CodeReview 后）**：删除 10605 硬编码适配器，改为租户文档驱动 + LLM http_api 工具循环的通用实现 src/services/recap/tasks/external_push.py——BASE_URL/接口路径/模块字段元数据全部由租户文档 storage/tenants/{tid}/templates/pre-sales-api.md 指定（文档顶部 ```api-meta 机器可读块声明 login_url/user_token_name/external_userid_field，缺省回退 client_token/unionid），推送序列由主模型按文档多轮 http_api 调用自主完成（轮次上限 external_push.pre_sales.max_tool_rounds=8；report_push_result 确定性终止；Code=-99 强刷委托登录仅一次；连续 3 次工具失败熔断；每轮 record_background_llm_usage(source=pre_sales_push, model 显式传) 计费；文档全文不截断注入 system_prompt）。10605 文档模板补充 api-meta 块示范，待服务器 agent2 实测。**2026-09-07 降延迟 + 计费缺口修复（trace tr_8bc6bd794c14450f 实测 24s，其中推送循环 4 轮主模型调用占 23s）**：①推送循环由 chat_with_tools 改 chat_lite（qwen3.8-flash），system 消息并入 messages 首位（chat_lite 无 system_prompt 形参），ValueError 回退主模型；②api-meta 新增 push_exclude_sections 键 + _strip_excluded_sections 裁剪注入文档章节（10605 裁剪登录/详情/跟进 4 章约 19KB/32%，支持中英文逗号）；③system prompt 增加并行工具调用约定减少轮次；④计费缺口修复——runner._run_tasks 开头清空 SessionRecord ContextVar（create_task 拷贝指向已落库 record，add_llm_usage 累加永不持久化），使 record_background_llm_usage 走独立落库分支，计费 user_id 归属到具体用户；provider 响应不含 model 键，新增 _resolve_lite_model_name() 显式解析 lite 模型名保证单价归属（CodeReview P1）；record_background_llm_usage 异常日志 debug 提升 warning。**待办**：服务器租户文档副本同步 push_exclude_sections 两行后真机验证（预期时延 <10s + chat_records 出现 background_llm/pre_sales_push 计费行）。**2026-09-08 执行进程隔离 + trace 观测**：①执行从 HTTP worker 移到 background runner 独立进程（触发侧 trigger_recap 序列化 payload 经 Redis 队列 recap_task_queue 入队，background_runner._recap_consumer 2s 轮询 LPOP 执行；Redis 不可用降级进程内 asyncio 执行）——根因是 gunicorn max_requests=1000 使 worker 每 40-60 分钟优雅自重启，10:52:34 一次重启把进行中的推送静默杀掉（start 无 done），max_requests 同步调大到 20000/jitter 2000；②recap 记录进当轮对话同一 trace——触发时从 record_service.trace_collector 捕获 trace_id，trace_persist 新增 append_recap_span/append_recap_summary（obs_spans 追加 generation/span、obs_traces.metadata.recap 摘要），管理后台 TraceDetail 可见推送全过程，计费通路不变。**2026-09-08 Code=-99 请求方式偏差修复（14:33 实测事故）**：lite 模型（qwen3.8-flash）偶发把文档声明为 POST 的查询类接口自作主张改成 GET（鉴权 Header 缺失，ERP 返回 -99「请求缺少身份令牌」，强刷 token 救不了形态错误，连续 3 次失败熔断，本轮推送 failed）。修复：①api-meta 新增可选 http_method 键，parse_api_meta 大写归一（未声明移除），_run_push_loop 执行 http_api 前经 _normalize_http_method 确定性强制纠正 method 并记 tlog（未声明不干预，向后兼容）；②system prompt 认证约定追加「按文档声明的请求方式调用，勿因查询自行改 GET，每次完整携带全部鉴权 Header」；③服务器租户文档 9b9fb62aff5e api-meta 已补 http_method: POST（对旧代码为无害未知键）。单测 tests/unit/test_external_push_method_guard.py 8 例通过。待部署 agent2 后真机验证。 | [方案](subagent/pre-sales/external-push-code-hook-design.md) | — |
| 62 | 子智能体 Recap 机制（轮后异步沉淀任务） | 🔧 部分完成（代码实现完成，待服务器 agent2 实测） | **定位**：SUBAGENT.md 目前只有对话期配置（tools/skills/context），缺「每轮问答结束后沉淀类动作」的表达位。**Recap 定义**：每轮问答结束、回复送达之后由代码异步执行的沉淀任务——决策者是代码不是模型；模型只做适配器内部的单轮文本生成（如摘要）；与中长期记忆整理（background_runner 周期触发）边界明确，不合并。查询类动作（查商品/订单/知识库）不属于 recap，保留 LLM 工具调用。**配置**：SUBAGENT.md frontmatter `context` 后新增 `recap.tasks` 块（name/when/enabled），无 recap 块 = 行为与现状一致；DB 覆盖智能体暂不支持（subagent_definitions 无对应字段，Q2 暂缓 DB 化）。**架构**：src/services/recap/——runner.py（配置读取、幂等、任务分发、故障隔离、汇总日志）+ tasks/ 注册表 + 适配器契约（RecapPayload dataclass，HTTP timeout 15s、串行、异常上抛由 runner 兜底）。**触发点**：ChannelSessionManager.process_and_persist 末尾（send_ok=True 才触发，全渠道单一收口，agent 实例直读 subagent_config.recap）。**幂等**：recap_task:{tenant}:{task}:{round_message_id}（Redis SET NX TTL 24h，键为本轮落库 user message_id）。**适配器 LLM 统一约定**：单轮无工具 + record_background_llm_usage(source="recap_<task>") 计费 + 失败降级不弃任务。**未来任务示例**：satisfaction_check / customer_profile_sync / lead_scoring / conversation_digest（仅示意）。**决策**：Q1 崩溃丢任务接受；Q3 不做并发控制。**2026-09-08 执行进程隔离 + trace 观测（机制级改造）**：触发侧与执行侧解耦——API worker 仅入队（Redis FIFO recap_task_queue，仅 Redis 不降级内存），background runner 新增 _recap_consumer 消费执行（_run_tasks 双入口共用，幂等/开关/分发逻辑不变）；详见机制设计 §4.4/§4.6 与 cache_usage.md §7.4.6。 | [机制设计](subagent/recap-mechanism-design.md) | — |
| 63 | 售前推送链路修复（头像/性别注入 + http_api 审计 + 隐藏命令清留资） | 🔧 部分完成（代码+单测完成，待部署 agent2 真机验证） | **触发**：2026-09-08 测试环境（agent2）排查确认三问题——①推送第三方缺微信头像（推送上下文只注入昵称，LLM 无处取头像，`weixintouxiang` 留空）；②http_api 推送成功零日志、推送原文不可追溯；③客户留资后发「新会话」仅软删消息，`metadata.lead_capture` 仍存留，推送仍取到昨日手机号（"穿越"）。**改动 1 头像/性别**：users 表加 `gender SMALLINT DEFAULT 0` 列（init-postgres.sql + db_update.yaml 2026-09-08 13:23:51 增量 + database_system_table.md 同步）；auto_register 新建/更新用户时从企微 batchget 返回的 gender（0未知/1男/2女）落库（UserDB.update_info allowed_fields 放行）；external_push `_collect_context` 按 session.user_id 读 users.avatar_url/gender 注入 ctx，`_build_user_message` 客户上下文段新增微信头像/性别行（性别复用 channel_user_info.GENDER_LABELS，仅当租户文档声明性别字段时推送）。**改动 2 http_api 审计**：logging.py 新增独立审计 sink（`log/agent/http_api_audit_YYYYMMDD.log`，filter=_is_http_audit，主日志/控制台/error/JSON sink 反向过滤防重复），命中 log_retention 15 天自动清理；HttpApiTool.execute 成功/超时/连接失败/异常四路径统一 `_write_http_audit` 单行 JSON（ts/duration_ms/tenant/session/subagent/method/url（剥 query）/query_params+body+form_data（凭证键 `_SENSITIVE_KEY_RE` 递归脱敏为 [REDACTED]）/status_code/response_body（20KB 截断 truncated 标记）/error），审计保留手机号明文（用户拍板，追责需要），异常不阻断业务。**改动 3 隐藏命令清留资**：LeadCaptureDB.delete 物理删除线索（lead_id+tenant_id）；hidden_commands 新增 `_clear_session_lead_info`——「清空会话/新会话」删消息后 ①全量合并回写 metadata 移除 lead_capture（update_session 整体替换语义，保留 service_state 等键）②非 converted 线索物理删除（converted 仅清 metadata 断推送引用）；tenant_id 为空不删线索；全程 try/except 失败不阻断命令文案；to_thread 包裹同步 DB 调用；四渠道触发点自动生效。**验证**：6 个测试文件 188 passed（dev_test.sh 容器内），db_update.yaml 校验通过；高风险三智能体流程（独立测试 + 独立 CodeReview 两轮，P1=converted 除外/P2 全修）。**待办**：部署 agent2 后端到端验证（留资推送带头像、新会话后库中 metadata.lead_capture 消失+线索行删除、再问不再带手机号、审计文件有完整原文）。关联 59/60/61 号。 | — | — |

| 64 | 留资线索动态刷新（lead_refresh：意向度 + 需求分条 + 人工归属） | 📋 已完成方案 | **触发**：2026-09-10 产品需求——留资后客户继续交流（智能体轮次 + 转人工期）仅落 `channel_messages`，线索行不再更新，运营页看不到最新客户状态。**方案**：复用 recap 框架新增 `lead_refresh` 适配器（拉模式——执行时自采 channel_messages 快照，防抖跳过的消息不丢失）；双触发入口：A 智能体轮次（现有 trigger_recap 钩子 + SUBAGENT.md recap.tasks 加一行，零代码）；B 人工期客户消息（`_persist_kf_context_customer_message` 落库后 `enqueue_lead_refresh()` 轻量入队，**不调起智能体**——background_runner 消费只认 payload.task_config，解决"转人工后无 recap"缺口）。**字段**：`bs_lead_capture_leads` 加 6 列（intent_level/intent_reason/demand_points JSONB/servicer_name/last_human_transfer_at/last_analyzed_message_id）；`transferred_to` 现有列复用（当前无人写入），由 `transfer_to_human` 工具转接成功时事件驱动回写（确定性数据零 LLM 成本）；`demand_summary` 保持留资时点快照不覆盖。**防抖**：Redis `lead_refresh_cooldown:{tenant}:{lead}` SET NX EX 300，LLM 失败删占坑键重试。**计费**：record_background_llm_usage(source="lead_refresh")。**解耦**：适配器只认 session.metadata.lead_capture，对智能体零感知，其他智能体启用 = recap.tasks 加一行；写操作收敛在 LeadCaptureDB 新方法（update_analysis/update_transfer_info）。**待开发**：DDL 三处同步、适配器、双入口、转人工回写、前端留资线索 Tab 展示（意向度徽章+需求分条+人工归属）。开放问题：增量游标（Phase 2）、servicer_name 数据源、人工期是否推送第三方、租户级意向度判定标准。 | [设计](subagent/pre-sales/lead-capture-refresh-design.md) | — |

## 前端

> 2026-07-15：Agent Desktop 增加可交付 API 配置。构建必须显式传入 API 基址并写入受控包内资源；首次启动原子初始化 `%APPDATA%\aid-agent-desktop\desktop-config.json`，升级保留且可编辑；`AID_AGENT_API_BASE_URL` 仅作为最高优先级运维覆盖，非法或缺失配置 fail-loud。详见 [Windows 编译与打包手册](system/desktop-agent-client-build-manual.md)。
>
> 2026-08-12：Agent Desktop 开发计划升级为 v2.4。保留 4 个产品里程碑，将主线细化为 14 个可独立验收的执行阶段；D 拆为协议/Gateway、Coordinator/核心对话、Event Store/Relay，E 拆为 Host Core、本地 Executor、Provider 信任、多节点路由；新增 Alpha/Beta/RC/GA 分级门槛、后端平台资源假设和 K 独立工期。Browser Runtime 保持可选增强轨。详见 [开发计划](system/desktop-agent-client-dev-plan.md)。

> 2026-08-12：M0 Phase B 已完成。Phase C 的独立 Desktop Shell、登录、启动状态机、Shared auth/platform contract、v3 preload 启动状态 bridge 和 production `web/**` 零引用门禁已完成开发、独立测试与 CodeReview；Phase B/C 门禁 80/80、Agent Desktop 39/39，双 typecheck、Web/Desktop production build、artifact/边界/协议门禁通过。主控补齐锁定 Electron 43.1.0 的 Windows 可执行 smoke，返回 `AGENT_DESKTOP_SMOKE_PASS`；当前没有 macOS 设备，macOS arm64 dev smoke 与固定视口截图仍待真机补证。严格 M0 尚未关闭，也未进入 M1。
> 2026-08-13：Web 生产构建隔离已完成开发：`npm run build/typecheck` 使用 Web-only TypeScript project 和 Web scope 依赖门禁，仅检查 Web 及真实 import；full scope 继续由 Phase 门禁覆盖 Desktop、Shared 与 client core。Web 模块 manifest 校验成功后从部署产物删除。该调整不修改 Web 运行源码、路由、认证或样式。
> 2026-08-14：M0 已完成。macOS arm64 真机补齐可执行 smoke、默认/720×500/150%/200% 视口、Dock 关闭恢复、`Command+Q` 与登录页窗口拖动证据；Windows smoke、双端构建和工程门禁此前已通过。M1/D1 Agent Turn Protocol 与 Remote Tool Gateway 已完成开发、独立测试和 CodeReview：语言无关 1.0 协议、真实 Agent 分步 tool-call seam、HMAC 授权票据、跨 worker 幂等/审计、cursor SSE replay 与 pending 取消均已落地。默认关闭时后端不导入/注册 D1 API，常规部署不创建 D1 表，共享 Agent/ToolExecutor 只在显式内部 D1 控制下进入新分支；启用需显式执行专用 DDL、配置并重启。D2/D3 尚未开始，因此整个 Agent Desktop 仍为 🔧 部分完成，不移入完成索引。

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 33 | 前端 Office 预览 | 📋 待开发 | 前端在线预览 Office 文档（Word/Excel/PPT） | [设计](research/frontend/frontend-office-preview-design.md) | — |
| 36 | Agent 跨平台桌面客户端 | 🔧 部分完成 | 2026-08-12 完成前端目录分层第一阶段：现有 `frontend/src/` 原样迁移到 `frontend/web/`，通过 alias 保持全部 `@/` import 和 Web 行为不变；同步 Web/Desktop 入口、Vite/Vitest/TypeScript/Tailwind 与 Desktop artifact verifier，并增加禁止恢复旧 `frontend/src/` 的结构测试。本阶段未开发独立 Desktop UI、未提取 `shared/`。2026-07-14 Phase 0～3 Windows 完成。2026-07-20 因 Desktop renderer 已包含新的侧栏、对话和多会话提交，完成 Windows NSIS 自动更新本地实现：`electron-updater`、包内只读 HTTPS 更新源、左下角按需下载/确认重启、窄 IPC、状态竞态保护，以及 `latest.yml`/`.blockmap`/`app-update.yml`/publisherName/签名一致性门禁；Web 与 Python 服务不受影响，development-unsigned 固定禁用真实更新。三智能体和主控终检通过，最终 `0.0.2` 开发包 100,415,947 bytes，SHA-256 `36e017...0ab70`，NotSigned。正式图标、证书签名、真实更新源和旧版→新版/坏签名/回滚真机矩阵仍待完成，因此生产自动更新未标完成。Phase 4 统一承接 browser runtime：代码位于 `clients/agent-desktop/browser-runtime/`，复用 Desktop 认证、installation identity、签名安装包、更新器、托盘和设置页，不产生第二个客户端或发布链路；macOS 延后到 Mac 设备逐 Phase 验证。 | [设计](system/desktop-agent-client-design.md) | [开发计划](system/desktop-agent-client-dev-plan.md) |
| 43 | 多会话后台流式 | 🔧 部分完成 | 2026-07-20 代码与单测完成，待真实环境 E2E 验收。useAgent 从全局单份流式状态重构为 per-session 状态池（`shallowReactive` Map + computed 视图代理，消费组件零改动）：切换历史会话/新建会话/切换数字员工不再弹「终止当前会话」confirm，旧会话后台继续流式；切回进行中的会话显示实时累积内容；会话列表进行中显示 spinner、后台完成未查看显示小点（查看后消失）。后端零改动（`/api/chat/stream` 按 session_id 独立管理）。7 个新单测全过，build 通过。 | [设计](system/multi-session-background-streaming-design.md) | [开发计划](plans/plan-multi-session-background-streaming.md) |
| 55 | Playwright E2E 测试框架 | 🔧 部分完成 | 2026-08-16 搭建：`@playwright/test` + Chromium 真实浏览器，连本地容器 aid-agent-api 后端（vite dev :15173 代理 /api → :8000）。图形验证码自动化方案：captcha 接口拿 id → 读数据库 captchas 表明文 → password_login。双认证：storageState 快照（`frontend/e2e/scripts/refresh-auth.mjs`）+ UI 登录 flow（`frontend/e2e/fixtures/auth.ts` loginViaUI）。用例：smoke（积分用量页/弹窗打开）+ login 2 条。框架已验证浏览器端到端跑通（登录页用例通过）。待补：测试账号 `PLAYWRIGHT_*` 环境变量、平台管理员快照（验证弹窗 7 分项列）、业务用例扩展。 | — | — |

---

## 技术栈优化

> 在"功能不变、推倒重来"前提下，对前后端技术栈的系统性优化建议。当前状态均为 💡 灵感阶段。

| # | 功能 | 状态 | 说明 | 设计文档 | 开发计划 |
|---|------|------|------|---------|---------|
| 71 | 彻底移除 demo 模式与 SAAS_ENABLED 开关 | ✅ 已完成开发 | demo 模式（免登录演示，`DEMO_ENABLED`/`VITE_DEMO_ENABLED` 控制）已无人使用且与租户模式并存造成大量死分支；系统定位即 SaaS 平台，不存在「非 SaaS 模式」。**2026-09-02 开发完成**：①配置层删 `DemoConfig`/`SaasConfig.enabled` 及 `.env`/`config.yaml` 开关；②认证层删 demo 登录分支（任意手机号+888888 自动注册 `tenant_id='demo'`），短信验证码无条件真实发送；③middleware 新增 `TenantHeaderRequired`：platform_admin 无 X-Tenant-Id 时返回 400 中文提示（替代原 demo 回退），未认证仍 401；④业务层删 saas/api 78 处 `if not settings.saas.enabled` 守卫、`_ensure_saas_enabled` 20 处、checker/permissions/agent/desktop_agent 等全部 demo 特判与 saas 早退（delegation_policy/scheduled_task 的 fail-closed 语义保留）；⑤知识库 SQL `(tenant_id='demo' OR tenant_id IS NULL)` 统一改为 `tenant_id IS NULL`（三分支结构保留，platform_admin 全局视图不受影响）；⑥前端删 demo 侧顶层路由（/customer-info 等十余个）与 `useDemoAuth`/`LoginModal.vue`，鉴权收敛到 `getAuthHeader()`/`useTenantAuth`（/t/ → 租户隔离 key，/portal → portal_token，桌面端 → 无后缀 saas_token），ChatContainer 保留本体仅删 demo 分支；⑦存量 `tenant_id='demo'` 数据不删除（变为不可见）；⑧测试同步更新（后端 30+ 文件删 patch/改断言/整删用例，前端 10+ 测试文件 mock 收敛到 useTenantAuth）；⑨文档同步（database_dev.md 租户 NULL 语义、渠道集成文档「单租户模式」改写、历史文档加已移除注记）。无数据库 schema 变更，按 commit revert 即可回滚。 | — | — |
| 40 | 后台管理界面引入 Element Plus | 💡 灵感 | 后台管理 16 个页面（表格/表单密集型）从手写 TailwindCSS 迁到 Element Plus 组件库（`el-table`、`el-form`、`el-dialog` 等），聊天主界面保持不变。搭配按需导入，约 8-9 人天（含 toast 替换）。 | [设计](tech-stack-optimization/admin-element-plus-migration.md) | — |
| 41 | toast 迁至 Element Plus ElMessage | 💡 灵感 | 替换停更的 `vue-toastification@rc`，使用 Element Plus 的 `ElMessage.success/error/info/warning`，零额外依赖增量。涉及 21 个文件约 182 处调用 + 1 处测试 mock。约 1 人天。 | [设计](tech-stack-optimization/toast-migration.md) | — |
| 67 | report_model 重构为 lite_model（轻量小模型全局单配置 + 跨 provider） | 🔧 部分完成 | `report_model` 字段名不副实——并非只用于报告，还承担日报/周报/复盘/简历评分/数字员工空态摘要等快速便宜小模型任务；且原实现为 per-provider 结构（qwen/zhipu/deepseek 各配一个 `report_model`），但 `get_report_model()` 只读**当前主 provider** 的字段。主 provider 已切 deepseek（`LLM_PROVIDER=deepseek`）后，qwen3.7-flash（约 deepseek-v4-flash 的 1/8 价格）**永远无法生效**——即使配置了也会被 DeepSeekProvider 用 deepseek base_url 请求 qwen3.7-flash，必然失败。**重构（2026-08-27，开发完成待三智能体流程 + 真机验证）**：①字段 `report_model` 重命名为 **`lite_model`**（轻量小模型），改为**全局单配置**（`llm.lite_model`，环境变量 `LITE_MODEL_CODE`），删除 qwen/zhipu/deepseek 三处 per-provider `report_model` 及 `get_report_model()`；②`lite_model` 支持 **`provider/model`** 写法（如 `qwen/qwen3.7-flash`）跨 provider 调用小模型，`LLMGateway.chat_lite()` 统一收口：同 provider 走 `self.chat`（完整链路含 failover），跨 provider 用独立 key_pool + provider 直接调（**指定即专用**、不走 failover），对 deepseek 目标自动加 `thinking:{"type":"disabled"}`；③调用点 5 处改造（summarizer/generator/work_outcome_review/subagent/recruiting_match_service）统一 `chat` → `chat_lite`，计费/落库/日志 model 字段改 `lite_model`。单测 64 用例 + 报告/llm 回归 104 + work_outcome/subagent 23 全绿，配置冒烟 `.env` 设 `qwen/qwen3.7-flash` 返回 `('qwen','qwen3.7-flash')`、未配置 fallback 主模型通过，import 终检通过。`.env` 已切 `LITE_MODEL_CODE=qwen/qwen3.7-flash`，待真机触发一次报告生成/复盘任务确认日志走 qwen3.7-flash。 | — | — |

---

## 故障复盘索引

线上故障与生产事故的复盘文档（存放于 `docs/incidents/`）：

| 故障主题 | 文档 | 关联功能 |
|---------|------|---------|
| 对话上下文重建避坑速查 | [context-reconstruction-pitfalls.md](incidents/context-reconstruction-pitfalls.md) | 消息历史加载/窗口裁剪 5 大陷阱(来源分流、最近N条、对齐user、表分离、默认值漂移) |
| qwen3.7-flash 工具结果缓存数组化回显故障复盘 | [qwen-tool-message-cache-echo-incident.md](incidents/qwen-tool-message-cache-echo-incident.md) | 2026-08-19 线上故障：显式缓存"末尾标记"把 tool 消息 content 数组化（违反 OpenAI 兼容规范），qwen3.7-flash 概率性(~7%)按 Anthropic 语义回显工具结果。已修复为"从后往前找可安全标记文本消息"；缓存范围相对退化但绝对成本 ~0.001 元/次 |
| 数据分析智能体 max_tokens 截断致空结论误判"无数据" | [analysis-agent-empty-conclusion-incident.md](incidents/analysis-agent-empty-conclusion-incident.md) | 2026-08-26 生产故障：AnalysisAgent 硬编码 max_tokens=4000，deepseek-v4-pro 推理模型烧穿预算返回空结论（completion 恰达上限 + content 空），主智能体误判"知识库无2025-07数据"。已修复：max_tokens 4000→16384 + 空总结重试兜底（仍空返回明确失败）+ execute 出口空结论降级失败 |

## 调研报告索引

以下调研报告为多项功能设计的前期研究，不单独对应开发任务：

| 调研主题 | 文档 | 关联功能 |
|---------|------|---------|
| 企业级智能体平台调研 | [enterprise-agent-platform-research.md](research/enterprise-agent-platform-research.md) | 可观测性、Prompt 管理、知识库 |
| 基础设施差距分析 | [enterprise-agent-infrastructure-gap-analysis.md](research/enterprise-agent-infrastructure-gap-analysis.md) | 整体规划 |
| Prompt 版本管理调研 | [prompt-version-management-research.md](research/prompt-version-management-research.md) | Prompt 全生命周期管理 |
| AI Agent 可观测性调研 | [observability-design-research.md](research/observability-design-research.md) | 可观测性与质量保障 |
| 知识库 RAG 技术调研 | [enterprise-knowledge-base-rag-research.md](research/enterprise-knowledge-base-rag-research.md) | 知识库能力增强 |
| 知识库行业产品调研与低改动快速增强建议 | [enterprise-knowledge-base-quick-wins.md](research/enterprise-knowledge-base-quick-wins.md) | 知识库能力增强（补充 Phase 1-2 之外的快速增强点） |
| Text-to-SQL 调研 | [text-to-sql-data-analysis-agent-research.md](research/text-to-sql-data-analysis-agent-research.md) | 数据分析智能体 |
| 企微客服 AI 绑定调研 | [wecom-kf-ai-chatbot-binding-research.md](research/wecom-kf-ai-chatbot-binding-research.md) | 企业微信客服 AI 绑定 |
| 前端样式调研 | [frontend-style-research.md](research/frontend/frontend-style-research.md) | 前端样式统一 |
| 前端 Office 预览调研 | [frontend-office-preview-research.md](research/frontend/frontend-office-preview-research.md) | 前端 Office 预览 |
| HTML 转 PPTX 技术调研 | [html-to-pptx-conversion-research.md](research/html-to-pptx-conversion-research.md) | PPT 技能 PPTX 导出 |
| 企业微信个人账号 RPA 生产级客户端技术方案调研 | [wecom-personal-rpa-client-implementation-research.md](research/wecom-personal-rpa-client-implementation-research.md) | 企业微信个人账号 RPA 接入 |
| 会话内上下文压缩业界方案调研 | [context_compression_research.md](research/context_compression_research.md) | 会话内上下文压缩（中期记忆） |
| 社媒内容运营智能体与聚合平台可行性调研 | [social-media-operations-agent-platform-research.md](research/social-media-operations-agent-platform-research.md) | 社媒内容运营智能体与聚合平台 |
| 微信桌面版 RPA 自动化技术方案调研 | [wechat-desktop-rpa-technical-research.md](research/wechat-desktop-rpa-technical-research.md) | 微信搜一搜 RPA（协会采集）；影刀失败后改自研「图像+OCR+键鼠」Python 方案，4 轮 PowerShell 真机验证通过 |
| 微信搜一搜 RPA 命令行工具 | [设计](tools/wechat-souyisou-rpa-design.md) / [连续查询会话交接记录](tools/wechat-rpa-session-handoff.md) | 🔧 部分完成：正式 `collect` 已接入 HWND 会话状态机、10 分钟预算和 Per-Monitor V2；输入采用可信键盘导航与精确回读，详情候选采用可信 HWND 内 UIA 双语义筛选和物理 `ClickablePoint`。旧 `Ctrl+Tab`、像素 card band、视觉搜索框、比例 locator 与 `flow_probe` 固定坐标路径已删除。单条“中国黄金协会 / 周洲”已真机验证详情正确打开、等待、关闭并以 `inconclusive + session_closed=true` 安全结束；待继续验证连续 9→10、普通 1→2 和连续 20 条。[开发计划](tools/association-profile-enrichment-dev-plan.md) |
| 协会官网优先资料补全 | [设计](tools/association-profile-enrichment-design.md) | 🔧 部分完成：新增严格 14 字段提取、自动站内导航、批量无界面 CLI 和 Excel 输出。输入支持文字/CSV/XLSX，解析协会清单并去重；官网按 HTTPS→HTTP 尝试，不可达时使用项目 WebSearchTool+LLM 补基础信息；微信补联系人手机。2026-08-03 已修复微信清理失败级联，并补充官网/网络回退字段级证据隔离、领导独立提取和稳定错误码。微信统一关闭完整插件并保留具体清理码，搜索输入使用可信键盘导航、粘贴回读和提交门禁，详情定位已改为 DPI-aware UIA；批处理显式传播熔断状态，所有手机证据绑定标准保持不变。此前 20 家结果因没有输入框回读证据已作废，断点续跑与新一轮真实 20 家稳定性复验仍待完成。浏览器固定 `headless=false`。2026-08-05 解析优化：按「给定信息说A就A」原则删除解析器的主观内容门禁（逐字 evidence_quote 校验、姓名-手机号绑定/最近距离判定、计数字段 token 校验、全字段拒绝门禁），只保留结构安全校验（JSON schema、字段类型、email/count/mobile 格式、source_url 域名边界、fallback 手机号业务隔离）；模型解析出的字段一律接受，真实性由模型负责，evidence_quote 仅作审计。单测 111 项通过。2026-08-24 官网领导页第一优先：LLM 逐层导航搜索（导航标签交 LLM 选航，4 层/10 点击/翻页找名单），模型切 deepseek-v4-flash 并默认关闭思考（官方参数 `thinking.type=disabled`，nav-pick 0.6~1.6s），30 协会真机验收：成功跑完采集的 25 家秘书长 25/25 全命中（含人口学会庄亚儿翻页链路、焊接协会李连胜在发展历程页、冶金教育学会孙建林兼职秘书长、残疾人康复协会侧栏大小写选择器修复）；通用根因（思考烧穿 max_tokens、BFS 浏览器漂移失配、flip_next 稳定误判、悬浮子菜单重载收起、慢 SPA 菜单渐进渲染提前返回壳、CSS class 选择器大小写漏侧栏、收录排序被新闻刷词压过、发展历程换届页被提取规则误杀、QWEN failover 模型错配）已修复并单测锁定；剩余 4 家为环境问题（2 站超时、1 死链、1 无 URL）。官网未拿到秘书长时发 official_secretary_miss 审计事件，客户端经遥测落服务端 client_usage_logs（按协会/域名聚合可查，SQL 见设计文档 2026-08-25 续三节）；客户端新增每协会余额守卫（余额不足熔断批次、提示充值后重跑，透支上限压到一个协会）。详见设计文档 2026-08-24/25 章节。[开发计划](tools/association-profile-enrichment-dev-plan.md) / [本机演示手册](tools/association-enrichment-local-demo-guide.md) / [本地调查工作台 UI 设计](tools/association-enrichment-ui-design.md) / [UI 开发计划](tools/association-enrichment-ui-dev-plan.md) |
| 懂车帝与汽车之家客户留资统一接入可行性调研 | [automotive-platform-lead-integration-research.md](research/automotive-platform-lead-integration-research.md) | CRM 智能体、汽车平台渠道集成 |
| 抖音电商飞鸽客服接入 Agent 可行性调研 | [douyin-shop-pigeon-agent-customer-service-research.md](research/douyin-shop-pigeon-agent-customer-service-research.md) | 抖店/飞鸽客服渠道、Agent 自动接待、转人工 |
| 抖音本地生活订单→门店微信群推送系统 私有化部署可行性评估 | [douyin-lifeservice-order-wechat-group-dispatch-research.md](research/douyin-lifeservice-order-wechat-group-dispatch-research.md) | 竞品（TkTok Sys）视频还原；抖音生活服务开放能力接入路径（商家自研 vs 服务商）、微信/企微推送通道选型、功能模块与工作量（一期约 8~10 人月）、预开通账号清单、手机号隐私等风险与待确认项 |
| 抖音来客订单同步与门店微信播报系统 解决方案（含开发计划与报价，面向客户） | [design/douyin-lifeservice-dispatch/solution-proposal.md](design/douyin-lifeservice-dispatch/solution-proposal.md) | 一期打包价 ¥29.8 万/203 人日/约 14 周交付；通道矩阵（企微内部群 webhook 全自动合规 / 企微客户群群发限频 / RPA 播报为可选项）；抖店发货提醒等为二期可选包 |
| 小红书客服接入 Agent 可行性调研 | [xiaohongshu-agent-customer-service-integration-research.md](research/xiaohongshu-agent-customer-service-integration-research.md) | 小红书电商客服、专业号私信、小程序客服、Agent 自动接待与转人工 |
| AI 智能体行业产品体验提升调研与「工作日报」方案设计 | [ai-agent-experience-daily-report-research.md](research/ai-agent-experience-daily-report-research.md) | 工作日报（个人日报 + 团队日报）、AI 价值证明、续费驱动 |
| 从个人经验到组织能力：AI 智能体组织知识沉淀调研与方案设计 | [org-knowledge-sedimentation-research.md](research/org-knowledge-sedimentation-research.md) | 组织知识沉淀（三层知识架构 + 专家识别 + 自动抽取 + 主动推荐）、个人经验转组织资产、续费护城河 |
| 微信公众号文章搜索「不依赖微信 App」可行性调研 | [wechat-article-search-without-app-feasibility.md](research/wechat-article-search-without-app-feasibility.md) | 搜一搜无 App 外通道；不依赖 App 全域关键词搜文章只能在「搜狗(免费不稳)/商业聚合API(付费稳)/回退App内搜一搜」间三角取舍，无完美解 |
| BOSS 直聘智能招聘 Agent 可行性调研 | [boss-recruiting-agent-research.md](research/boss-recruiting-agent-research.md) | BOSS 简历筛选助手（独立桌面应用）。含真机单变量实验：精确锁定 CDP `Runtime.enable` 为详情刷新最小触发条件；详情正文为 Canvas 渲染（WASM 解密后 fillText 绘制，DOM 仅 27 节点无文本）。设计：[原生 CDP 设计](design/recruiting/boss-resume-assistant-native-cdp-design.md) / 计划：[原生 CDP 开发计划](plans/recruiting/boss-resume-assistant-native-cdp-dev-plan.md)。🔧 开发进度（2026-08-05）：Phase 1~9 代码全部完成（214/214 测试全绿，node 22），产品形态已转为 **CLI**（Electron 壳保留不删）；首次真机因「全新 profile Chrome 登录」被 BOSS 风控封号，已改为 **attach 用户日常 Chrome** 模式（账号已解封）；真机实证 CDP 合成点击被风控 SDK 选择性拦截（卡片可点、筛选类控件不可点），确立**双通道点击**方案（CDP 定位 + Win32 真实鼠标，设计文档 §16 决策 8）；**筛选设置全流程 demo 已跑通**（5-10年+本科及以上+10-20K→确定→列表刷新，筛选·5 生效）；2026-08-05 路线收敛：Electron GUI、spawn 模式 ChromeLauncher、探针脚本、spike 工程已全部删除，CLI 为唯一形态，真机踩坑清单沉淀于设计文档 §17；Phase 10.1/10.2 已完成（2026-08-05）：WinMouseClicker + FilterSetter 产品化，**CLI `filter` 子命令真机端到端验证通过**（5-10年+本科/硕士/博士+10-20K → 筛选·5 徽章校验、列表刷新全员本科，208/208 测试绿）；新增踩坑 #14（Chrome 多 render widget 须取可见实例）#15（行锚定分行布局/卡片诱饵）；待做：YAML 筛选集成、付费检测修复、DB 假数据清理、写动作真机验证。🔧 开发进度（2026-08-06）：CLI `goto`/`accept`/`ask`/`chat` 子命令完成并真机验证（见开发计划 Phase 10 清单）；**新增 `reject` 子命令 + chat 对话「不合适」动作**（`ChatRejectExecutor`，设计文档 §10.4）：沟通页当前会话右侧面板点「不合适」标记候选人，确认弹层自动点确定，原因选择层 fail-loud 不乱选；真机验证通过（连续标记 2 人），275/275 测试绿；**新增 `interview` 子命令**（`InterviewDemoExecutor`，设计文档 §10.5）：约面试表单填充演示（逐字填备注+选日期+取消，只填不发送），真机端到端验证通过，284/284 测试绿。**【新设计 2026-08-10】招聘平台 CLI 接入智能体系统**：[设计](design/recruiting/recruiting-cli-agent-integration-design.md) / [开发计划](plans/recruiting/plan-recruiting-cli-agent-integration.md) — 统一隔离执行节点（客户内网已有 Windows VM/专用机优先 → 客户云/BYOC → 厂商托管一租户一 VM → 多平台统一协议），用户终端仅需浏览器；MVP 画面采用 WGC/GDI 低帧率 JPEG/WebP 帧流，WebRTC 后置，**MCP 风格接入**（CLI 自带 MCP server + agent 通用 MCP client，否决专用 invoke 工具，CLI 保持一等公民），分布式反向 MCP transport（sidecar 出站 WS + Redis Streams 路由至 WS owner）+ `RemoteMcpTool` 为新建核心；修订早期「不接入 agent 服务端」决策。🔧 M0 同机演示架构已审定、可开发（2026-08-10 四次收敛：当前一期只在同一 Windows 演示机原生运行 aid-work-agent 后端与 boss CLI，通过 MCP stdio 验证 agent 完整组合 CLI；不做远程 worker/WSS/Redis/安装器/画面。后续 R1 才解决客户内网或公网独立节点通信与 WGC/GDI 帧流，并以 `McpExecutionTransport` 保持工具和子智能体不变；WebRTC/人工接管继续后置；统一 CLI Provider 兼容未来 Agent Desktop：第一方/第三方 CLI 均以 MCP stdio 或受控 adapter 接入，Desktop Tool Host 复用远程 transport，boss CLI 不重写）**日期脆弱测试修复（2026-08-31）**：interview-demo 完整流程用例原用 Date.now() 取「明天」，每月末运行（明天=1号）会走跨月路径致点击序列断言多出下月箭头坐标；改为注入固定 now 走月中路径（跨月场景已有独立用例覆盖），328/328 全绿。 |
| BOSS CLI 接入 Web Agent MVP（覆盖上行旧方案） | [设计](design/recruiting/recruiting-cli-agent-integration-design.md) / [MVP 开发计划](plans/recruiting/plan-recruiting-cli-agent-integration.md) | 🔧 部分完成（2026-08-11：M0.0~M0.6 全部完成并过三智能体流程；M0.7 真机验收待进行）：M0.1 删除旧 run/ask/chat/review/export/audit/probe 及专用代码（70 文件）；M0.2 七个结构化 operation + MCP stdio server + doctor/version + 共享 conformance 套件（[规格](plans/recruiting/m02-implementation-spec.md)，137/137 测试绿）；M0.3 云端 4 表 + src/local_tools/ + 配对/设备/Runtime API（[规格](plans/recruiting/m03-implementation-spec.md)，37/37）；M0.4 clients/agent-tool-runtime（pair/start/doctor/unpair、DPAPI、长轮询、MCP Provider 管理，[规格](plans/recruiting/m04-implementation-spec.md)，22/22）；M0.5 ExecutionTarget + LocalToolProxy + 招聘操作子智能体 recruiting-operator（[规格](plans/recruiting/m05-implementation-spec.md)）；M0.6 Web「本地工具」设备管理页（[规格](plans/recruiting/m06-implementation-spec.md)）；删除清单 [m00](plans/recruiting/m00-deletion-inventory.md)。M0.7 待办：真机端到端、Codex/WorkBuddy 直连 smoke test（验收测试指南：[m07-acceptance-test-guide.md](plans/recruiting/m07-acceptance-test-guide.md)）。原设计要点：云端 Agent → PostgreSQL invocation/event → 用户 Windows 电脑 Local Tool Runtime 主动 HTTPS 长轮询 → MCP stdio → 7 个已真机成功的 BOSS 操作。旧 `run/ask/chat/review/export/audit/probe` 及专用失败/实验代码在 MVP 中删除；画面、VM、WSS、Desktop UI、第三方 CLI 不在本期。BOSS CLI 同时作为可独立销售的标准本地 MCP Provider，不依赖自有云端；MVP 增加 Codex/WorkBuddy 直连 smoke test。统一 `SERVER/LOCAL_REQUIRED/EITHER` 执行位置模型为未来 Agent Desktop 和本地文件工具打基础。写动作采用当前对话明确动作与数量即授权、模糊则澄清，并执行设计规定的默认值与单次上限。**2026-08-11 补充**：两个客户端改为 npm 全局安装包（tgz）分发——验收/用户机器无需代码库，`npm install -g` 后使用 `boss-cli` / `aid-runtime` 命令；boss 包作为 runtime 的 bundledDependency 捆绑进 tgz（file: 符号链接依赖会导致 tar `..` 路径错乱，已由 runtime 的 prepack/postpack 钩子自动落实体目录打包、打完恢复符号链接）；boss CLI 入口解析改为「包内 node_modules 优先、仓库兄弟目录 fallback」。**0.2.4 分发包与全链路工具同步（2026-08-31，为租户交付重打）**：双包升版 0.2.4 重打 tgz（0.2.3 包 8/24 打包，缺 8/27 新增的 read-chat/open-chat）；补齐全链路注册断点——runtime TRUSTED_MANIFEST 受信清单 14→16（漏配会 TOOL_NOT_ALLOWED，2026-08-17 同款坑）、云端 manifest.py/catalog.py/proxy_tool.py（16→18，新增两个只读工具类）、recruiting-operator SUBAGENT.md 白名单+授权规则+组合链路（boss_open_chat→boss_read_chat 读消息/未读清单）；云端 94 测试与 runtime 22、boss-cli 328 测试全绿；release 交付目录换 0.2.4 包并新增 VERSION.txt，安装手册版本引用刷新+补锁屏/RDP FAQ 条目。**本地工具按次计费（2026-08-31，租户交付）**：boss_* 工具调用接入租户积分体系（协会 client_usage_logs 同款台账）——settings 新增 `boss_tool_billing`（总开关/default_credit_price/每工具单价，configs/config.yaml 可覆盖，默认外部写动作 0.5~1 积分、重只读 resume_detail/batch 1~2、纯导航探查免费），LocalToolProxyTool 基类统一挂计费：收费工具扣费前查租户余额（≤0 返回 NO_CREDIT 不建 invocation），成功终态经 `ClientUsageLogDB.record_tool_usage` 写台账+同事务扣 `tenants.credit_balance`+失效缓存，失败/超时/取消不扣，计费异常只记日志不影响工具结果；`local_tool_invocations` 新增 credit_cost 列回写对账（deploy/db_update.sql + init-postgres.sql）。15 个计费单测，local_tools 全套 61 绿。**send-to 统一会话切换（2026-08-31，v0.2.5）**：boss_send_to 不再直接驱动 ChatSearchExecutor，改为调 ChatOpenExecutor.open（与 open-chat 同一条「找人会话」链路：already 零点击/搜索找人/会话列表兜底 + 头部身份校验防串会话，写动作安全增益），ChatSearchExecutor 降级为 open 内部搜索原语不再被 operation 直接引用；data 新增 via 路径；toolDefs/cli usage/云端 BossSendToTool 描述同步（display_name 改「BOSS 向联系人发消息」）；新增 boss-send-to operation 测试 3 例，331/331 全绿；双包升版 0.2.5 重打 tgz 更新 release 目录与手册。**弹层自愈 overlay heal（2026-08-31，v0.2.6）**：解决 BOSS 页面不可预见弹层（广告/功能引导）盖顶致后续操作必然失败的问题——本地工具失败（仅 UI_CHANGED/BUSY；EXECUTION_UNKNOWN 副作用不明绝不触发）时云端自动编排「boss_overlay_inspect 导出主文档文本节点清单 → 启发式（白名单+弹层类名，零 LLM 费）或 chat_lite 结构化选择关闭控件（token 记 record_background_llm_usage）→ boss_overlay_dismiss 白名单点击+快照校验消失 → 重试原操作一次」；防误点三层铁律：关闭语义白名单（CLI/云端同表）+ LLM 只能选白名单内且候选存在的文本（防幻觉）+ CLI 端拒绝执行非白名单；自愈失败返回原错误不掩盖；自愈救回才收专项费 boss_overlay_heal（overlay_heal_price 默认 2 积分，config.yaml 可覆盖），原工具费按重试结果正常计不双扣；runtime 受信清单 16→18、云端注册 18→20（overlay 原语不进 SUBAGENT 白名单，agent 不可见）；设计 [boss-overlay-heal-design.md](design/recruiting/boss-overlay-heal-design.md)；CLI 340/340、云端 99/99 全绿；双包升版 0.2.6 重打。**真机验证（2026-08-31，两处关键修正）**：真机测试立即暴露纯文本路线盲区——广告弹窗（BOSS直聘 Windows 客户端推广）的关闭 × 是无文字图标，class 是唯一信号（boss-popup__close/icon-close/ad-banner-close 等 5 个 close-like），inspect 增导 icon_candidates（class 命中 close/guanbi 正则）、dismiss 支持 icon:<cls> 引用（class 即关闭语义，安全等价文本白名单），云端启发式 icon 优先+LLM 双候选输入；对照实验证明弹层 × 用 Win32 真实点击无效、CDP 合成点击一击命中（与「写动作第一优先 Win32」不冲突——弹层关闭属页面 UI 操作），dismiss 改 CDP 主通道+未生效 Win32 兜底；真机关闭 boss-popup 弹窗与限时优惠广告条均验证通过（广告轮播位关旧换新会保守判未关闭，重复调用可关轮换后的新广告）；CLI 344/344（+4 icon 用例）、云端 82/82 全绿；0.2.6 包重打。**真机第二轮（同日）：发现并修复 win-click.ps1 存量致命 bug**——send-to dry-run 输入不落地排查出 FindRenderWidget 取「第一个可见」Chrome_RenderWidgetHostHWND，今天 Chrome 页面存在两个可见渲染面（一个 96px 高小部件 + 1278x1304 主渲染面），取错致 scale=0.848 全部 Win32 点击集体打偏（搜索入口/列表点击/输入聚焦全失效，弹窗 × 同理）——8/26-27 真机通过纯属当时首个匹配恰好正确；改为**可见集合取面积最大**（主渲染面必然最大，scale=1.000 恢复），真机复验 Win32 点击列表切换联系人 ✓、send-to 统一链路 dry-run 完整通过（切会话→输入→校验→不发送）✓。该修复对所有 Win32 工具（筛选/打招呼/接收简历/发消息）生效，属部署必带修复。 |
| 网页操作实时视图（Boss CDP 首期，原 login assist / page stream） | [设计](design/recruiting/login-assist-page-stream-design.md) / [实验纪要](research/cdp-page-stream-experiment-notes.md) | 📋 设计完成待开发（2026-09-01，v3.2/v2.2 已吸收开发前全仓代码核查与独立复审）：通用 `page-stream/1.0` 首期接入 `aid-runtime + boss-cli + CDP`，真实 Playwright 本期仅做契约验证。一次 Agent execution 内连续 Boss 调用复用 offer；正式交付 Web，legacy Desktop 可继承但不作门禁，独立新 Desktop/第三方 channel 本期不创建 offer。控制面采用 5 表事务/CAS/持久 command queue；新库 DDL 写 init-postgres.sql，存量增量只写 db_update.yaml。JPEG 经单 worker Gateway 内存中继；HMAC ticket 走 subprotocol。已冻结 7 个协议 schema、Gateway 终态投递、WSS/HTTP 状态事实源、登录 QR 算法、prod/test/dev/EAS 部署及共享测试库隔离规则，可直接交 GLM5.3。 | [开发计划](plans/recruiting/web-operation-live-view-dev-plan.md) |
| 招聘 CLI 接入 Agent：MVP 后总体架构 | [总体架构基线](design/recruiting/recruiting-cli-agent-post-mvp-architecture.md) | 🧭 总体方向已保留、待 MVP 后分阶段深化：MVP 的用户当前 PC 就是第一台 Windows 执行节点；后续只是把同一 Runtime + BOSS CLI 部署到客户内网其他 PC/Windows VM、客户 BYOC 或厂商托管 VM，复用同一配对、invocation/result 和 MCP 契约，不另建远程 CLI。新增工作主要是安装/节点分配、Windows 交互会话保障和 WGC/GDI 画面回传；人工接管/WebRTC 后置。未来 Agent Desktop 仍作为同类 MCP Host 使用同一 Provider。 |
| BOSS CLI 沟通发消息能力（send-to / send-current） | [原生 CDP 设计 §10.6](design/recruiting/boss-resume-assistant-native-cdp-design.md) | 🔧 部分完成（2026-08-13）：把已真机验证的「搜索找人 + 输入 + 发送」链路封装为两个 CLI 子命令与同名 MCP tool——`send-to <姓名> --message <消息> [--dry-run]`（搜索找人→进入对话→输入→发送）与 `send-current --message <消息> [--dry-run]`（前提已选会话→输入→发送）。默认真发送，`--dry-run` 只输入不点发送用于测试链路。新增 `ChatSearchExecutor`（搜索图标 GetCursorPos 固定坐标 / 搜索框 doc0 几何定位 / 「联系人」标签锚点 + 人名列 cx 偏移点结果卡片 / 进入对话校验发送按钮）与 `ChatSendExecutor`（发送按钮 cx>850 唯一定位 / 发送按钮 center+偏移反推输入框激活点 / CDP typeChar 逐字输入 / dry-run 校验 / 真发送点发送后未清空=EXECUTION_UNKNOWN 不重试）。关键结论：会话列表滚动 CDP+Win32 双失效改用搜索找人；CSS 搜索图标与结果卡片人名节点 DOMSnapshot 抓不到，靠锚点+偏移+固定常量定位；写动作走 Win32、输入走 CDP。154/154 测试绿、typecheck 0 错误。**待真机验证**：发送后输入框清空校验（消息气泡也进 strings，naive 校验可区分性待确认，代码已标 TODO）。**已提交**（b246356d + 36dd67a5）。**滚动说法已修正**：会话列表可滚动（`CHAT_LIST_SCROLL_POINT` + `listSignatureOf`），搜索找人与滚动互补，非"失效才搜索"。**🔧 2026-08-26 通道回退（点击/输入第一优先 Win32）**：用户定调防爬优先，撤销 v0.2.3 的「DPI 偏差」误判（实为用户移动窗口致输入框不可见，换算无偏差，§17 坑22）——搜索入口/结果卡片点击回退 Win32 click；搜索框/聊天输入框聚焦+输入改为 Win32 clickAndType（win-click.ps1 一次调用原子完成：拟人点击聚焦 → SendInput KEYEVENTF_UNICODE 逐字真实键入，keybd_event 单字节装不下中文故必须 SendInput）；CDP typeChar 通道移除，InterviewDemo 备注输入同步切换。真机复验通过（搜索找人→进对话→dry-run 输入校验，245/245 测试绿、typecheck 0 错误）。设计文档 §10.6/§16 决策9/§17 坑22 已同步。**🔧 2026-08-27 沟通会话消息读取——开发完成（304/304 测试绿，待真机验证）**：`read-chat [姓名]` CLI 子命令 + `boss_read_chat` MCP tool（只读 effect=none，Deps 仅 snapshot 零写原语）。新增 `ChatReadExecutor`（§10.9 算法产品化：消息区 conversation-message 定位 / item-myself·item-friend·item-system 祖归发送方 / text-content 正文 / message-time 组时间挂载 / status-read 已读标记 / badge-count 未读清单含视口外 / x<188 全等数字总徽章防 SVG path 干扰 / contact 校验不自动切换会话）；domSnapshot.ts 新增 attributes 解析（Chrome 151 实测 dense 嵌套数组 attributes[nodeIdx]=[nameIdx,valIdx,...]）与 classOf。全部离线 fixture 测试（.tmp/chat-snapshot-01.json 真机形状建模）。此前 2026-08-27 真机只读实验结论（实验报告见设计文档 §10.9）：核心结论：消息全在主文档 doc[0]，一次 DOMSnapshot 无需输入/滚动即可读全——item-myself/item-friend class 区分发送方（结构信号优于几何，LIST_MAX_X=850 在聊天面板失效），text-content 整串非碎片；时间戳/已读/职位卡片气泡可读；左列表未读徽章（含视口外）全量可读，可产出未读会话清单；多副本坑（列表预览/推荐 iframe 同文案）须限定 conversation-message 子树。推荐 boss_read_chat（contact 缺省读当前、不自动切换会话）。探针存 .tmp/chat-read-*.mjs。**✅ 2026-08-27 开发完成并真机验证通过（三智能体全流程，304/304 绿）**：新增 ChatReadExecutor（纯只读 Deps 仅 snapshot；domSnapshot.ts 加 attributes 解析 + classOf，Chrome 151 dense 嵌套数组定型）+ bossReadChat operation（readonly，URL 前置校验不自动跳转）+ CLI read-chat [姓名]（validate 表同步）+ MCP boss_read_chat（tool 数 15，manifest/conformance 同步）。CR 修 P1 性能：解析 O(n²) 真机快照实测 10s → 18ms（layoutIndex O(1) + ctx.parents 有界爬链）。真机验证：高长磊会话 10 条消息（含 system 碎片链/已读标记/时间戳）、未读 36 人（姓名+时间+条数+最后一条预览）、总徽章 218、contact 三路径（匹配成功/存在未打开报错/不存在报可用名单）全部符合设计。已知待验（P2 记录）：>8 字长姓名启发、'99+' 徽章形态、长会话历史虚拟化、图片/附件气泡、dpr≠1。**🔧 2026-08-27 open-chat 切会话命令 + 搜索框定位稳定化（三智能体全流程，328/328 绿，真机验证通过）**：read-chat 只能读当前会话，本命令补切换能力。CLI open-chat <姓名> / MCP boss_open_chat（tool 数 16）。三路径：already（头部已是目标零点击）/ search（搜索找人，**用户定调优先**；CR 补身份校验防错卡串会话）/ list 兜底（Escape 清浮层 → 左列表唯一姓名 → 视口外 wheel 滚动 → Win32 点击姓名 x+60 → 轮询头部校验）。**搜索框定位范式重写**（真机诊断：搜索框已变原生 <input>，点入口后收缩条原位变形，旧 diff 范式失效致 8-27 晚两种失败）→单快照直接识别（INPUT+形态唯一/多 INPUT 入口邻近/DIV 回退 cx<44%）+ 输入未落地清空重试一次（CDP ctrl+a+Delete）。ChatReadExecutor 抽共享 headerContactOf/conversationListNamesOf；dispatchKey 支持 modifiers。真机验证：already/搜索两路径 + read-chat 张楠楠 3 条消息全部通过。待验（P2 记录）：clearInput 焦点落聊天框会清草稿的场景、missingChars 单字撞车、非标准滚动下列表滚动 15 轮上限。 **2026-09-10 审核修正**：撤回后附“435/435、气泡增量/resolve-probe 已开发”声明，当前源码仍为旧校验。新增 [写后验证设计](design/recruiting/boss-send-verification-design.md) / [开发计划](plans/recruiting/boss-send-verification-dev-plan.md)，尚未实施；不影响前述其他历史能力记录。 |
| BOSS CLI 职位切换能力（list-jobs / select-job） | [原生 CDP 设计 §10.7](design/recruiting/boss-resume-assistant-native-cdp-design.md) | ✅ 已完成开发（2026-08-13 封装 + 真机验收 + 增强，待提交）：封装为 `list-jobs`（只读 `boss_list_jobs`）+ `select-job <职位名>`（写 `boss_select_job`）+ 同名 MCP tool。新增 `JobSwitcher`（openJobDropdown 幂等 / locateJobBox 筛选锚点 / parseJobItems 遍历所有 nodeValue）。真机验收全通过：list-jobs 解析 4 职位并标注开放状态；select-job 切换 + 校验通过。真机过程发现并修复 3 点：① DOM 模型——下拉打开时职位框 trigger 仍带 bounds（共 N+1 节点），selectJob 按 nodeIndex 排除框、只在下拉项精确匹配（否则切当前职位框+项命中 2 个）；② 待开放徽章——未发布职位项右侧有「待」文本节点（同 y、x+≈120-136px），list-jobs 据此标 `pending`，select-job 点击前拒绝 pending 职位（切过去致页面异常）；③ iframe 重载校验——切换后推荐列表 iframe 重载、职位框暂消失，固定延时改**轮询校验**（≤10 次 snapshot 等职位框重现且文本=目标）。172/172 测试绿、typecheck 0 错误。dev-workflow 三智能体（开发→测试→CR）全通过。未提交（待用户确认）。 |
| 招聘操作智能体「简历库」业务页 | 无独立设计文档（范式沿用 video_agent 知识中心：src/api/recruiting_operator.py + ResumeLibrary.vue）；CLI 对接交接：[resume-detail-cli-integration-handoff.md](plans/recruiting/resume-detail-cli-integration-handoff.md) | 🔧 部分完成（2026-08-16）：新表 `bs_recruiting_operator_resumes`（幂等建表 + deploy/db_update.sql 已登记）、`/api/recruiting-operator/resumes` CRUD（列表筛选分页/职位下拉/详情/PATCH 状态流转/删除，全部租户隔离；POST 支持 file_id 引用与 base64 直传两路图片，base64 落盘 `storage/tenants/{tid}/recruiting/`，mime 限 image/*、单张 ≤10MB）、前端「简历库」页（列表筛选 + 详情画廊/状态/备注/OCR + 手动录入），路由 `/recruiting-operator/resumes`（含租户路由），SUBAGENT.md 已声明 business_pages。22/22 集成测试绿、前端 build 0 错误。待做：BOSS CLI 侧联调入库（source=boss 的 images_base64 路已备好未真机验证）。**2026-08-16 分层升级**：业务逻辑抽至 `src/services/recruiting_resume_service.py`（API 薄壳），新增 `boss_resume_detail` 本地工具骨架（云端编排自动落库、紧凑摘要返回，图片不进 LLM 上下文；CLI 结果契约对齐点已标记，待明日真机联调）。**2026-08-17 批量读取入库（boss_resume_batch）开发完成、待三智能体流程+真机验证**：CLI `resume-batch` 子命令 + 第 13 个 MCP tool + 云端 `BossResumeBatchTool`——推荐牛人页逐个点开当前视口牛人卡片（CDP 浏览类点击 `clickBrowse`，真机 2026-08-17 实证卡片点击有效不需 Win32）→ 复用 ResumeReader 读取管线（滚轮/拼接/OCR）→ Escape 关闭 → 下一份，输出 `resumes` 契约数组（姓名 = 卡片行「活跃」状态锚定 DOM 配对 + OCR 首行启发式兜底），云端逐份 `create_resume_record_from_tool_result` 入库（单份失败记 failures 不中断）、返回紧凑摘要列表。CLI 203/203 测试绿 + typecheck 0 错误；云端 87 集成/单测绿。SUBAGENT.md 链路：`boss_goto(recommend) → boss_resume_batch(limit≤3)`。未提交。**P0 姓名铁律（2026-09-02 三智能体全过，未提交）**：真机反馈「OCR 可能认错姓名、错名入库致打招呼打错人，姓名无论如何不能错」——①姓名唯一来源=非 OCR：boss_resume_batch 只取卡片 DOM 配对（与 greet 同一套 cardName 锚定；配对失败记 failures 跳过不入库），boss_resume_detail candidate_name 必传（缺参校验前移到参数校验阶段 fail-fast，不再白借真实鼠标滚 30 秒）；删除 OCR 首行启发式入库路径（extractCandidateNameFromOcr 整体移除）。②新增 ocrNameMatches 交叉校验防张冠李戴：姓名须在 OCR 文本前 400 归一化字符内模糊命中（去空白 + n-1/n/n+1 滑窗 Levenshtein≤1 容一字 OCR 误差），不过→batch 记 failures 跳过 / detail INVALID_ARGUMENT 报错，绝不入库。③云端最后防线：payload 增 name_source（param/dom），create_resume_record_from_tool_result 白名单校验、其余（缺失/'ocr'/任意值）ResumePayloadError 拒绝（有意不兼容旧 CLI，需随 runtime 0.2.7 重发生效）。CLI 360/360 + typecheck、云端 36 集成（真 PG）+ 96 local_tools 全绿。**真机验证通过（2026-09-02，推荐牛人页 resume-batch limit 1+2）**：黄钰鑫/胡锦彪两份读取，DOM 配对与交叉校验全过、零误跳零错名，OCR 首行噪声确含姓名（"黄 钰 鑫 飓 飓 活 跃"/"0 胡 锦 彪 飓 飓 活 跃"）验证容差设计成立；跨次调用同卡片会重读（processedNames 仅进程内）已知行为。**真机同时坐实 P1 证据**：两份拼接长图均 3200-3500px 处「假到底」截断（底部专业技能被切、字节差<200 误判），黄钰鑫份中段工作经历还有接缝错位/重复；OCR 错字率肉眼可见（工具→工具、问题→河题、Linux→Linuw）坐实 P2 RapidOCR 必要性。后续 P1（逐段 OCR 替代整图长图识别 + 到底判定/接缝质量加固）、P2（RapidOCR 备选引擎，复用仓库根 venv + 2x 放大，doctor 探测）未开发。**🔧 P1 分段 OCR + 拼接加固（2026-09-02 开发完成待真机验证，未提交）**：①到底判定三层加固——字节差 ≥BOTTOM_SIZE_EPSILON 快路径直接入列；字节差 <epsilon 调新增 sameView dep（新脚本 scripts/cv-segdiff.ps1：裁剪 canvas 区域、降采样宽 180、\|dR\|+\|dG\|+\|dB\|>60 采样错配率 misrate，≤0.01 判同画面）像素确认；连续两次相同（再滚 8 格 + BOTTOM_CONFIRM_DELAY=1800ms 懒加载等待 + 再截一张）才确认到底并丢弃两张重复截图，确认截图不同=滚动被吞恢复入列；MAX_SEGMENTS 12→16，另设 MAX_CAPTURES=2*16+8 防死循环。②逐段 OCR 替代整图长图（规避 WinRT OCR MaxImageDimension=10000px）：cv-stitch.ps1 增可选 -CropDir 落盘 crop-00..NN.png，ResumeReader 逐段调 ocr（cv-ocr.ps1 超时 300s→120s/段），新增纯函数 mergeSegmentTexts 归一化（去空白）重叠去重合并（A 尾/B 头各 MERGE_WINDOW=1024 字符窗——CR 修复：初版 400 在真机重叠 ~750-970 归一化字下必然失配全量直拼；最大 k 下降扫描 + Levenshtein ≤max(1,⌊k*0.2⌋) ≥80% 容错（布尔判定走 Ukkonen 行最小值提前放弃防 1024 窗全扫描秒级开销，细化保留精确距离）+ 过伸下降修正防系统性吞字，k≥K_MIN=8 才可信）；单段空 OCR 计数不炸整体。③接缝质量元信息：stitchParts 解析 cv-stitch 输出 mis=（每接缝错配率），ResumeReadResult/payload 新增 seam_mis/suspect_seams（mis>0.35）/text_seam_unmatched/ocr_empty_segments，detail/batch 完成消息与 CLI 渲染追加 ⚠️ 可疑接缝警告（提示人工核对拼接图）。CLI 380/380 测试绿 + typecheck 0 错误（新用例：到底双确认/滚动被吞恢复/快路径不调 sameView/sameView 抛错 fail-loud/确认路径取消/mergeSegmentTexts 十例含真机规模重叠 700-900 字+性能防退化/MAX_SEGMENTS=16 边界；CR 必修项 MERGE_WINDOW 400→1024+提前放弃已修复）。cv-stitch/cv-segdiff PS 改动仅语法解析验证（离线无法单测）。**真机验证通过（2026-09-02，黄钰鑫+胡锦彪两人复验）**：到底判定修复——OCR 文本尾读到 BOSS 平台页脚声明、拼接图完整收尾（黄钰鑫 3235→4356px、胡锦彪 3543→4184px，P0 版均为「假到底」截断）；接缝质量门禁真实生效（首验即抓到 ⚠️ 警告并定位问题）。**真机校准修正（主控者验证轮）**：SEGMENT_WHEEL_NOTCHES 8→5——8 格 ≈1000px 接近整视口（canvas ~1230px），wheel 惯性累积时跃过整视口致相邻段**零重叠**（实测接缝 1/2 文本差异率 0.968/0.973，像素拼接器在空白区滑到假对齐 mis~0.21，且拼接图实际丢 ~700px 内容）；降为 5 格（~625px，保证 ≥50% 重叠、落在 MERGE_WINDOW 窗口内）后复验全绿：黄钰鑫 5 段 seam_mis=[0.04,0.01,0.07,0.14]、text_seam_unmatched=[]、无警告，胡锦彪同样干净通过（380/380 单测复跑绿）。**🔧 P2 RapidOCR 主引擎 + WinRT 兜底（2026-09-02 三智能体全过 + 真机双路径验证通过，未提交）**：真机基准（同批 550×1100 切片）——质量压倒性：WinRT 满篇错字（飓飓活跃/大亏/河题/Linuw、字符间全空格"5 年 工 作 经 验"），RapidOCR（仓库根 venv 同款 rapidocr-onnxruntime 1.4.4 + onnxruntime 1.20.1，2x LANCZOS + y 聚类行重建）近乎全对、检出字数多 ~13-22%、天然无字符间空格；速度 4.2s/段 vs 0.5s/段（每份 +20-30s，600s 超时内）→ 定案切换。实现：①新 scripts/cv-ocr-rapid.py 批量适配器（**单进程跑全部段**，模型只加载一次；异常非零退出）；②bossResumeDetail 引擎解析 resolveOcrEngine（env AID_BOSS_OCR_ENGINE=winrt/rapid/auto 缺省 auto；python 定位 AID_BOSS_RAPIDOCR_PY > 仓库 venv > PATH，import 探测模块级缓存）+ ocrBatch（rapid 失败**整批原子回退** WinRT 并 stderr 记原因、engine 如实标记；强制 rapid 探测/运行期失败均 fail-loud 不静默降级——静默降级会用 WinRT 错字搞坏姓名交叉校验）；③cleanOcrText 空格清理（任一侧 CJK 相关空白删除、ASCII 字母数字两侧保留单空格、CRLF→LF、不动换行）merge 前逐段清洗，WinRT 兜底文本同样受益；④doctor 增 OCR 引擎检查项（✅rapid+python 路径 / ℹ️winrt 兜底原因 / ❌强制失败）；README+安装手册增可选安装小节（pip install rapidocr-onnxruntime==1.4.4 Pillow，模型随 wheel 内置离线可用）。**真机验证（黄钰鑫双路径）**：rapid 主引擎 ocr_engine=rapid、尾部文本接近完美（"为妥善保护牛人在BOSS直聘平台提交、发布、展示的简历…"vs WinRT"为 妥 蓉 屏 护"）、3417 干净字；强制 winrt 兜底 ocr_engine=winrt 如实标记、空格清理生效、姓名交叉校验两路径均通过、零接缝警告。CLI 401/401 + typecheck 绿（新增 ocr-engine/cleanOcrText 用例 21 个）。**服务器部署要点**：目标机 pip 装 rapidocr-onnxruntime==1.4.4 + Pillow（无需联网下载模型）；缺省 auto 自动探测，未装时静默用系统 WinRT 不影响可用性；批量场景可用 AID_BOSS_RAPIDOCR_PY 显式指定 python.exe。**零成本提速优化（2026-09-02 主控者直改+独立测试回归+真机验证通过）**：RapidOCR 为纯 CPU 推理（onnxruntime CPU EP），客户机性能弱于开发机需提速——①去掉 2x LANCZOS 放大（那是企微 UI 小字场景经验；简历 canvas 字号大，1x 与 2x 正文识别一致、差异仅标点/装饰符级，置信度同 0.99）；②onnxruntime 线程显式限核 intra_op_num_threads=min(8, CPU核数)（28 核开发机实测：默认全核过订阅 4.2s/段 → 限 8 线程 2.1s/段）；开发中自修 1 个真 bug（SCALE=1 直接用源图时 PIL 懒加载像素未 load()，with 退出关文件句柄后 engine 取像素 I/O 报错）。综合效果：4.2→2.6s/段（-38%），单份简历读取真机实测 46s 全程。③新增 cv-ocr-rapid.py --bench 自检模式（合成中文图单次推理计时，模型加载不计入）+ bossResumeDetail.benchRapidOcr + doctor OCR 检查项追加「单次推理实测 X.Xs」（bench 失败只降级展示不判装机失败）——装机验收拿真实数据判断客户机 CPU 够不够用，>10s 再考虑 DirectML（onnxruntime-directml + use_dml，核显即可无需 NVIDIA，备案未启用）。GPU 路线评估：CUDA 需 NVIDIA 卡装机排除。**DirectML 实测后定案默认启用（2026-09-02 隔离 venv 基准 + 真机全链路验证）**：用户质疑「测过更好就默认」——搭隔离 dml-venv（onnxruntime-directml 1.20.1 覆盖 CPU 版）实测：Intel 核显预热 0.30s/段 vs CPU 0.87s（**~3 倍**）、识别字数完全一致 → 定案装了就默认用。实现：①适配器 detect_dml（onnxruntime providers 含 DmlExecutionProvider 即启用；env AID_BOSS_OCR_DML=0 强制关）+ use_dml 按模块前缀传 det_/cls_/rec_（全局无前缀版不生效，读 rapidocr UpdateParameters 源码实证）+ stdout 首行 engine=dml|cpu + DML init 失败落 CPU、推理中途失败**整批 CPU 重跑**（不留混合输出）；②ocrBatch 解析 engine= 行返回 accel → ResumeReadResult.ocrAccel → payload ocr_accel（dml/cpu/none）；③doctor OCR 项显示「DirectML GPU 加速 / CPU，单次推理实测 X.Xs」；④README+安装手册加 onnxruntime-directml==1.20.1 可选安装行。**真机对比（同一人简历全链路）**：DML 35.5s/份 vs CPU 46.3s/份（省 ~11s），ocr_chars 均 3413、零警告。开发/测试轮共抓 3 个真 bug 并修复：PIL 懒加载句柄关闭后取像素报错（SCALE=1 直用源图时）；make_engine 局部导入 NameError；**DML 中途回退 CPU 时适配器打两行 engine=、Node 按任意行 grep 误报 dml**（改 parseAccel 取最后一行=最终引擎，回归测试对旧代码必红）。CLI 408/408 + typecheck 绿。部署注：repo venv 不装 directml（与 wecom-cli 共享 venv，避免未测变更），客户机按手册可选安装。**✅ 已提交并重打包发版（2026-09-03）**：ecba3660 提交全部代码（rebase 到 origin/master 最新 12 提交无冲突）；双包升版 0.2.9→0.2.10 重打 agent-tool-runtime-0.2.10.tgz（prepack 自动构建捆绑 boss-cli），冒烟验证通过（临时目录安装+doctor 可执行+新脚本 cv-ocr-rapid.py/cv-segdiff.ps1 与新函数在包内）；release 目录更新（新 tgz/zip、VERSION.txt 置顶 0.2.10 变更清单、安装手册版本引用 3 处）；**门禁撤销（2026-09-03 评审定案）**：name_source 白名单强控撤掉改为观测不拦截（服务端 warning 日志、照常入库）——用户定调「服务端更新不得强制客户端更新，姓名正确性由客户端流程保证」：①门禁校验的只是客户端自报标签，不验证真实质量，防御价值低；②把旧 CLI 的「质量降级」变「功能不可用」，违背宁降级不宕机；③制造后端↔客户机部署耦合。姓名正确性四层防线全在客户端（batch DOM 配对/detail 显式传参/缺参 fail-fast/交叉校验）已真机验证。云端测试翻转（拒绝→接受+warning），向后兼容恢复。**✅ 方案A 捆绑 OCR 环境 + 发版 0.2.11（2026-09-03 三智能体全过 + 真机/冒烟验证，已提交）**：用户定调客户机手动 pip 装 Python+包门槛过高——①新 scripts/build-ocr-python.mjs 构建便携环境（python-3.12.6 embeddable + pip --target 预装 rapidocr-onnxruntime 1.4.4 + onnxruntime-directml 1.20.1（DML 核显加速开箱即用）+ opencv-headless 等精简依赖链，_pth 编辑 + fail-loud 自检，幂等/--force/自检失败自动清目录重建），产物 ocr-python/ 363MB gitignore 不入库随 tgz 分发；②探测顺序 env > 包内捆绑 > 仓库 venv > PATH（开发机此后验证的就是发货物）；③打包链 files 白名单 + prepack 前置 build:ocr-python --if-present（失败中断防打出缺 OCR 的包）；④doctor OCR 项标注「包内置环境」。测试轮修 2 bug（自检失败重建不清目录叠加残留、下载不校验 HTTP 状态非原子写），CR 修 3 处（测试残渣污染包根/doctor 标注/解压失败提示）。CLI 414/414 + runtime 26/26 + typecheck 全绿；真机全链路 ocr_accel=dml 35.8s/份质量一致；冒烟安装后从 node_modules 直跑捆绑 env engine=dml bench=0.55s。tgz 3.3MB→147.8MB（一次性代价，离线可用）。客户升级流程简化为：停 Runtime → npm install -g 新 tgz → 启动 → doctor（零 Python/pip 知识要求）。**🔧 方案 A：捆绑便携 OCR Python 环境（2026-09-03 开发完成未提交，客户机零 Python/pip 要求）**：RapidOCR 全家桶预装进发布包——①新 `clients/boss-resume-assistant/scripts/build-ocr-python.mjs`（幂等：已存在且自检过跳过、--force 重建；下载 python-3.12.6-embed-amd64.zip 缓存 clients/.cache → System32 tar 解压【Git Bash GNU tar 会把 C:\ 当远程主机，必须绝对路径】→ 仓库 venv python `pip install --target ocr-python/Lib/site-packages`：rapidocr-onnxruntime==1.4.4 **--no-deps**（其声明 opencv-python 带 113MB Qt 废料、onnxruntime 无 DML）+ 手动装齐真实依赖链（numpy/opencv-python-headless/Pillow/PyYAML/pyclipper/Shapely/six/tqdm + **onnxruntime-directml==1.20.1**）→ 编辑 python312._pth 追加 Lib/site-packages（可嵌入发行版默认隔离，标准必做）→ fail-loud 自检：providers 必含 DML+CPU + 适配器 --bench 跑通）；产物 `ocr-python/` 363MB 已 gitignore；②CLI 探测序改为 env AID_BOSS_RAPIDOCR_PY > 包根捆绑 `ocr-python/python.exe`（dist 上溯 4 级/src 3 级）> 仓库 venv > PATH，开发机构建后也优先走捆绑环境（有意——验证的就是发货物）；③打包链路：boss files 白名单加 ocr-python + `build:ocr-python` script，agent-tool-runtime prepack 前置该构建（--if-present 与 --prefix 组合已验证正常执行）；④CLI 414/414 + typecheck 绿（新增候选顺序用例 6 个，rapidPythonCandidates 最小导出 + exists 注入）；端到端：bundled bench engine=dml 0.45s / AID_BOSS_OCR_DML=0 engine=cpu 0.81s，真机切片 2 张识别 chars=706/772 中文正确；npm pack 重打 tgz 3.2MB→**147.8MB**（解包 397MB），冒烟临时目录安装验证 `node_modules/agent-tool-runtime/node_modules/boss-resume-assistant/ocr-python/python.exe` 存在且 import 出 DML+CPU providers；⑤文档：README「编译新版本」加步骤 0、安装节 RapidOCR pip 段改「已内置无需安装 Python」、安装手册同步删 pip 步骤。 |
| 招聘「职位管理/简历库」UI 重构（列表/详情路由化 + job_id 强关联 + 删 demo 预置） | 评审定案（2026-08-31，会话内评审，无独立设计文档） | ✅ 已完成开发（2026-09-01 全部子项完成，含④期/A组/回写；开发过程：后端招聘模块 169 集成测试绿（真实 PG）+ 前端 typecheck/build/23 单测绿；未提交）：①删除职位库 demo 预置 ensure_default_job（每租户首访不再自动插入「PHP开发工程师（Laravel）」+13 条话术，空职位库合法，测试反转为空库断言）；②职位-简历 job_id 强关联：list_resumes 加 job_id 精确筛选、update_resume 加 job_id 三态（None=不改/空串=清 NULL/非空=校验本租户回填规范名）、API Create/UpdateResumeRequest + GET /resumes 透传（CR 修复 job_name+job_id 同传时同列二次赋值致 PG 500）；③职位管理拆分：JobLibrary.vue 重写为纯列表页，新增 JobDetail.vue（基本信息/沟通话术/关联简历 三 tab）+ JobFormModal.vue，demo+租户共 4 条详情路由；④简历：ResumeLibrary 筛选/手动录入改职位选择器（job_id 不再自由文本）、筛选条件同步 URL query，新增 ResumeDetail.vue（简历详情/匹配评估 两 tab 路由详情）。待做（下一期）：沟通记录/邀约信息两张新表及详情页对应 tab、详情页加载失败统一 toast、「返回列表」保留筛选 query。**第④期已开发完成（2026-08-31 三智能体流程全过，后端招聘回归 184 集成绿（真实 PG）+ 前端 typecheck/build/34 单测绿，未提交）**：新增表 `bs_recruiting_operator_resume_comm_logs`（沟通时间线：direction out/in、channel、content、user_id 操作人）与 `bs_recruiting_operator_resume_invitations`（邀约：interview_at/interviewer/method/status 五态/notes，均 FK 引用简历表 CASCADE），服务层 `recruiting_resume_timeline_service`（幂等建表，已注册进 src/db/database.py 启动链、顺序在简历表之后；deploy/db_update.sql 已登记）+ 6 个 API 端点（comm-logs/invitations 子资源 CRUD，子端点先判简历 404 fail-closed）；前端 ResumeDetail 扩四 tab（简历详情/匹配评估/沟通记录/邀约信息，新 tab 懒挂载子组件 ResumeCommTab/ResumeInviteTab：时间线+补录弹框+删除、邀约卡片+新增/编辑弹框+状态即时 PATCH），P2 打磨完成（详情页加载失败统一 toast、返回列表优先 router.back() 保筛选 query）；测试修 1 真 bug（编辑邀约清空面试时间静默不生效→空串置 NULL 语义对齐），CR 无 P0/P1。**待做：agent 端 boss_send_to 自动回写沟通记录（涉及工具链路，需真机验证）、notify 日志与邀约联动（notify 表无 resume_id）**。**真机回写打通与时间戳修正（2026-09-01）**：boss CLI ChatReadExecutor 时间行采集扩展（真机确认 `.message-time` 为「MM-DD HH:MM」；正则放宽兼容 HH:MM/今天|昨天/M月D日 + 时间行按 y 序向后继承，每条消息带 ts）；后端 create_comm_log/API 加 `occurred_at`（ISO，缺省 NOW）落原始聊天时间；真机验证朱华宁/李泽楠两条链路落库（含已入库记录时间修正），timeline 集成测试 21 绿；CLI typecheck/build 绿。**A1 发送自动回写已开发+真机验证（2026-09-01 三智能体全过，未提交）**：BossSendToTool 真发送成功后云端尽力回写 comm_logs（direction=out/channel=boss/content=发送原文/user_id=受信用户；无同名简历静默跳过、回写异常只 warning 绝不改发送结果与键集；boss_send_current 设备结果无联系人姓名不回写已注明）；新增 test_recruiting_send_writeback 6 用例，招聘回归 194 绿；真机全链路验证通过（脚本驱动云端工具→invocation 共享库→设备 runtime→CDP 真发李泽楠→回写自动落库 id=104）。白名单 catalog/manifest 零改动（未新增工具）。**顺带修复既有 P1**：set_invocation_credit_cost 对 UUID id 做 int() 恒抛 ValueError 被吞→本地工具计费对账回写（invocation.credit_cost）从未成功过且工具结果缺 credit_cost 字段，已改直传 UUID 字符串（实际扣费 client_usage_logs 不受影响）。待做：话术模式/INVALID_ARGS 不触达钩子的回归用例（CR P2）。**A 组四项已开发（2026-09-01 三智能体全过，未提交）**：①boss CLI goto 幂等（已在目标页直返；点击未生效且在 /web/chat 区→CDP Page.navigate 页内兜底再校验，methodPolicy 白名单+设计文档 §6.1 均已登记）；②邀约删除 API（DELETE /invitations/{id}）+前端删除按钮；③notify 面试通知与简历联动（notify_logs 加 resume_id FK SET NULL 幂等 ALTER，BossInterviewNotifyTool 单候选人按姓名解析、多候选人恒 None，GET /resumes/{id}/notify-logs + 邀约 tab「企微通知留痕」区）；④P2 打磨（saveStatus/saveRemark catch、邀约徽标函数下沉 recruitingDisplay、timeline 排序加 id DESC）。测试：后端 210 + 前端 37 + CLI 全量 348 全绿（顺带修正 2 个 chat-read-executor 过时 ts 断言——属时间戳继承提交的遗留）。**真机演示验收通过（2026-09-01，本地最新后端代码）**：boss_jobs_list（PHP开发工程师，简历35·匹配19）→ goto recommend 幂等 → boss_filter 按职位要求三档位（筛选·4，薪资 10-15K 校准为 10-20K）→ boss_resume_batch 3 份入库+LLM 评分全 matched（72/78/78）→ matched-only 定向打招呼张运豪（姓名配对，招呼 11:17 送达，read-chat 验证）→ send_to 自动回写（李泽楠 comm_logs id=104）。**验收发现（重要）**：生产 runtime 0.2.3 内置旧版 boss CLI 已过期——boss_filter 直接 INTERNAL_ERROR、greet 用旧「按钮数减少」校验误报 EXECUTION_UNKNOWN 且实际未发出（同操作仓库 CLI 均成功）→ 需发版 runtime 0.2.7（含今日 CLI 全部改进）并在 agent2 发布最新后端后，匹配体系/演示闭环两条验收条目方可正式移入 ideas_finished；另 greet 默认招呼不在 send_to 回写范围（后续增强候选）、经验档位 Win32 点击偶发失效（重试即成功，观察）。 |
| 招聘客户端部署封装（npm install 一键装机） | [设计](design/recruiting/recruiting-client-deployment-design.md) / [部署手册 clients/README.md](../clients/README.md) / [同事装机手册](../clients/release/安装手册.md) | 🔧 部分完成（2026-08-24 分发包 v0.2.3，send-to 沟通搜索链路全面通用化重写）：**send-to 通用化**（另一台机器分辨率不同点错搜索按钮暴露：①搜索图标固定坐标 519,135 废弃→「批量」锚点+小图标聚簇+按钮形态，阈值全视口比例化（850/940/200/100 绝对像素全灭）；②Win32 点击 DPI/缩放换算偏差（点击不生效）→搜索入口/搜索框聚焦/结果卡片/聊天输入框激活点全改 CDP 点击（真机实证有效，写动作仍 Win32）；③BOSS 改版搜索框/聊天框为 contenteditable（非 input）→diff「新增输入条形态」定位（不挑标签）+逐字落地校验+「浮层必须关闭」进对话强校验+新增 strings 字符覆盖率校验（页面原有内容不参与，杜绝假放行）；④弹层开关型残留→Escape 清场+无新增重试一次。真机全链路验证：搜索刘梦阳→进对话→dry-run 输入肉眼确认 ✅，243/243 绿）。v0.2.2：`clients/release/` 正式分发包（agent-tool-runtime-0.2.2.tgz，含定向打招呼 names/姓名配对/画布 400 等真机修复 + **Chrome 调试实例自动拉起** + **collect_logs 日志采集**，冒烟+真机验证通过；tgz 不入库）+ 同事版傻瓜装机手册（Node 20+ 升级强调→装包→登录一次 BOSS→配对→后台自启→验收清单，Chrome 快捷方式降级为兜底）。**日志采集**（collect_logs.bat/ps1：日志+config+环境/包版本+status/doctor 输出打包桌面 zip，PS5.1 需 [Console]::OutputEncoding=UTF8 防 cmd 子进程 UTF-8 输出按 GBK 误解码；绝不采集 credentials.bin）。**本机事故教训（2026-08-24）**：全局 0.1.0 runtime 心跳 404 数日后进程挂死（日志停写 3 小时、进程僵尸）→ 升 0.2.x + Stop/Start-ScheduledTask 恢复；RUNTIME_VERSION 常量曾硬编码 0.1.0 与包版本漂移误导排障 → 改运行时读 package.json（src/dist 双布局兜底）；Stop-Process 杀 node 会带走整个计划任务宿主（自愈循环接不了手），重启必须 Stop+Start-ScheduledTask（README §六/§七已更正）。**Chrome 自动拉起**（ChromeLauncher：App Paths 注册表定位 chrome.exe → detached spawn 固定持久 profile C:\chrome-debug + zhipin 初始页 → 轮询端口 ≤15s，fail-open 不改变原失败语义；AID_BOSS_AUTO_CHROME=0 可关；probe 2s 超时防悬挂端口；决策 6 修订二——固定 profile 不属临时 profile 风控禁区，仍绝不杀进程/碰日常 profile）：测试隔离踩坑记录——MCP SDK StdioClientTransport Windows 默认只继承 12 个白名单 env，开关变量到不了子进程，需测试侧显式透传 env + run-tests.mjs 全局注入；239/239 绿。engines node>=20 已声明（Node 16 无全局 fetch 不支持）。README 部署手册与 start_runtime.bat 自愈脚本、register_runtime_task.ps1 登录自启此前已落地并在本机验证。📋 设计完成待开发（2026-08-19）：runtime+boss CLI 封装为客户交付包——install.ps1 一条命令（Node 检查→装 tgz→写 server 配置→Chrome 调试实例快捷方式→schtasks 登录自启→doctor 自检→配对引导）；分钟级看门狗自愈+日志轮转；upgrade.ps1 + 云端 min_provider_version 软门禁激活（设备页黄牌）；doctor --report 诊断包。不做 SYSTEM 服务（须跑用户会话操作桌面 Chrome/鼠标）/私有 registry/Intune（后置）。三 Phase：安装闭环→升级门禁→诊断打磨。 | 
| 招聘面试邀约企微通知（两点式） | [设计](design/recruiting/recruiting-interview-notify-design.md) | 🔧 部分完成（Phase 1 核心闭环已开发，2026-08-19 三智能体全过——测试修 2 真 bug + CR 修 1 个 P1（executor 强转 Pydantic 模型实例致生产路径通知必败，测试直传 dict 掩盖），66 核心测试绿，待真实企微群 webhook 验收；三次对齐收敛：定时日报→逐成果推送→**仅面试场景两点式，筛选/打招呼/发消息一律不发**）：①邀约前知会+②邀约后通报统一为云端工具 `boss_interview_notify` 两种模式 kind=pre\|done（混合模式同 boss_jobs_list，不碰设备）——**开发期设计调整**：原「事后由 interview_demo 工具层全自动内嵌」不可行（interview_demo 入参仅 remark 无名单/日期，候选人名只在 agent 对话上下文），改为 SUBAGENT 链路规定调用（pre 在邀约前/done 在逐人完成后），失败不阻塞邀约；②新建 `src/services/wecom_bot.py`（markdown >2040 字节按行截断分多条 + text 用 mentioned_mobile_list@手机号，errcode 校验+失败重试 1 次+异常不外抛）与 `src/services/recruiting_notify_service.py`（settings/webhook Fernet 加密+API 掩码 `***`+末4位、push_interview_notify 编排写 notify_logs 留痕、resend_log 补推）；两表 bs_recruiting_notify_settings/bs_recruiting_notify_logs（幂等建表+db_update.sql+启动注册）；API GET/PUT /notify-settings + POST /notify-logs/resend；SUBAGENT.md 面试链路两步通知+授权规则；工具注册 6 处同步（15→16）。集成 22 绿 + wecom_bot 单测 14 绿 + 注册联动绿。待做：Phase 2 配置界面（前端通知设置卡）；真机企微群验收。不做企微侧回复审批（确认在对话里）。 |
| 浏览器 RPA（纯键鼠路线）深度调查报告 | [browser-rpa-vs-anti-bot-deep-research.md](research/browser-rpa-vs-anti-bot-deep-research.md) | BOSS 简历筛选助手、微信搜一搜 RPA、通用 RPA 底座。沉淀「特殊防爬网页纯键鼠 RPA 六件套」方法论；核心结论：纯键鼠在浏览器层（事件 isTrusted=true + 环境零注入）优于 CDP/stealth，但不解决网络/行为层；Boss 详情核心正文是 Canvas（复制可行性待 5 分钟真机验证，但详情页非 Canvas 的 DOM 碎片可能可复制）；现有 CDP 方案的"不启用 Runtime 域"约束脆弱（依赖经验观察），是否转纯键鼠取决于复制实验结果 + 对"技术栈统一/抗平台更新"的权重（决策矩阵见报告 §6.1） |
| LLM 调用计费缺失审计与修复计划 | [开发计划](plans/llm-billing-gap-audit.md) | 📋 待开发（2026-08-14 审计 + 当日逐点复核 + 与上次计划交叉核对）：原 13 处缺口与 2 处风险全部核实真实，复核新发现 1 处连带 P1 bug（followup_manager 同步调用 async 函数致 LLM 实际未执行）。**交叉核对上次计划（a76b9b1/e183508）后又新发现 5 处遗漏**：数据 schema 管理路径 embedding 3 处（schema_saver/update_schema/_update_chunk_embedding）+ ASR 工具入口 1 处（speech_to_text_tool 本身无计费，仅 channel_routes 外层补）+ backfill 运维脚本 1 处。合计 18 处缺口。上次遗漏根因 7 条（方法论盲区/服务vs工具边界/管理后台 API 未纳入/travel-quote 局部覆盖/ASR 入口单一假设/embedding 视角盲区/schema 管理路径未审计），文档 §6 详述并建议建立计费审计 checklist |
| 上传文件存储合规审计 | [审计报告](plans/upload-storage-compliance-audit.md) | ✅ 已修复并发生产、已验证（2026-08-14 审计 + 当日修复 + 当日发生产验证）：主体合规（Phase 1~8 改造有效，持久化入口均走 storage.py 工具函数）。原 1 处违规（P1）：`tenant_config_file.py` 硬编码拼路径 + 无场景子目录，已修复为 `_get_config_path` 走 `ensure_tenant_storage_dir`/`get_tenant_storage_abs_path` 落 `templates/` 场景（与 `subagent_template_file.py` 一致）；同时 `storage_migration.py` 新增 Phase E `_relocate_legacy_config_files`，启动时把历史落盘在 `tenants/{tid}/{subagent}-api.md` 根目录的违规文件幂等搬到 `tenants/{tid}/templates/` 子目录，复用 `_migrate_file` 同大小跳过 + Redis path 字段同步，必须在 Phase D 之后运行。回归 54 passed（含新增 5 测试：bare 路径迁移/二次运行幂等/templates 内文件不被误搬/非 -api.md 文件不搬/tenants 根缺失安全跳过）+ subagent_template_file 19 passed |
| deepseek-v4-flash 平替模型调研 | [deepseek-v4-flash-replacement-research.md](research/deepseek-v4-flash-replacement-research.md) | LLM 模型选型、计费降本（2026-08-16 调研完成：选定 qwen3.7-flash 0.2/0.8 + enable_thinking:false + 显式缓存，较 deepseek 3.0/9.0 降幅约 91%） |
| 小米 MiMo-v2.5 平替可行性调研（扩展） | [mimo-v2.5-replacement-research.md](research/mimo-v2.5-replacement-research.md) | 评估小米 MiMo-v2.5 平替 deepseek-v4-flash（2026-08-16 调研+实测完成）。**小米官方 API（api.xiaomimimo.com，国内人民币计费）确认可用**；mimo-v2.5 官方 1.0/2.0（命中 0.02）**实测后降级为容灾/多模态备选**：缓存确定性命中 99.8%、关闭思考后能力达标，**但 tokenizer 中文膨胀 2.5~4.5x 致实际成本 ≈0.005~0.007 元 ≈ deepseek 无优势**，叠加限流 RPM100 承载不足（账号级全 Key 合并，仅数十并发）；mimo-v2.5-pro（3/6）成本高排除；百炼中转价（7/21）加价 2.3 倍排除。**qwen3.7-flash 维持首选**（0.00054 元/次，显式缓存确定性命中）。注意：MiMo 缓存为自动（隐式）无 cache_control、命中量在 prompt_tokens_details.cached_tokens（不在顶层）、思考默认开可 thinking:{type:"disabled"} 关。官方文档摘要：[ext/mimo-mi-api-docs.md](ext/mimo-mi-api-docs.md)；文档入口速查：[ext/llm-doc-entrances.md](ext/llm-doc-entrances.md) |
| LLM 提供商文档入口速查 | [ext/llm-doc-entrances.md](ext/llm-doc-entrances.md) | 记录百炼/DeepSeek/小米 MiMo/火山方舟官方文档入口 URL（2026-08-16 均验证 HTTP 200 可达），供 curl 兜底抓取资料用。相关快照：[ext/qwen-llm-price.md](ext/qwen-llm-price.md)、[ext/qwen-llm-context-cache.md](ext/qwen-llm-context-cache.md)、[ext/mimo-mi-api-docs.md](ext/mimo-mi-api-docs.md)、[ext/volc-llm-price.md](ext/volc-llm-price.md) |
| 火山方舟模型平替调研摘要 | [ext/volc-ark-replacement-research.md](ext/volc-ark-replacement-research.md) | 评估火山方舟文本模型平替 qwen3.7-flash（2026-08-16）。**结论：火山方舟无全面平替**——最接近的 doubao-seed-1.6-flash（0.15/1.5）输入更便宜但输出贵 ~1.9 倍，agent 场景混合单价贵 ~40%（加显式缓存后 ~60%）；doubao-seed-2.0-mini（0.2/2.0）输出贵 2.5 倍；唯一输出便宜的 1.6-lite（0.30/0.6）输入贵 1.5 倍。火山方舟整体定价偏贵，缓存为透明前缀（0.03）比 qwen 显式命中（0.02）贵；其托管的 deepseek-v4-flash 8/21 调价至 3.0/9.0 更贵。**qwen3.7-flash 维持首选**。快照：[ext/volc-llm-price.md](ext/volc-llm-price.md) |
| BOSS CLI 部署方案调研：员工电脑本地虚拟机 vs 云桌面 | [boss-cli-vm-vs-cloud-desktop-deployment.md](research/boss-cli-vm-vs-cloud-desktop-deployment.md) | 100 账号/100 台员工电脑场景（2026-09）。结论：本地 VM 可行且默认推荐（VM 窗口无需常开，VM 内会话须保持登录未锁屏；注意 RDP 断开锁屏坑）；云桌面可行但机房 IP 有 BOSS 风控风险须先小规模验证，100 台约 3000-7000 元/月（无影/华为 Flexus/天翼/移动） |
| BOSS 矩阵账号—企业微信招聘协同客户方案 | [客户解决方案](solutions/recruiting/boss-wecom-recruiting-service-solution.md) / [实施与验证计划](plans/recruiting/boss-wecom-recruiting-service-rollout-plan.md) | 🔧 部分完成（2026-08-31）：形成约 100 个 BOSS 账号、约 10 台在线 VM 的客户方案，云端虚拟机报价 1,000 元/台/年，按 10 台初算约 10,000 元/年，最终按试点确认的实际台数计费。流程为 BOSS 获取候选人授权联系方式→企微手机号加好友→企微持续沟通→确认面试后发送地址与导航。BOSS 与企微加好友/文本消息已有实测基础；“每 VM 约 10 账号”仍须 1—2 周试点验证独立桌面会话与资源上限；不同 VM 必须具有独立、稳定公网出口，不能共用同一 NAT 后宣称 IP 已分散。面试地点首期采用“详细地址 + 地图导航链接”；Windows 企业微信个人客户聊天原生位置卡片当前未验证，不列入上线承诺。 |
| 桌面 CLI 无人值守底座与微信/BOSS 就绪度调研（2026-09-08） | [调研](research/weixin-cli/automation-readiness-2026-09-08.md) | 核实微信能力与 Windows/Mac 边界；补核 BOSS 未读/写后证据/候选人/话术/通知现状，通用能力归中立底座设计与计划；本轮无真实发送。 |
| 竣工图纸差异对比（转图片 + 多模态）测试报告 | [cad-multimodal-drawing-comparison-test-report.md](research/cad-multimodal-drawing-comparison-test-report.md) | 图纸差异对比功能（2026-09-04 实验）。实测 GLM 多模态对比 DWG 渲染图：图名/专业/轴网/户型/设备编号识别可靠，能发现语义级真实差异（雨篷投影）；小号尺寸标注不可读、无法保证不漏差异，定位为"初筛+语义描述+人机联审"。关键发现：样本两文件为建施 vs 结施不同专业，流水线必须前置图幅切分/图名识别/专业分类/配对 |
| CAD 处理与多模态图纸对比技术报告 | [cad-processing-and-multimodal-drawing-comparison-tech-report.md](research/cad-processing-and-multimodal-drawing-comparison-tech-report.md) | 图纸差异对比功能（2026-09-04）。全链路技术底稿：ODA 转 DXF → ezdxf 解析与图幅切分 → matplotlib 渲染（中文 SHX 大字体乱码解法：字体样式按**文件名**替换 simhei.ttf；天正代理图形保留；裁切宽高比修正）→ 多模态对比方法论（任务分解/提示词设计/分辨率策略 4200px+/能力边界）→ 双轨落地建议（矢量数据走实体级比对，扫描件走多模态初筛） |
