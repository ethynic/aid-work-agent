# BOSS 写后验证开发计划

> 2026-09-10；[设计](../../design/recruiting/boss-send-verification-design.md)。状态：方案审核修订完成，代码未开始；不代表用户授权真机试发、部署或提交。

## 第一批：send 与自查

- [x] 核对当前源码及 agent2 调用账本，明确 unknown 统计和重复调用的证据边界。
- [x] 修订设计及两处不匹配源码的开发完成登记。
- [x] 开发前答疑已入设计：量化门禁、A/B/C 分支、正文比较矩阵与读回关联评估；均为计划要求，未代表真机验证通过。
- [ ] 登记调试 Chrome 设备/连接入口/可观察会话及已有授权范围；仅缺真机信息时继续 fixture、阶段处理和契约工作，不机械重复请求授权。
- [ ] 主控在真机操作前核对上述清单，登记授权来源、允许动作、测试对象/文案/次数（如涉及发送）、核对人及日期。已有明确授权直接确认适用，不要求用户重复批准；缺失或有歧义只询问具体缺项，执行者不自行扩大范围。当前桌面环境与试发范围尚未登记确认。
- [ ] **取证第一优先：确定 B/C 分界。** 检查发送中、发送失败或其他可靠提交确认信号及其状态迁移；`status-read` 存在不能证明未读消息已发送成功，未观察到失败标记不能证明不存在失败。先查既有样本，缺少的故障状态仅在授权测试中补采；分别记录已观察/未观察/无法判断及证据来源。若无法区分乐观气泡与静默失败且无替代可靠证据，进入 C，不能以等待更久或重复快照冒充确认。
- [ ] 取证目标身份、编辑器、消息稳定标识/尾部关联、发送中/失败状态，按设计 §4.2 选择 A/B/C；C 可完成基础改造但阻断发送自动确认发布。
- [ ] 完成正文取证矩阵：首尾/连续空格、空行/多行、普通/ZWJ emoji、零宽字符、分片/实体；记录原文到编辑器/气泡的转换与判等反例，缺失写入样本在授权测试阶段补齐。
- [ ] 抽共享解析，保留 read_chat 原兼容行为，添加严格验证视图及解析失败原因。
- [ ] send_to/send_current 接入目标复验、输入核对、紧邻点击基线、单调截止轮询与提交阶段错误处理。
- [ ] 成功及 unknown 证据完整接入 operation/runtime/结果持久化/proxy，验证是否确实无需服务端变更。
- [ ] 修改提示词：先有界读回，历史同文与“未看到”不升级结论，未知不重发。
- [ ] 开发自测、独立测试、独立 CodeReview，主控验证受影响入口及必要启动检查；按项目 dev-workflow 执行。
- [ ] 经独立测试对象授权后执行真机门禁，保留证据级别与限制；不把客户端可见解释为送达。
- [ ] 按设计 §8.1 固定用例集，≥20 次正常发送（两入口各≥10、至少两轮），直接 applied≥95%、人工核对≤5%、错误 applied=0、自动重复=0；拒绝/unknown 不移出分母，独立核实真值，验证8s截止。测试报告逐例列出输入类别、入口、结果、证据和耗时，不仅报百分比。
- [ ] 故障集独立全量通过；每个正常非 applied 完成归因，无未解决的确定性误判缺陷；灰度首100次及后续窗口复核，样本不足不宣称生产比例已证明。

## 第二批：greet

- [ ] 针对定向/非定向取证，确认同一卡片身份与按钮状态变化能可靠关联。
- [ ] 实施有界验证、逐目标结果、partial/unknown 证据与停止后续写动作。
- [ ] 回归推荐区插入、同名、卡片位移、付费墙、失败/取消/断连；独立测试、CR 和授权真机验收。
- [ ] 评估只读恢复关联：读取 invocation_id 与原发送不是同一标识；检查原发送引用、目标/设备/租户、基线/时间窗和新增状态证据是否足够。只产可行性结论及后续实施范围，弱关联不能直接核销 unknown。

## 估算与排期

当前不承诺人日；取证第一优先项完成并选定 A/B/C 后，主控补充分批工作量区间、依据与风险，分别列出开发、独立测试/CR、真机验收工作量，以及等待环境/授权的日历时间。此项不阻塞不依赖真机的准备工作；若进入 C，只估算已明确可交付的基础改造，不给自动确认发布虚构日期。

## 完成条件

每批完成同步更新本清单与 docs/ideas.md。只有全部约定开发及验证完成，才把对应条目移至 docs/ideas_finished.md；#76 整体聊天自动化仍含其他未完成工作，不随本补丁整体结项。未开展的测试不能记通过。用户明确要求“提交代码”后再按仓库规范提交推送。
