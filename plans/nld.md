### 重构主子智能体架构
所有智能体不管是主智能体和还是subagent，都应该是agent.py的一个实例，按照以下要求重构主子智能体：1.主智能体和子智能体都是agent.py的示例，主智能体和子智能体的产别在于systemprompt，主智能体有如何委派任务给子智能体的使用规则，子智能体示例是没有如何委派任务给子智能体的说明，因为子智能体不可以委派任务  2.子智能体只能是主智能体创建出来，在独立线程中运行，一旦任务委派给子智能体了，用户继续交互的上下文就是提供给子智能体的了，直到子智能体完成任务并将结果返回给主智能体  3.委派给子智能体的任务有子智能体独自的plan和执行，并且执行过程要记录到主智能体规划的任务执行记录中  4.处理打招呼闲聊外，主智能体必须要plan，然后plan出来的每个任务，如果有子智能体可以处理委派给子智能体处理，等待子智能体返回结果，如果是工具或者skills可以处理，那么主智能体直接调用工具处理，知道所有任务全部完成后汇总最终结果给到用户