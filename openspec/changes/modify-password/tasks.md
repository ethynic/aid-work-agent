## 1. 前端路由配置

- [x] 1.1 在 `frontend/src/main.ts` 中新增 `/t/:tenant_id/reset-password` 路由，映射到 `ResetPassword.vue`
- [x] 1.2 验证路由在租户前台可正常访问

## 2. ResetPassword.vue 适配已登录模式

- [x] 2.1 引入 `useTenantAuth` composable，获取当前登录状态和用户手机号
- [x] 2.2 添加 `isLoggedIn` 计算属性，判断用户是否已登录
- [x] 2.3 页面标题根据模式动态切换：未登录显示"重置密码"，已登录显示"修改密码"
- [x] 2.4 已登录模式下，手机号输入框预填充当前用户手机号并设为 `disabled`
- [x] 2.5 修改成功后，如果是已登录模式，先调用 `logout()` 清除登录态，再跳转登录页
- [x] 2.6 确保未登录用户的"忘记密码"流程完全不受影响

## 3. MenuSidebar.vue 新增修改密码入口

- [x] 3.1 在侧边栏底部区域（"退出登录"按钮上方）新增"修改密码"按钮
- [x] 3.2 仅对租户模式（`isTenantMode`）且已登录（`tenantIsLoggedIn`）用户显示
- [x] 3.3 点击后跳转到 `/t/${tenantId}/reset-password`
- [x] 3.4 添加合适的图标和悬停样式

## 4. 测试与验证

- [x] 4.1 租户管理员登录后，侧边栏显示"修改密码"按钮，点击可进入重置页面
- [x] 4.2 普通租户用户登录后，侧边栏同样显示"修改密码"按钮
- [x] 4.3 已登录用户进入重置页面时，手机号已预填充且不可修改
- [x] 4.4 已登录用户完成修改密码后，自动登出并跳转到登录页
- [x] 4.5 用新密码可以正常登录
- [x] 4.6 未登录用户通过登录页"忘记密码"进入重置页面，流程完全正常
- [x] 4.7 未登录用户在重置页面中手机号可手动输入
- [x] 4.8 演示模式（`/` 路由）下不受影响
- [x] 4.9 平台管理员登录后的 Portal 界面不受影响（本次不改动 PortalLayout）

## 5. 文档与清理

- [x] 5.1 确认所有改动符合前端开发规范
- [x] 5.2 清理临时调试代码
- [x] 5.3 验证构建通过：`cd frontend && npm run build`
