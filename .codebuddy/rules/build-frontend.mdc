---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-06T12:33:20.274Z
provider: 
---

# 构建代码

自动更新前端版本号并构建项目。

## 触发条件

当用户说 "构建代码"、"打包代码"、"发布前端" 时使用。

## 执行步骤

1. **更新 VERSION**：将 `frontend/vite.config.js` 中的 VERSION 变量更新为当前时间，格式为 `yyyyMMddHHmm`（如 `202603061530`）

2. **执行构建**：运行 `npm run build --prefix frontend`

3. **返回结果**：告知用户构建完成，并说明新的版本号

## 命令

```powershell
# 更新 VERSION 并构建
node update-version.js
npm run build --prefix frontend
```