---
description: 发布代码到生产服务器。执行完整的部署流程：Git 提交、推送和服务器部署。
alwaysApply: false
enabled: true
updatedAt: 2026-03-11T02:46:36.810Z
provider: 
---

# 发布代码技能

当用户请求发布代码或部署项目时，执行以下工作流程：

## 工作流程

### 1. 自动决策部署信息
**无需询问用户，自动执行以下决策**：
- **提交信息**：根据代码更改内容自动生成合适的提交信息
  - 优先分析代码更改，生成语义化的提交信息（如 `feat:`, `fix:`, `refactor:` 等）
  - 如果无法确定具体类型，使用默认：`update: 代码更新`
- **部署确认**：始终执行完整部署流程（提交 + 推送 + 服务器部署）

### 2. 执行部署

#### 步骤 1：更新前端版本号
修改 `frontend/vite.config.ts` 中的 `VERSION` 常量为当前时间（年月日时分）：

```bash
python scripts/update_version.py
```

> **辅助脚本说明**：如 `scripts/update_version.py` 不存在，需先创建：
> ```python
> from datetime import datetime as dt
> import re
> t = dt.now().strftime('%Y%m%d%H%M')
> with open('frontend/vite.config.ts', 'r') as f:
>     content = f.read()
> content = re.sub(r"VERSION\s*=\s*'[^']*'", "VERSION = '%s'" % t, content)
> with open('frontend/vite.config.ts', 'w') as f:
>     f.write(content)
> print(t)
> ```

#### 步骤 2：提交代码到 Git 仓库
使用 `commit.py` 提交代码（支持中文和空格）：

```bash
python scripts/commit.py "提交信息" --push
```

**参数说明**：
- `"提交信息"`：Git 提交信息，支持中文和空格
- `--push` 或 `-p`：提交后自动推送到远程仓库
- `--status` 或 `-s`：（可选）提交前显示 Git 状态

**commit.py 功能**：
1. 添加所有更改到暂存区（`git add -A`）
2. 提交更改（`git commit -m "提交信息"`）
3. 推送到远程仓库（`git push`）

#### 步骤 3：前端编译
执行前端构建：

```bash
cd frontend && npm run build
```

#### 步骤 4：FTP上传代码
使用 `deploy.ps1` 通过FTP上传代码：

```bash
powershell.exe -ExecutionPolicy Bypass -File deploy/deploy.ps1
```

## 使用示例

**用户**："发布代码"
**助手**：
- 自动分析代码更改并生成提交信息
- 执行 `commit.py --push` 提交代码
- 执行 `deploy.ps1` FTP上传代码
- 报告部署结果

**用户**："发布代码，提交信息：feat: 添加用户管理功能"
**助手**：
- 使用用户指定的提交信息
- 执行部署流程
- 报告结果

## 注意事项

1. **提交信息规范**：建议遵循 Git Commit 规范
   - `feat:` 新功能
   - `fix:` 修复 bug
   - `refactor:` 重构
   - `docs:` 文档更新
   - `config:` 配置更改
   - `update:` 通用更新

2. **部署前检查**：
   - 确保所有测试通过
   - 检查是否有未解决的冲突
   - 确认要发布的内容

3. **回滚准备**：
   - 记录当前版本号
   - 准备回滚方案

4. **编码支持**：
   - `commit.py` 已处理 Windows 终端 UTF-8 编码问题
   - 支持中文提交信息和空格

## 相关文件
- 提交脚本：`scripts/commit.py`
- 部署脚本：`deploy/deploy.ps1`
- Git 规范：`.codebuddy/rules/git-commit-convention.mdc`
