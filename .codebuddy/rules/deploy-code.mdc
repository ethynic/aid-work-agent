---
description: 发布代码到生产服务器。执行完整的部署流程：Git 提交、推送和服务器部署。
alwaysApply: false
enabled: true
updatedAt: 2026-03-11T02:46:36.810Z
provider: 
---

# 发布代码技能

当用户请求发布代码或部署项目时，执行以下工作流程：

## 工作流程

### 1. 自动决策部署信息
**无需询问用户，自动执行以下决策**：
- **提交信息**：根据代码更改内容自动生成合适的提交信息
  - 优先分析代码更改，生成语义化的提交信息（如 `feat:`, `fix:`, `refactor:` 等）
  - 如果无法确定具体类型，使用默认：`update: 代码更新`
- **部署确认**：始终执行完整部署流程（提交 + 推送 + 服务器部署）

### 2. 执行部署

#### 步骤 1：提交代码到 Git 仓库
使用 `commit.py` 提交代码（支持中文和空格）：

```bash
python scripts/commit.py "提交信息" --push
```

**参数说明**：
- `"提交信息"`：Git 提交信息，支持中文和空格
- `--push` 或 `-p`：提交后自动推送到远程仓库
- `--status` 或 `-s`：（可选）提交前显示 Git 状态

**commit.py 功能**：
1. 添加所有更改到暂存区（`git add -A`）
2. 提交更改（`git commit -m "提交信息"`）
3. 推送到远程仓库（`git push`）

#### 步骤 2：触发服务器更新
使用 `deploy.ps1` 触发生产服务器更新：

```bash
powershell.exe -ExecutionPolicy Bypass -File scripts/deploy.ps1
```

**服务器操作**：
- 从 Git 仓库拉取最新代码
- 安装依赖（如有更新）
- 构建前端项目（如有更改）
- 重启后端服务（如有更改）

### 3. 部署流程说明
完整流程：
1. **检查 Git 状态** - 查看当前更改
2. **添加所有更改** - `git add -A`
3. **提交更改** - `git commit -m "提交信息"`
4. **推送到远程** - `git push`
5. **触发服务器部署** - 调用生产服务器部署钩子

### 4. 处理结果
- **成功**：告知用户部署已完成，显示：
  - Git 提交 hash
  - 推送状态
  - 服务器部署结果（构建时间、状态等）
- **失败**：显示错误信息，建议检查：
  - Git 状态和冲突
  - 网络连接
  - 服务器部署钩子状态

## 使用示例

**用户**："发布代码"
**助手**：
- 自动分析代码更改并生成提交信息
- 执行 `commit.py --push` 提交代码
- 执行 `deploy.ps1` 触发服务器更新
- 报告部署结果

**用户**："发布代码，提交信息：feat: 添加用户管理功能"
**助手**：
- 使用用户指定的提交信息
- 执行部署流程
- 报告结果

## 注意事项

1. **提交信息规范**：建议遵循 Git Commit 规范
   - `feat:` 新功能
   - `fix:` 修复 bug
   - `refactor:` 重构
   - `docs:` 文档更新
   - `config:` 配置更改
   - `update:` 通用更新

2. **部署前检查**：
   - 确保所有测试通过
   - 检查是否有未解决的冲突
   - 确认要发布的内容

3. **回滚准备**：
   - 记录当前版本号
   - 准备回滚方案

4. **编码支持**：
   - `commit.py` 已处理 Windows 终端 UTF-8 编码问题
   - 支持中文提交信息和空格

## 相关文件
- 提交脚本：`scripts/commit.py`
- 部署脚本：`scripts/deploy.ps1`
- Git 规范：`.codebuddy/rules/git-commit-convention.mdc`
