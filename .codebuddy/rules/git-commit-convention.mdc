---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-24T08:50:00.000Z
provider: 
---

# Git Commit 规范

**核心原则**：使用 `scripts/commit.py` 工具提交代码，支持中文和空格，避免终端解析问题。

**重要**：
- **不要自动提交或推送，所有提交操作由用户手动执行**
- 用户会自己运行 `python scripts/commit.py "提交信息" --push` 或类似命令
- AI 不要在完成任务后自动执行 git 提交或推送

## 推荐方式：使用 commit.py

### 1. 基本用法

```bash
# ⚠️ 注意：commit 后必须 push，否则代码不会同步到远程仓库

# 正确方式：提交并推送（推荐）
python scripts/commit.py "提交信息" --push
# 或
python scripts/commit.py "提交信息" -p

# 提交前查看状态（但不推送 ❌）
python scripts/commit.py "提交信息" --status
# 或
python scripts/commit.py "提交信息" -s

# ❌ 错误方式：只 commit 不 push
python scripts/commit.py "提交信息"  # 没有 push，代码停留在本地
```

### 2. 提交信息模板

**推荐使用中文，清晰描述更改内容**：

| 场景 | 推荐格式 | 示例 |
|------|----------|------|
| 新功能 | `feat: 中文描述` | `python scripts/commit.py "feat: 添加用户登录功能"` |
| 修复 bug | `fix: 中文描述` | `python scripts/commit.py "fix: 修复数据库连接超时问题"` |
| 重构 | `refactor: 中文描述` | `python scripts/commit.py "refactor: 优化数据库查询性能"` |
| 文档更新 | `docs: 中文描述` | `python scripts/commit.py "docs: 更新 README 文档"` |
| 配置更改 | `config: 中文描述` | `python scripts/commit.py "config: 更新环境变量配置"` |
| 删除文件 | `remove: 中文描述` | `python scripts/commit.py "remove: 删除旧的配置文件"` |
| 通用更新 | `update: 中文描述` | `python scripts/commit.py "update: 更新依赖包版本"` |

### 3. 完整示例

```bash
# 示例 1：添加新功能并推送
python scripts/commit.py "feat: 添加用户认证模块，支持 JWT token 验证" --push

# 示例 2：修复 bug
python scripts/commit.py "fix: 修复用户登录时密码验证失败的问题"

# 示例 3：重构代码
python scripts/commit.py "refactor: 重构订单服务，提升代码可维护性"

# 示例 4：更新配置并推送
python scripts/commit.py "config: 更新数据库连接池配置，增加最大连接数" -p

# 示例 5：查看状态后提交
python scripts/commit.py "docs: 更新 API 接口文档" --status --push
```

### 4. commit.py 功能说明

**自动执行的操作**：
1. 添加所有更改到暂存区 (`git add -A`)
2. 执行提交 (`git commit -m "消息"`)
3. 如果使用 `--push` 参数，自动推送到远程仓库

**支持的参数**：
- `--push` 或 `-p`：提交后自动推送到远程仓库
- `--status` 或 `-s`：提交前显示 Git 状态
- `--help` 或 `-h`：显示帮助信息

**输出信息**：
- 📋 当前 Git 状态（使用 --status 时）
- 📤 添加更改到暂存区
- 💾 提交更改（显示提交信息和 hash）
- 🚀 推送到远程仓库（使用 --push 时）
- ✅ 操作成功提示
- ❌ 操作失败提示

## 备选方案：直接使用 git commit

如果 `commit.py` 不可用，可以手动执行 git 命令：

**重要**：手动提交时也应优先使用中文，只有在终端环境不支持中文时才使用英文作为备选方案。

### 1. 终端环境注意事项

不同终端环境对 Git 命令的解析可能不同：

**Windows CMD/PowerShell**：
```bash
git commit -m "feat: 添加功能"  # ✅ 优先使用中文
git commit -m "feat:add-feature"   # ✅ 中文不支持时的备选方案
```

**WSL (Windows Subsystem for Linux)**：
```bash
git commit -m "feat: 添加功能"  # ✅ 优先使用中文
```

**Git Bash**：
```bash
git commit -m "feat: 添加功能"  # ✅ 优先使用中文
```

**遇到中文显示问题时使用备选方案**：
```bash
# 方案 1：配置终端支持 UTF-8 编码
git config --global core.quotepath false
git config --global i18n.commitencoding utf-8

# 方案 2：如果中文仍然有问题，使用英文作为备选
git commit -m "feat: add feature"

# 方案 3：使用连字符代替空格（英文）
git commit -m "feat:add-user-login"

# 方案 4：使用简单的英文描述
git commit -m "update"
git commit -m "remove"
```

### 2. 正确示例

```bash
# ✅ 推荐格式（优先使用中文）
git commit -m "feat: 添加用户认证功能"
git commit -m "fix: 修复数据库连接超时问题"
git commit -m "refactor: 优化查询性能"

# ✅ 中文不支持时的备选方案（英文）
git commit -m "feat: add user authentication"
git commit -m "fix: resolve database connection timeout"
git commit -m "refactor: optimize performance"

# ✅ 无空格格式（终端解析问题时的备选）
git commit -m "feat:add-auth"
git commit -m "fix:db-timeout"
```

### 3. 错误示例

```bash
# ❌ 特殊字符（可能导致解析错误）
git commit -m "feat: 添加功能!@#"
git commit -m "fix: bug #123"

# ❌ 过长的提交信息
git commit -m "this is a very long commit message that might cause issues in some terminal environments"
```

### 4. 提交前检查

```bash
# 查看将要提交的内容
git status

# 查看详细更改
git diff --staged

# 添加文件到暂存区
git add .
git add filename
git add -A

# 提交（优先使用中文）
git commit -m "feat: 添加功能"

# 推送到远程
git push
```

### 5. 常见问题解决

**问题 1**：`pathspec did not match any file(s)`
- 原因：提交信息被误解析为文件路径
- 解决：配置 Git 支持 UTF-8：`git config core.quotepath off`，或使用英文作为备选

**问题 2**：`paths ... with -a does not make sense`
- 原因：混用了 `--all` 和文件路径
- 解决：只使用 `git commit -m "消息"` 或 `git commit --all -m "消息"`

**问题 3**：中文字符乱码
- 原因：终端编码问题
- 解决：
  1. 配置 Git 使用 UTF-8：`git config --global i18n.commitencoding utf-8`
  2. 配置终端使用 UTF-8 编码
  3. 如果问题仍然存在，使用英文作为备选方案

### 6. 推荐工作流（手动方式）

```bash
# 1. 查看状态
git status

# 2. 添加更改
git add .  # 或 git add -A

# 3. 提交（优先使用中文）
git commit -m "feat: 添加功能"

# 4. 如果中文有问题，配置 UTF-8 编码
git config core.quotepath off
git config i18n.commitencoding utf-8

# 5. 如果问题仍然存在，使用英文作为备选
git commit -m "feat:add-feature"

# 6. 推送
git push
```

## 快速检查

```bash
# 查看最近的提交记录
git log --oneline -5
```

## 实施检查清单

- [ ] 优先使用 `python scripts/commit.py "提交信息"` 提交代码
- [ ] 提交信息优先使用中文，清晰描述更改内容
- [ ] 遵循提交信息格式（feat/fix/refactor/docs/config/remove/update）
- [x] **必须使用 `--push` 参数推送到远程仓库**
- [ ] 提交前可使用 `--status` 检查更改
- [ ] 手动提交时优先使用中文，仅在终端不支持时才使用英文作为备选方案
- [ ] 手动提交后必须执行 `git push`