---
description: 前端和后端代码日志规范
alwaysApply: true
enabled: true
---

# 代码日志规范

## 前端代码 (JavaScript/Vue)

### 1. 复杂逻辑代码 - 长期保留日志

在复杂业务逻辑处允许增加长期保留的 `console.log`，**必须**注明"前端日志"：

```javascript
// 前端日志：用户登录流程
console.log('前端日志：开始验证用户凭证', { username });

// 前端日志：计算结果
console.log('前端日志：订单总价计算', { items, total });
```

### 2. 临时调试日志

临时调试用的 `console.log`，**必须**注明"临时调试"，在 bug 修复后删除：

```javascript
// 临时调试：检查 API 返回值
console.log('临时调试：API 响应', response);

// 临时调试：变量状态
console.log('临时调试：当前步骤', step);
```

**重要**：临时调试日志仅在调试期间保留，bug 修复后立即删除。

---

## 后端代码 (Python)

### 1. 复杂逻辑代码 - 长期保留日志

在复杂业务逻辑处使用 `logging` 模块记录日志，**必须**注明"后端日志"：

```python
import logging

logger = logging.getLogger(__name__)

# 后端日志：用户登录流程
logger.info('后端日志：开始验证用户凭证', extra={'username': username})

# 后端日志：计算结果
logger.info('后端日志：订单总价计算', extra={'items': items, 'total': total})

# 后端日志：异常捕获
logger.error('后端日志：数据库连接失败', exc_info=True)
```

### 2. 临时调试日志

临时调试用的日志，**必须**注明"临时调试"，在 bug 修复后删除：

```python
# 临时调试：检查请求参数
logger.debug('临时调试：请求参数', extra={'params': params})

# 临时调试：变量状态
logger.debug('临时调试：当前步骤', extra={'step': step})
```

**重要**：临时调试日志仅在调试期间保留，bug 修复后立即删除。

---

## 日志级别参考

| 场景 | 前端 | 后端 |
|------|------|------|
| 常规业务流程 | `console.log` | `logger.info` |
| 异常/错误 | `console.error` | `logger.error` |
| 临时调试 | `console.log` (标注"临时调试") | `logger.debug` (标注"临时调试") |

