# 协会信息收集客户端 CLI 依赖
# PyInstaller 打包时用；运行时只需这些 + 项目 src/ 模块

httpx>=0.27.0
playwright>=1.40.0
openpyxl>=3.1.0
# 注意：以下依赖来自项目 src/，PyInstaller 打包时通过 pathex 引入
# - src.services.association_*（协会收集核心）
# - src.llm.gateway（ProxyLLMGateway 接口兼容）
# - src.tools.ocr.ocr_tool（OCR，经服务端代理调用）
